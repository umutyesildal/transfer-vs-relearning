# Task packet sets

- `study-v2/`: active 19-stage eval-v2 packet set.
- `study-v1/`: preserved retired eval-v1 packet set; never dispatch by default.

Select exactly one packet through `.agents/GOAL.md`. A directory is never a task packet and packet
presence never grants execution authority.
