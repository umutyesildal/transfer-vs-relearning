[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transfer-vs-relearning"
version = "0.1.0"
description = "Infrastructure for multilingual factual transfer versus relearning experiments."
requires-python = ">=3.11"
dependencies = [
  "accelerate",
  "datasets",
  "huggingface_hub",
  "lm-eval @ git+https://github.com/EleutherAI/lm-evaluation-harness.git@6d642546f4688648fced259eb3302efd36ece5af",
  "mwparserfromhell==0.7.2",
  "mwxml==0.3.8",
  "numpy",
  "pandas",
  "pyarrow",
  "pyyaml",
  "scikit-learn",
  "tqdm",
  "transformers",
]

[project.optional-dependencies]
dev = ["pytest"]
torch = ["torch"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
