# RETIRED DEFAULT READ — preserved legacy Luna handoff

> Stop: this file is historical and must not be loaded for ordinary agent work. Start with
> `AGENTS.md`, `documentation/current/START_HERE.md`, and
> `documentation/current/AGENT_BRIEF.yaml`. Read this legacy handoff only when a current task cites
> a specific section. Its body is preserved below and grants no current authority.

# LUNA Worker — Current Handoff and Living Task Prompt

> **Purpose:** This is the single living operational handoff for LUNA-Worker 2. It is not a
> numbered scientific result and never supersedes `AGENTS.md` or the chronological reports under
> `documentation/`.

## Usage and Update Rules

1. Read `AGENTS.md` first. If this file conflicts with `AGENTS.md` or a later numbered authority,
   stop and report the conflict.
2. The user must explicitly say to execute the current task. Merely reading this file is not
   authorization for HU access, downloads, evaluation, training, deletion, or publication.
3. Before acting, verify that the task status below is `READY_FOR_USER_HANDOFF` or `AUTHORIZED` and
   remain inside its scope.
4. After completing an authorized task:
   - update **Current Project State** with the verified result;
   - change **Current Task Status** to `COMPLETED` or `BLOCKED`;
   - fill **Last Worker Completion Summary**;
   - fill **Single Next Authorization Request**;
   - append a compact entry to **Task History**;
   - do not invent or execute the next task.
5. Preserve previous numbered scientific documents. Corrections must be append-only or recorded in
   a later numbered document; do not silently rewrite failures.
6. Never place credentials, `.env` contents, raw PII, or sensitive corpus text in this file.

## Current Project State

**As of:** 2026-08-09, Europe/Berlin

- Completed Qwen Wikipedia-only M2/M3 pilot remains historical evidence, not the final thesis
  design.
- New literature-first design uses parallel sibling arms:
  - `M2-A`: general Turkish adaptation without synthetic factual re-exposure;
  - `M2-B`: matched adaptation in which controlled Turkish factual rows replace neutral tokens.
- Historical vngrs operational/sample-manifest gate: **PASS / closed**; current 151ah/151ak
  preparation gate: **BLOCKED** pending shard-stratified route feasibility and exact footer/byte
  evidence. Model-neutral 151ak calibration does not require tokenizer fertility; tokenizer-yield/
  dose adequacy remains a later 151ah/materialization or model-specific planning blocker.
- CulturaX: `excluded_access_blocked`; CulturaX–vngrs comparative selection is unavailable.
- Measurement-design gate: `blocked_by_measurement_design`.
- Global training gate: **BLOCKED**.
- Documents 152–154: unauthorized and uncreated.
- Documents 151h/151i record the completed repair wave and current post-repair decision.
- Document 151j is `SUPERSEDED_UNEXECUTED_REQUIREMENTS_DRAFT`; it produced no scientific results
  and must not be executed.
- Document 151m is the frozen bounded Phase-1 evidence-resolution contract and was executed once.
- Documents 151n/151o preserve the Phase-1 execution result and post-execution gate as historical
  preliminary/provisional records. Document 151p is the current local validation and blocker
  correction authority. It reproduces the frozen Relation V2 synthetic inventory exactly and
  closes the synthetic-provenance blocker and exact 65,717-pattern inventory component. The
  benchmark and source-model registries remain incomplete collection/extraction from the executed
  bounded wave; the correction does not claim that official sources are unavailable. Document 151q
  is the frozen bounded benchmark/source-model metadata registry contract with three append-only
  corrections; it was executed once under the user's explicit Single Next Authorization Request.
  Its original SHA-256 is
  `b55499242100263e0d9adbe946679b6175268012d1c3e897298413a2af1ef60c`, its first corrected SHA-256 is
  `0acf5251bea811e07b6442681ec02c7bc4fa2ea584a55e8b48cbcb704d4209e3`, its second corrected SHA-256 is
  `c217f4d8395a8e3b657f96fd46f3e6443a11fcde6bbfbd6f7a8414933ccf89ee`, and its final third-correction
  SHA-256 is `f1cdfe082a78fce612d7bc53766e88dae3182ffcf52a225f2aa81e24c2491561`. The third correction
  limits earlier no-HU/SSH language to the preparation/correction passes and allows only a future
  separately authorized 151q wave to use the documented HU route, mandatory preflight, public HTTP
  within the existing limits and the frozen new scratch root. Document 151r records the result and
  Document 151s records the post-execution gate. The effective operational gate is
  `blocked_by_operational_access` because the frozen EXAMS `test_with_para` response exceeded the
  32 MiB single-response bound and the wave failed closed. Benchmark scoring is unconditionally
  forbidden. Future alias/template/overlap definitions and capability measurement remain unresolved.
  The global gate remains `blocked_by_measurement_design`.
- Document 151t was the frozen minimal registry-completion retry contract. Its pre-correction
  SHA-256 is `eef968538b2022250803504ba1f206860c053663bb9ce74f761c3ae25c4c11cc`; its final
  Hugging Face route-correction SHA-256 is
  `63951ba5543c2c803e8466d0c43e0aace9637ca1239164dc1d9f5e49ea75f46b`. It preserved 151q/151r/151s
  and the first execution root
  `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_v1` read-only. Under one
  explicit authorization it was executed once using the new retry root
  `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_retry_v1`. Documents
  151u/151v record the PASS result and post-retry gate. The retry completed only the missing EXAMS
  artifact and three raw model-card responses; it cannot close `blocked_by_measurement_design` or
  set `ready_to_train = true`.
  Current local SHA-256 values are `151u = 579be50a33a8bc26c71b7f47969bfca4a9e30fde06172e3cbe21dfa772976909`
  and `151v = 5f822449cd5295cee26b9f550c5883d4c897276fb0ee754b20a8540393edb871`.
- Document 151w is the current coverage validation authority. Its HU inspection was read-only
  and found six retry entity-summary rows, missing coverage metadata on all 132 first-wave rows,
  incomplete 27-field benchmark retry rows and incomplete 23-field model retry rows. The 151u/151v
  coverage PASS is preserved historically but is now `PROVISIONAL / UNSUPPORTED BY THE FROZEN
  COVERAGE RULE`; successful EXAMS/raw-README/hash/storage facts remain valid scope facts. The
  narrow gate is `BLOCKED` by `blocked_by_coverage_schema`, with contributing
  `blocked_by_benchmark_registry`; 151w SHA-256 is
  `2b19bfbea496bb76efc4e06d24d815d2b83b06090e6ebdee6526773c5fb96de3`.
- Document 151x was the current frozen minimal repair contract and was executed exactly once under
  explicit authorization. Its pre-correction
  SHA-256 is `a19ed3b7e15540fa2810d5f483b2015cc5badd2bd41949d8678f945d3a6fb32e`; its final
  protocol-correction SHA-256 is `9c66cb95ee264d8411750d8e79aff258eaaeb4d1789ffc77bc8e162ffc93555b`.
  The append-only correction names `output_artifact_manifest.jsonl`, freezes a self-reference-free
  final-audit chain, requires exactly 150 required field-level coverage rows and mandatory HU
  storage/path/inode preflight. Its repair root
  `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_coverage_repair_v1` now contains
  the nine ordered outputs. Document 151y records the execution result and Document 151z the
  post-repair gate. The execution is `BLOCKED` by `blocked_by_benchmark_registry` and
  `blocked_by_source_model_provenance`; the global gate remains `blocked_by_measurement_design`.
  The root has 9 files / 203,993 bytes with inventory SHA-256
  `7bce6b0d70c8069595d9c8ca96801b2eca1faf5a31973b8741e176926ef26e82`; both existing HU roots
  remain unchanged and read-only. No training or Documents 152--154 are authorized;
  `ready_to_train=false` and no further 151x execution is authorized.
- Document 151at is the current frozen, unexecuted local/public-metadata-only correction contract
  for the observed Hugging Face CDN redirect. It preserves 151an’s exact immutable revision and
  32 paths, allows zero or one validated HTTPS 302 hop only to `xethub.hf.co`/`cdn.hf.co`, keeps
  signed Location metadata secret-safe, separates 121 logical request attempts from 242 HTTP hops,
  and retains the existing response/file/inode/time ceilings. The local implementation, validators
  and tests are in follow-up commit `de4a14e3370326173bdf04ce33356aae7826ddda`, which was published
  by ordinary non-force push. 151at SHA-256 is
  `d846b3636aa4d55b4fdf95eebf00241ed6c85e6243b3c6a54dbd99bc546576fa`. The one authorized wave
  fast-forwarded HU with the 42-entry dirty state unchanged, but failed closed on the mandatory
  home-usage `du` timeout before PyArrow/source access. 151au SHA-256 is
  `a83d832efa7478e86fa6bfa555cfe70d900f5284bf1bc862a8e5ca696d43fd8e`; 151av SHA-256 is
  `d25789ddece62628a9cd6913eb1cbb94306816413a5b0b7768fec85a2709944a`. The operational gate
  remains `blocked_by_operational_access`; the global gate remains `blocked_by_measurement_design`.
- Document 151aw is the current documentation-only literature/model/corpus/roadmap alignment
  authority. It keeps OLMo/Pythia/Falcon as non-selected screening candidates, Qwen as the
  multilingual positive control, vngrs as conditional, trwiki as control-only and CulturaX as
  access-blocked. It also records that a future 151at wave must explicitly authorize publication
  and preservation-checked HU synchronization before execution. It grants no execution authority.
  SHA-256 is `d28cf560dc8ee3eba0ca435d81df2651f88d30ef6f412f1b83e8c4bd0b6255a8`.
- Document 151ax is the current frozen, unexecuted local HU storage-preflight resilience
  correction contract. Its pre-clarification SHA-256 is
  `15bdc5a7ae0e0356254c5d5ffd5ad47b091f459a52689ce4c0cb1ecc9699ed22`; its current final
  SHA-256 after the append-only post-run audit binding clarification is
  `b32550966e29f3398239e7be778cb20e3344e427bbec6f664fdda062c0e9eaff`. The local implementation
  and test correction is unpushed in follow-up commit
  `92460a00ec136dd885b4940184bee9d954da9106`, after predecessor
  `6ff9ceb13bbf2b9a4de19ba1db7788f11d239570`. It makes exact-byte
  `du -x -B1 -s /vol/fob-vol6/mi25/yesildau` with a 120-second bound primary, keeps `du -xsh`
  as a non-blocking diagnostic when exact usage is below 30 GiB, and freezes mandatory
  capacity/inode/path/root/write-policy checks plus a bounded manifest-hashed >500 MiB home-file
  audit and pre/post reconciliation. It changes no 151an/151at scientific limits and grants no
  HU/network, source/footer, corpus, calibration, materialization, scoring, evaluation,
  GPU/Slurm, training or later-document authority.
- Verified local Relation V2 evidence:
  - source/legacy profile SHA-256 `020c4daef91a25e6cc553a67241c448d2a0bb7fb23b8184d5296b55e524f455b`
    has 829 unique EN/TR object surfaces;
  - derived Relation V2 profile SHA-256
    `60dd741f8ef2815755beafa8bb5799f4112af3d94b1b8c4c171bfef28b07e6c1` has 713 unique
    EN/TR object surfaces;
  - frozen release manifest SHA-256
    `94df56dba548c81d39b03b7b7fe4f9a59d9555997e984fd7aed5cabd0a113425` records the
    source-to-derived relationship and the authoritative Relation V2 relation IDs;
  - the existing contamination builder deterministically reproduces 65,717 patterns, including
    713 canonical objects, 25,000 fact IDs, 20,000 unique training sentences, four 5,000-item
    subject/name channels and four dataset-artifact identifiers.
- Treat 151n/151o as chronological evidence with a **provisional scientific gate**; 151p is the
  current correction authority and does not silently rewrite those records.
- Documents 151k/151l remain uncreated from the superseded 151j path; Documents 152–154 remain
  unauthorized and uncreated.
- Document 151aa is the current read-only final evidence-gap audit and manifest-count correction.
  It inspected only the six pre-authorized files in the immutable 151x repair root. The actual
  `output_artifact_manifest.jsonl` has 7 rows, not 8; the 151y “previous eight outputs” wording is
  corrected append-only in 151aa. The field matrix has exactly 150 rows: 96 `verified` plus 54
  non-verified (`30 not_reported`, `18 not_retrieved_in_this_wave`, `6 blocked`). All 54 rows are
  classified by entity, field, status, blocker, evidence reference and reason. The six registry
  entities remain `BLOCKED` (3 benchmarks × 27 fields; 3 source models × 23 fields). Document
  151aa SHA-256 is `0a063d7d7465eb8bffdfa47a55fa95adc8420cef0a641e9d967c19ef6cdb69ae`.
- Document 151ab remains the current frozen measurement-design authority/minimal baseline contract,
  now executed once for its bounded inventory-only scope. Its original pre-correction SHA-256 is
  `500b24f6945272cbf7ddb0f26e95449434857bcac89ed5fb5d593e3fd189b4dd`; its first corrected final
  SHA-256, preserved as the pre-operational-correction hash, is
  `3320516e674c12288d70396e31b33c059550c15365caabe9453e932e3858e2dc`; its final operational
  correction SHA-256 is
  `3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c`. The operational correction
  resolves the HU/SSH versus scratch-write contradiction: any future inventory wave must use
  mandatory HU storage/path/inode preflight, the documented HU/SSH route, a closed exact
  read-only source allowlist and only the new scratch root
  `/vol/tmp2/yesildau/luna_measurement_design_input_preparation_v1`. Existing sources, HU home and
  prior roots remain read-only; public HTTP/download and recursive corpus reads are zero, large
  weights are path/stat-only, and compact output limits are frozen. The wave may inventory
  candidate corpus evidence but may not select/materialize the primary in-domain split; absence
  remains `blocked_by_corpus_selection_or_materialization`, and `trwiki-20260601` is control-only.
  The substantive correction still freezes BPB, exact M0/M1 states, both estimands and the review
  ledger. Documents 151ac/151ad remain reserved and unused. The one authorized inventory execution
  wrote exactly eight compact outputs under
  `/vol/tmp2/yesildau/luna_measurement_design_input_preparation_v1`; existing sources, HU home and
  prior roots remained read-only. Document 151ae records the execution result (SHA-256
  `b6a90ce5573de1c29828186dbc278c7c92c87dc1e435ef44965d0eff6f8e1601`) and 151af records the
  post-execution gate (SHA-256
  `1e96a4b8d29edc50a8f151a34990c93edf3b5115dfb76416500261f8f8d817d1`). The operational
  inventory passed, but the scientific authority remains `blocked_by_measurement_design` with
  contributing `blocked_by_corpus_selection_or_materialization`; `ready_to_measure` and
  `ready_to_train` are false. No selected adaptation corpus or primary in-domain split was
  fabricated.
- Document 151ag is now the current exact C1 reconciliation and corpus-selection decision. It
  inspected only the six named inventory files read-only and found 60 source-allowlist rows, 5
  model/tokenizer rows, 6 evaluation-input rows and 17 C1 rows, with C1 status counts
  12/2/1/1/1 as recorded in 151ag. It classifies vngrs only as a conditional primary
  materialization candidate, trwiki-20260601 as cross-domain control-only and CulturaX as
  `excluded_access_blocked`. 151ag SHA-256 is
  `54c9e1f8c626fb0f67a2d1f5396277304c30714074683b386ac39da3a8f22497`.
- Document 151ah is the current frozen, unexecuted vngrs acquisition/materialization contract
  with append-only metadata/structural and systematic-selection corrections. Its pre-correction
  SHA-256 was `a8c1d1d2082ec3ae5b31ace5dc0a9506ace90f82d0f7bd1a2c1a528069ef2269`; its immediately
  prior SHA-256 was `9151da7112b6d1ab9bbb3b483b202dec23449624beeddb53c23682569a0f598b`; current
  SHA-256 is `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`. The official
  metadata pass verified immutable revision `ee5c6201ee84457a18182bfc483a7d8a7f3655ba`,
  50,336,214 train rows, schema `text/corpus/original_id`, a 284-shard Parquet tree and
  CC BY-NC-SA 4.0 metadata. The exact 32-path systematic midpoint set is frozen; its selection
  payload SHA-256 is `dbb9714347970634386ff62366384fcae12cf5f12f1df1df676cb9af3ae25686`.
  Exact per-shard LFS/SHA/footer/byte metadata, execution-time license byte hashes and
  sample-based tokenizer-yield evidence remain unresolved, so 151ah remains
  `PREPARATION_BLOCKED` and the path set is not yet an executable download allowlist.
- Document 151ak is the current frozen, unexecuted model-neutral bounded sample-calibration
  contract. It preserves all earlier SHA-256 values through `16f2978b10fc2b71490917ffe9ed549b574d2b364865675f0f1d900fb4320d68`; the append-only final evidence-graph
  correction SHA-256 is `1920f1c58d8ada250af50d1f088f5ad2fc3a15e8221f84d92f5458dab415154b`.
  The effective final profile freezes exactly 10,000 raw records, the immutable vngrs revision,
  the exact schema, the systematic 32-shard path/payload identity, explicit logical-request and
  HTTP-attempt ordinals, record/request foreign keys, canonical metric/artifact payload hashes
  and a self-reference-free final audit. Local preparation helpers remain `DIAGNOSTIC_ONLY`.
  151ak remains `FROZEN — PREPARATION_BLOCKED — UNEXECUTED`; primary gate is
  `blocked_by_measurement_design`, contributing gate is
  `blocked_by_corpus_selection_or_materialization`, and `ready_to_measure`/`ready_to_train` are
  false. Documents 151ai/151aj/151al/151am remain reserved and uncreated.
- The strict local control is explicitly `STRUCTURAL_SYNTHETIC_CONTROL`, not source or route
  evidence. The validator now blocks out-of-bounds/overlapping/duplicate windows, schedule
  mismatch, unbound source/route/response child hashes and tampered near-dedup count/rate. The
  row-count-weighted midpoint schedule requires a computed minimum of 373 contiguous windows on
  the structural fixture, exceeding the frozen 100 successful `/rows` request envelope; this is a
  recorded feasibility contradiction and the estimand is not changed. Focused verification is
  `30 passed, 1 skipped`; compatible verification is `260 passed, 8 skipped` after the same three
  documented collection exclusions. These are local implementation checks only. Route/footer/byte
  evidence, actual source access, scientific LID/quality/PII/contamination evidence and the global
  `blocked_by_measurement_design` gate remain unresolved. Document 151an is the current frozen,
  unexecuted contract, execution-ready only for the bounded metadata/footer feasibility scope. Its
  pre-correction SHA-256 is
  `435e0c25cedd7fd8fcb70862c637040300c2d5b201bfb5fa25c2b20232e71096`; its prior corrected SHA-256
  is `572a14636dfc44f23cdff5ac536838ea671a488ddcd24968097bc4942bb0d4e4`; and its current strict
  parser correction SHA-256 is
  `e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac`; its current retry/bound
  correction SHA-256 is
  `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`. The corrected profile
  excludes Dataset Viewer `/rows` because its dataset/config/split/offset/length shape cannot bind
  the frozen revision and selected shard; freezes one `parquet_footer_range` vocabulary, exact
  immutable direct-file routes, scratch root
  `/vol/tmp2/yesildau/luna_vngrs_metadata_footer_feasibility_v1`, seven top-level outputs,
  separate full-object/LFS versus compact metadata hashes, a pure-Python compact-Thrift parser
  for complete footer framing, two-stage trailer/exact-footer ranges, shared HEAD binding,
  contiguous retry chains, actual response-byte binding, canonical manifest bytes and a
  self-reference-free final-audit chain. Local validators/tests now enforce these fail-closed
  rules. The one explicitly authorized wave is recorded in 151ao/151ap as `BLOCKED` before source
  access: HU synchronization failed on a dirty old-base checkout and the mandatory byte-form home
  `du` preflight returned no parseable value. The frozen root was not created; no executor,
  request, retry, response byte or artifact exists. 151ao SHA-256 is
  `5b8cb4be094e78f9e37a927348efab4491e5f00355496f2996d1170494dfae46`; 151ap SHA-256 is
  `aef81d9b72dd856b802310daaa4c64c7e37089ef22e2527cb6c88bbba8923468`.
- The retry/bound correction is now frozen. Retryable HTTP statuses are only 429/503 with explicit
  error codes; no-response transport failures have nullable response fields and no artifact; every
  response-bearing failure has one `evidence/retry/` artifact with actual byte/hash binding; and a
  valid 200/206 response cannot be relabelled as retryable. The effective retry ceiling is 24, so
  97 base artifacts + 24 retry artifacts + 7 top-level files = 128 files/inodes. Local validation
  is `40 passed, 2 skipped` focused and `270 passed, 9 skipped` compatible, with the explicit
  independent-writer compatibility skip recorded. The current 151an contract remains frozen and
  unchanged; its operational result is the pre-source-access `BLOCKED` wave above. No automatic
  151an retry is authorized.
- Local commit `cf6307c2055e4d1c947e9807b1af4cb8b03db70b` remains preserved and was published
  together with the two reviewed follow-ups. Despite its
  commit subject, it contains the complete previously untracked vngrs package and tests: 13 files /
  6,243 inserted lines, not only `metadata_executor.py`. Separate follow-up commit
  `22e1c305726c6214dd5325391d5e9cb943ebd119` repairs the executor and adds executor-level adversarial
  tests without rewriting the prior commit. The executor now separates attempt/retry guards,
  enforces cumulative response bytes and pre-insertion artifact bounds, applies bounded storage
  subprocesses and the 30 GiB HU-home stop rule, preserves compact source-failure context, and
  invokes the post-run storage audit on PASS and source-attempt BLOCKED outcomes. The reviewed
  chain was pushed ordinary non-force; it was not synchronized onto HU because the HU checkout
  was dirty.
- Follow-up commit `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23` closes the remaining executor-only gaps without changing frozen 151an
  semantics. Response reads now respect trusted declared lengths and the remaining cumulative
  budget before consuming bytes; unknown/chunked responses are capped at the applicable budget
  plus one byte. Successful but unparseable or empty `du` output now fails preflight closed. The
  previous commits `cf6307c2055e4d1c947e9807b1af4cb8b03db70b` and
  `22e1c305726c6214dd5325391d5e9cb943ebd119` remain preserved and published; the HU checkout
  remains at the old base until a safe owner-controlled reconciliation.

- Document 151aq is the current bounded HU read-only operational diagnostic authority. It verified
  live/local publication at `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`, HU old-base identity
  `9f1755219ba003d4aaf962558b3c0512fc74f99a`, 42 status entries (`39 .D` plus three `?`) and
  zero overlap with the 13 published code/test paths. Both bounded 30-second `du` forms were
  successful and parseable: `du -xsh` returned 32 stdout bytes / 0 stderr bytes / `14G`, while
  `du -x -B1 -s` returned 40 stdout bytes / 0 stderr bytes / `14687617024`. Capacity/inode/path
  checks passed and the frozen root remained absent. Its SHA-256 is
  `5a48d297ef5475550df41fd7e2baace4278acf54bbfb32bbfe455909dde7dbea`. The current `du` component
  is closed, but the dirty HU checkout still blocks new 151an execution.

- Documents 151ar and 151as are the current result and post-execution gate for the one
  preservation-checked 151an wave. The HU checkout was fast-forwarded once from
  `9f1755219ba003d4aaf962558b3c0512fc74f99a` to
  `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23` after the exact 42-entry status blob remained
  unchanged (6,989 bytes, SHA-256
  `71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`, zero overlap). Preflight
  and independent PyArrow writer/parser self-check passed. The single execution blocked on the
  first frozen direct-route HTTP 302; no redirect was followed, retries/response bytes/artifacts
  were zero and the root remained absent. 151ar SHA-256 is
  `e531443254133a3ade95fcdf004420cc8726d28f337c7171c730937de3019967`; 151as SHA-256 is
  `03c603265836320b173489a6659f91916c97db7ec78ebdd7b8faf0c1122a0ceb`. The narrow operational
  gate remains `blocked_by_operational_access` due route integrity; global gate remains
  `blocked_by_measurement_design`; no retry or training is authorized.

## Current Task Status

```text
BLOCKED
```

The latest explicitly authorized execution wave is BLOCKED at HU read-only connectivity and
prewarm. No push, fetch, merge or HU checkout movement was performed; the separate
`210e47256a499d098da9879d7ade990527cdbe35` state remained outside scope. A bounded 30.004-second
read-only SSH probe returned no remote result, so the retry did not freshly verify the expected
HU HEAD `92460a00ec136dd885b4940184bee9d954da9106`, the 42-entry dirty-state/status SHA,
frozen-root absence, large-file manifest or exact-byte home usage. Documents 151bc/151bd record
this result and gate; their SHA-256 values are
`376c5e380ba1fa22262626b66b531d19f9333e168a2ffe3c86017b1218726edc` and
`c9544bbe410c2d4353ef6b1f1c4c72debd269bb3122d0c0de559b46680d61683`. Internal preflight,
PyArrow and executor were not invoked; the post-run audit probe also timed out before a remote
result. The operational gate is `blocked_by_operational_access`; the global measurement-design
gate remains blocked and `ready_to_measure`/`ready_to_train` remain false.

## Latest Execution Override — Blocked Before Publication

The authorized wave verified local HEAD `92460a00ec136dd885b4940184bee9d954da9106` with parent
`6ff9ceb13bbf2b9a4de19ba1db7788f11d239570`, then ran the live remote-base guard. Because the
remote branch had advanced to an unexpected base, the agent stopped before push as required.
HU preservation checks and dirty-state mutation were not attempted; no scientific or route
evidence was generated. Documents 151ay and 151az preserve this fail-closed result.

## Latest Revised-Base Execution Override — BLOCKED at Corrected Preflight

Under the explicit revised-base authorization, the live remote was verified at
`2ff1cacdffd55820fdf9a8f633c2bc20bffac807`; merge-base matched, local ahead was 2, remote ahead
was 0, and only the reviewed chain ending at `92460a00ec136dd885b4940184bee9d954da9106` was
published by ordinary non-force push. HU started at `2ff1cac...` and passed the ancestor,
42-entry, 39 tracked `.D`, 3 untracked, status SHA-256
`71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9` and zero-overlap guards.
Exactly one fetch and one `merge --ff-only` brought HU to `92460a0...`; the status blob stayed
6,989 bytes and byte-for-byte unchanged.

The corrected 151ax preflight then failed closed before PyArrow/source access. The primary
`du -x -B1 -s /vol/fob-vol6/mi25/yesildau` timed out at 120 seconds with null return code and
0 stdout/0 stderr bytes; diagnostic `du -xsh` also timed out at 30 seconds. `df -h`, `df -i`,
path/root/write-policy checks and the five-file >500 MiB manifest passed. The frozen output root
was absent and remained absent. PyArrow status was `NOT_RUN_PRE_FLIGHT_BLOCKED`; executor
invocations, logical requests, HTTP hops, retries, response bytes and output files were all 0.

The mandatory post-run audit was nevertheless run and PASSed: post-run exact-byte usage was
`14689083392`, human-readable usage was `14G`, the large-file manifest reconciled unchanged,
and the root inventory was 0 files/0 bytes. This later PASS does not reverse the preflight
BLOCKED result. Documents 151ba and 151bb are the current result/gate records with SHA-256
`d14c3b31b4d35517fbbcfaac2706b20f4108241c25f21b51c0e3157f0373ae26` and
`2ffaf1eed56a8c895dbb48715b4bab07d9e3d368363ee3a81dc7bdff2c34c606`. A later read-only check
observed local and remote at `210e47256a499d098da9879d7ade990527cdbe35`; it was outside the
authorized push and caused no further operation. Documents 151ay/151az
remain unchanged; no retry is authorized automatically.

## Latest HU-Only Prewarmed Retry — BLOCKED before Live Prewarm Evidence

Under the explicit HU-only authorization, no publication or synchronization operation was
performed. The requested HU target remained `92460a00ec136dd885b4940184bee9d954da9106`; the
separate `210e472...` state was not used. A combined read-only prewarm command was started, but
the helper returned no remote JSON. A formal short probe bounded the SSH helper at 30 seconds:
timeout `true`, return code `null`, duration `30.004 s`, stdout 226 bytes containing only the
SSH spawn line, stderr 0 bytes. The expected live HEAD, 42-entry status, 39 `.D`, 3 untracked,
6,989-byte status SHA
`71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`, root absence, large-file
manifest and exact-byte `du` result were therefore not accepted from historical records.

The large-file and exact-byte priming results are `NOT_OBTAINED`; no manifest hash or live home
usage value was fabricated. Internal 151ax preflight, PyArrow and the executor were not reached;
invocation count, logical requests, HTTP hops, redirects, retries and response bytes were all
zero. The mandatory post-run audit probe timed out after 30.005 seconds with return code `null`,
680 stdout bytes, 0 stderr bytes and no remote audit result, so its status is
`INCOMPLETE / NOT_OBTAINED`. Documents 151bc and 151bd are current with SHA-256
`376c5e380ba1fa22262626b66b531d19f9333e168a2ffe3c86017b1218726edc` and
`c9544bbe410c2d4353ef6b1f1c4c72debd269bb3122d0c0de559b46680d61683`. Documents 151ba/151bb
remain unchanged; no automatic retry is authorized.

## Completed Current Task Override — Local-Only HU Storage Preflight Resilience Correction

Under the explicitly authorized local-only correction, preserved Document 151ax's
pre-clarification SHA-256 `15bdc5a7ae0e0356254c5d5ffd5ad47b091f459a52689ce4c0cb1ecc9699ed22` and
append-only froze its current final SHA-256 as
`b32550966e29f3398239e7be778cb20e3344e427bbec6f664fdda062c0e9eaff`. The executor now
uses live exact-byte `du -x -B1 -s /vol/fob-vol6/mi25/yesildau` with a 120-second timeout as the
primary home-usage check. The 30-second `du -xsh` result is diagnostic-only and does not alone
block when exact-byte usage is parseable and below 30 GiB. The mandatory `df -h`, `df -i`,
resolved-root, root-absence and HU-home-write-prohibition checks remain fail-closed. A bounded
120-second `find` audit produces a canonical >500 MiB regular-file manifest with explicit
PASS/BLOCKED/INCOMPLETE states and pre/post path/byte reconciliation; incomplete evidence is not
called clean.

The narrow local code/test change is follow-up commit
`92460a00ec136dd885b4940184bee9d954da9106`, after preserved predecessor
`6ff9ceb13bbf2b9a4de19ba1db7788f11d239570`; only the vngrs executor and its focused tests were
committed. It now binds top-level PASS to source-stage PASS plus post-run-audit PASS, reports
audit timeout/parse/missing-reconciliation as INCOMPLETE, definitive command/policy/large-file
violations as BLOCKED, and preserves source evidence on audit failure. Existing dirty/untracked
user files were preserved and no push was performed. The
151an/151at limits, source identity, redirect semantics, scratch-only write rule and scientific
scope are unchanged. No HU/SSH, network, source/footer, corpus, calibration, materialization,
scoring, evaluation, GPU/Slurm, training, cleanup or 151ay/151az/152--154 action occurred.

## Completed Current Task Override — Publish, synchronize and execute 151an once

Under the explicit authorization, only the reviewed `de4a14e...` follow-up was pushed by ordinary
non-force publication. HU preservation checks passed before and after the single `merge --ff-only`:
42 status entries, 39 tracked `.D`, 3 untracked, status SHA-256
`71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`, and zero overlap with the
four incoming vngrs code/test paths. The HU HEAD is now
`de4a14e3370326173bdf04ce33356aae7826ddda`.

The mandatory preflight failed closed because `du -xsh /vol/fob-vol6/mi25/yesildau` timed out at
30 seconds (`returncode=null`, stdout 0 bytes, stderr 0 bytes). `df -h`, `df -i`, scratch-root
resolution and root absence passed. Home usage therefore remained unproven; the independent
PyArrow writer/parser self-check was `SKIPPED_PRE_FLIGHT`. The executor was not invoked, so HTTP
attempts/hops, retries, response bytes and artifacts are all zero. The post-run audit was run; the
root remained absent with 0 files/0 bytes, while its `du` and large-home-file checks also timed
out. Documents 151an–151at remain unchanged; no source/footer route, corpus row, full shard,
model, scoring, evaluation, GPU/Slurm, training, cleanup or Documents 152–154 action occurred.

## Completed Current Task Override — Freeze and implement controlled Hugging Face CDN redirect semantics

This is a contract/code/test preparation task. It may consult only official public Hugging Face
documentation/source metadata and edit local code/docs. It must not access the actual vngrs source
route, HU, or execute 151an.

1. Read `AGENTS.md`, `ssh-client/README.md`, this handoff and complete Documents
   151an/151ao/151ap/151aq/151ar/151as. Verify all current hashes and preserve those documents
   unchanged as executed historical authorities.
2. Use only official Hugging Face documentation and the official `huggingface_hub` repository to
   evidence that a commit-pinned `/resolve/{revision}/{path}` URL may resolve to a content-addressed
   CDN/Xet location. Record immutable source revisions where available; do not access the actual
   vngrs shard/README routes during this preparation.
3. Create Document 151at as a frozen, unexecuted controlled-redirect route-correction contract.
   Preserve the exact initial `https://huggingface.co/datasets/vngrs-ai/vngrs-web-corpus/resolve/`
   route, immutable revision and 32 paths. Permit either zero redirect or exactly one explicit
   absolute redirect with status 302, HTTPS, no userinfo/fragment, bounded URL length and a final
   host matching the official suffix allowlist `xethub.hf.co` or `cdn.hf.co`. Any other status,
   extra hop, relative target, downgrade, host, port or identity mismatch is `BLOCKED`.
4. Freeze safe signed-URL handling: never persist query values or credentials in logs, docs or
   canonical artifacts. Bind the exact raw `Location` in memory by SHA-256 and persist only the
   hash, scheme, normalized host, path hash, URL length and sorted query-key names. Strip
   authorization/cookie headers on cross-host requests. Preserve method and Range headers exactly.
5. Bind the redirect to immutable content identity. The initial URL must contain the exact commit
   and path; terminal HEAD/GET responses must reconcile content length, ETag/LFS OID,
   `Content-Range`, Parquet trailer/footer bytes and parsed metadata exactly as before. A signed CDN
   URL alone is never sufficient evidence.
6. Separate logical request, logical retry and HTTP-hop accounting. A redirect hop is not a retry.
   For 97 base logical requests and at most 24 logical retries, freeze at most 121 terminal request
   attempts and at most 242 HTTP hops under the one-redirect rule. Reconcile all counts in the
   request ledger and final audit. Keep the existing 64 MiB total-response, 4 MiB single-response,
   128-file/inode and 7,200-second ceilings; redirect metadata must be embedded in the existing
   logical-request artifact rather than creating an unbounded file class.
7. Update the executor, validators and focused tests locally. Test zero-hop success, one-hop
   `huggingface.co` to `*.xethub.hf.co`, one-hop to `*.cdn.hf.co`, method/Range preservation,
   authorization stripping, raw-Location hashing/redaction, hop/retry arithmetic and terminal
   ETag/footer binding. Negative controls must cover two redirects, HTTP downgrade, userinfo,
   fragment, non-default port, disallowed/near-match hosts, relative redirect, missing Location,
   changed method/Range, persisted query values, hop overflow and terminal identity mismatch.
8. Repair the existing nested `HTTPError` read-exception variable shadowing so a bounded response-
   read failure retains the original HTTP status and compact `response_read_failure` context.
   Add an executor-level regression test; do not allow it to fall into generic unexpected-error
   handling.
9. Run the focused and compatible repository suites. Create one narrow local follow-up commit
   without amending the published three-commit chain. Record the new commit hash, exact diff and
   future publication scope. Reserve Documents 151au/151av for a later execution result/gate but
   do not create them. Update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this
   handoff.

Hard exclusions:

- no HU/SSH, actual vngrs shard/README request, source/footer HTTP, scratch-root creation,
  PyArrow execution wave, 151an/151at execution or publication/push;
- no corpus rows/full shards, 151ah/151ak execution, sample calibration or materialization;
- no model/tokenizer access, scoring, inference, evaluation, GPU/Slurm, training, cleanup/deletion
  or Documents 152–154;
- no claim that redirect acceptance alone proves source integrity or readiness to measure/train.

Completion record: the protected 151an–151as hashes were unchanged. Official Hugging Face
documentation/source metadata was consulted without contacting vngrs. The local executor,
metadata validators and manifest validator now implement the secret-safe zero-or-one-hop protocol;
the HTTPError response-read shadowing defect is repaired. Focused verification was `66 passed, 2
skipped in 12.12s`; compatible verification was `305 passed, 9 skipped in 28.43s` after excluding
only `tests/test_m1_cross_family.py`, whose local collection requires unavailable PyYAML. The
follow-up commit is `de4a14e3370326173bdf04ce33356aae7826ddda`. No HU/network/source-byte or
execution action occurred.

## Completed Current Task Override — Preserve dirty HU state, fast-forward code, and retry 151an once

This is one fail-closed operational reconciliation-and-execution wave. It preserves the 42-entry
HU working-tree state in place; it does not clean, restore, relocate or reinterpret those entries.

1. Read `AGENTS.md`, `ssh-client/README.md`, this handoff and Documents 151an/151ao/151ap/151aq.
   Verify their current SHA-256 values, live `origin/corpus-update` at
   `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`, and the exact 151aq status classification before any
   HU write.
2. Re-capture HU HEAD and the canonical porcelain-v2 status path/class list. Require old HEAD
   `9f1755219ba003d4aaf962558b3c0512fc74f99a`, exactly 39 `.D` paths plus the same three untracked
   entries, and zero overlap with the exact 13-path incoming change set. Record the before-state
   digest. If any identity, class, count or overlap changed, stop before fetch/merge.
3. Fetch only `origin corpus-update`, verify fetched commit exactly `c1a3127...`, verify old HU HEAD
   is its ancestor and recompute zero path overlap. Then perform exactly one ordinary
   `git merge --ff-only` to the fetched commit. No pull wrapper, force, reset, restore, checkout,
   stash, clean or deletion is allowed. If the fast-forward cannot complete cleanly, stop without
   another Git mutation.
4. Immediately verify HU HEAD equals `c1a3127...` and the canonical 42-entry path/class list is
   byte-for-byte unchanged from the before-state. Any lost, added or reclassified entry is
   `BLOCKED`; do not attempt repair inside this wave.
5. Repeat the mandatory `du -xsh`, byte-form `du`, `df -h`, `df -i`, resolved-path and absent-root
   preflight with exact timeout/exit/stdout/stderr accounting. Require home usage below 30 GiB and
   the frozen 151an root absent before creation.
6. Run the frozen independent-PyArrow writer/parser self-check on the synchronized executor before
   any public request. Missing PyArrow, skip or failure is `BLOCKED` with zero source requests.
7. Only if every prior condition passes, execute frozen Document 151an SHA-256
   `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79` exactly once using its
   reviewed executor at commit `c1a3127...`, immutable HTTP allowlist, exact retry/attempt/byte/
   file/inode/time bounds and frozen scratch root. Retrieve zero corpus rows and no full shard.
8. Run the complete validator and mandatory post-run storage/path/inode audit. Create local
   Document 151ar as the new retry execution result and 151as as its decision gate; preserve
   151ao/151ap/151aq unchanged. Update `AGENTS.md`, `ssh-client/README.md`, the documentation index
   and this handoff with exact Git, preflight, self-check, request, byte, artifact, inventory, hash
   and honest PASS/BLOCKED evidence.

Hard exclusions:

- no force push/merge, reset, restore, checkout, switch, stash, clean, deletion, overwrite, owner-
  state backup removal or alteration of the 42 preserved entries;
- no Dataset Viewer rows, corpus rows, compressed row groups, full shards, 151ah/151ak execution,
  sample calibration or corpus materialization;
- no model/tokenizer access, scoring, inference, evaluation, GPU/Slurm, training, cleanup/deletion
  or Documents 152–154;
- a 151an PASS closes only the narrow route/footer/byte/schedule-feasibility component and cannot
  set `ready_to_measure` or `ready_to_train` true.

## Completed Current Task Override — Read-only HU checkout and home-usage preflight diagnosis

This task diagnosed the two pre-source blockers recorded in 151ao/151ap. It inspected but did not
reconcile, clean, synchronize or otherwise mutate HU, and did not authorize another 151an
execution.

1. Read `AGENTS.md`, `ssh-client/README.md`, this handoff and Documents 151an/151ao/151ap. Verify
   their current SHA-256 values and verify local/remote `corpus-update` identity at
   `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23` before HU access.
2. Through the documented SSH helper and bounded timeout, inspect the HU checkout read-only:
   branch/HEAD, `git status --porcelain=v2` classification, tracked modified/deleted paths,
   untracked top-level/path identities and the overlap between all dirty paths and the exact path
   set changed by `9f17552..c1a3127`. Record counts by status class and the exact overlapping path
   names. Do not read or publish file contents, credentials or corpus text.
3. Diagnose the home-usage preflight read-only. For each bounded `du` form, record the exact
   command identity, timeout, exit code, stdout byte count, stderr byte count and whether a numeric
   usage value is parseable. Retain at least the mandated `du -xsh` check and one GNU byte-form
   equivalent such as `du -x -B1 -s`; do not infer zero from missing output. Record whether the
   failure is command syntax, helper transport/silence timeout, NFS traversal timeout, permission,
   stderr-only output or another evidenced category. Continue to record `df -h`, `df -i` and
   resolved paths without writing to HU.
4. Compare only non-destructive remediation options: owner-controlled cleanup/reconciliation,
   clean fast-forward if Git proves no conflict, or a separately authorized clean checkout/clone
   on scratch pinned to `c1a3127`. Select no option unless the evidence proves it preserves all 42
   existing entries. Do not treat Git's ability to merge as permission to alter the dirty tree.
5. Document 151aq was created as the read-only operational diagnostic result. It states the exact
   evidence collected, what remains unverified and the safest separately authorized remediation.
   `AGENTS.md`, `ssh-client/README.md`, the documentation index and this handoff were aligned;
   151an/151ao/151ap remain unchanged.

Hard exclusions:

- no HU `git fetch`, pull, merge, reset, checkout, switch, stash, clean, restore, worktree, clone,
  file write, chmod, deletion or output-root creation;
- no public source/footer HTTP, PyArrow execution check, 151an executor invocation or 151an retry;
- no corpus rows/full shards, model/tokenizer access, scoring, inference, evaluation, GPU/Slurm,
  training, cleanup/deletion or Documents 152–154.

## Single Next Authorization Request

The next separately authorized wave must first reverify the live remote, currently last observed
at `210e47256a499d098da9879d7ade990527cdbe35`, and establish a bounded HU SSH response before
assuming anything about the checkout. It must then reverify the expected HU checkout
`92460a00ec136dd885b4940184bee9d954da9106`, the 42-entry/39 `.D`/3-untracked status identity,
status SHA, frozen-root absence and zero-overlap guards. The large-file priming and exact-byte
prewarm must each return complete live evidence below the frozen limits, followed by the
internal corrected storage preflight and independent-writer self-check. Only after every gate
passes may a new explicit authorization permit exactly one bounded 151an execution using 151at
redirect semantics. This request is not itself an execution authorization and does not authorize
an automatic retry. It does not authorize corpus rows/full shards, models/tokenizers, scoring,
inference, evaluation, GPU/Slurm, training, cleanup/deletion or Documents 152--154. It cannot
close `blocked_by_measurement_design` or set `ready_to_measure`/`ready_to_train`.

## Completed Current Task Override — Close final executor read-budget and preflight-parse gaps

This is one bounded **local-only** code/test/handoff correction. It does not authorize push,
HU/SSH, public HTTP or 151an execution.

1. Read `AGENTS.md`, `ssh-client/README.md`, this handoff, complete 151an and commits
   `cf6307c2055e4d1c947e9807b1af4cb8b03db70b` and
   `22e1c305726c6214dd5325391d5e9cb943ebd119`. Verify the unchanged 151an SHA-256
   `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`; preserve all unrelated
   files and do not rewrite either commit.
2. Make response reads respect both the 4 MiB single-response ceiling and the remaining 64 MiB
   cumulative budget before consuming the body. Use declared response length when trustworthy;
   for unknown/chunked length, use a conservative bounded strategy that never consumes several
   MiB beyond the remaining cumulative allowance. A response that cannot be proven to fit must
   fail closed without being retained.
3. Add an executor test with a tiny monkeypatched cumulative bound, one byte remaining and a much
   larger fake body. Assert the read call itself is capped by the remaining-budget strategy, the
   artifact/request row is not retained and the failure context reports the exact bound phase.
   Cover both normal and `HTTPError` response paths.
4. Make `storage_preflight()` return `BLOCKED` whenever successful `du` output cannot be parsed to
   an exact home-usage byte count. Retain the existing timeout, >=30 GiB, root-collision and
   resolved-path checks; add an explicit unparseable/empty-output test.
5. Reproduce focused and compatible suites. Create one new follow-up commit without amending or
   dropping either existing commit. Record all three exact full hashes and the total future push
   scope. Update this handoff; update broader authority files only if their operational statement
   materially changes.

Hard exclusions:

- no `git push`, external publication, HU/SSH, public network/source/footer access, scratch-root
  creation, 151an execution or 151ao/151ap creation;
- no rows/full shards, corpus materialization, 151ah/151ak execution, scoring, evaluation,
  GPU/Slurm, training, cleanup/deletion or Documents 152–154;
- no frozen 151an semantic change or execution-readiness claim without the new executor-level
  probes.

## Completed Current Task Override — Repair and test the 151an executor before any push

This is one bounded **local-only** code/test/handoff correction. It does not authorize push,
HU/SSH, public HTTP or 151an execution.

1. Read `AGENTS.md`, `ssh-client/README.md`, this handoff, complete 151an and local commit
   `cf6307c2055e4d1c947e9807b1af4cb8b03db70b`. Verify 151an SHA-256
   `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`; inspect Git status and
   preserve unrelated tracked/untracked files. Do not amend, reset or drop the existing commit.
2. Reproduce the focused/compatible suites and the independent probes recorded below. Add
   executor-level fake-opener tests; validator-only fixtures are insufficient.
3. Split the attempt and retry guards. Exactly 24 retryable failures followed by one terminal
   success must be accepted when every other bound holds; a 25th retry must block. The current
   behavior that blocks the post-24th-retry success is invalid.
4. Track cumulative actual response bytes inside `BoundedClient` and fail before retaining a
   response that would make the total exceed 64 MiB. Add a small monkeypatched-bound test proving
   that the over-limit response is not accepted or silently deferred to final validation.
5. Enforce response-artifact/regular-file bounds before inserting a new retry artifact, and retain
   the exact 24-response-bearing-retry boundary test. Keep top-level/file arithmetic consistent
   with frozen 151an; do not change contract semantics silently.
6. Give every storage/preflight subprocess a bounded timeout and fail closed on timeout. Parse and
   enforce the project stop rules, including unexplained/current HU home usage at or above 30 GB,
   root existence and exact resolved scratch placement. Add deterministic command-result tests.
7. Invoke and return the mandatory post-run storage audit on PASS and after any source-attempt
   BLOCKED outcome where auditing remains possible. Preserve compact failure context—phase,
   attempt/retry/byte counts, HTTP status and bounded redirect/location metadata—so a route
   feasibility failure can be documented rather than reduced to an untraceable exception string.
   Keep redirects fail-closed under the current frozen 151an; do not broaden the route allowlist
   without a later evidence-backed append-only contract correction.
8. Add executor-level tests for: 24 retries then success, 25th retry rejection, cumulative byte
   overflow, non-retryable 3xx fail-closed evidence, preflight timeout/home-limit failure, root
   collision, writer-check failure before any source request, and post-run audit invocation.
9. Create a new follow-up commit without rewriting `cf6307c`. Record both commit hashes and the
   exact publication scope. Update this handoff and, only where the current operational authority
   changes, `AGENTS.md`, `ssh-client/README.md` and the documentation index.

Hard exclusions:

- no `git push`, external publication, HU/SSH, public network/source/footer access, scratch-root
  creation or 151an execution;
- no rows/full shards, corpus materialization, 151ah/151ak execution, scoring, evaluation,
  GPU/Slurm, training, cleanup/deletion, 151ao/151ap or Documents 152–154;
- no change to frozen 151an semantics or readiness claim based only on validator tests.

## Pending Task After Executor Repair — Implement and execute the frozen 151an metadata/footer-only wave

This is one bounded implementation-and-execution task under the already frozen Document 151an.
It requires a new explicit user authorization. It is not corpus-row acquisition, sample
calibration, measurement, evaluation or training.

1. Read `AGENTS.md`, `ssh-client/README.md`, this handoff, complete 151ak and complete 151an.
   Verify current 151an SHA-256
   `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79` and inspect the
   `transfer-vs-relearning` Git status before changing anything. Preserve all unrelated files.
2. Verify that local `corpus-update` is exactly three commits ahead of
   `origin/corpus-update` at expected base `9f17552`, with the ordered commit chain
   `cf6307c2055e4d1c947e9807b1af4cb8b03db70b` ->
   `22e1c305726c6214dd5325391d5e9cb943ebd119` ->
   `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`. Fetch first only if needed to establish that the
   remote branch has not moved. If it has moved, diverged or cannot be proven to match the expected
   base, stop without pushing. Otherwise push exactly local `corpus-update` to
   `origin/corpus-update` using an ordinary non-force push. Do not publish the pre-existing
   untracked artifact directories or any other local content.
3. Use the existing reusable executor at
   `src/transfer_vs_relearning/corpora/vngrs/metadata_executor.py` for the exact 151an protocol.
   Record its file SHA-256 and executed Git commit. Do not replace it with an undocumented one-off
   remote program. If any implementation change is required after the authorized push, stop and
   request a new local correction/publication authorization; do not add an unreviewed execution
   commit inside this wave.
4. Reproduce the focused and compatible local suites. If implementation changes the frozen
   protocol rather than merely executing it, stop and request a new append-only contract
   correction; do not silently change 151an semantics.
5. Use only the documented `ssh-client` route. Synchronize the verified `corpus-update` commit on
   HU without overwriting unrelated work, then perform and record the mandatory HU home/scratch
   capacity, inode and resolved-path preflight. The only new remote output root is
   `/vol/tmp2/yesildau/luna_vngrs_metadata_footer_feasibility_v1`; HU home and every prior root are
   read-only. Use a bounded SSH timeout and never leave a hidden password prompt or hanging helper.
   If synchronization is not clean, the helper hangs, or the root already exists unexpectedly,
   stop before source access or root writes and report `BLOCKED`.
6. In the HU execution environment, run the independent PyArrow-writer parser self-check before
   any public source/footer request. Treat a skip, missing PyArrow, parser mismatch or test failure
   as fail-closed: make no source request and report `BLOCKED`.
7. If and only if preflight and the independent-writer check pass, execute Document 151an exactly
   once. Public HTTP is limited to its immutable allowlist and frozen bounds. Retrieve zero corpus
   rows and no full shard. Write only declared evidence and seven top-level outputs under the
   frozen root; write `metadata_footer_audit.json` last. Run the complete validator on actual
   payload bytes and perform the mandatory post-run storage/path/inode audit.
8. Create Document 151ao as the append-only execution result and Document 151ap as the post-wave
   decision gate, even if the wave fails closed. Record preflight, executor identity/hash,
   independent-writer result, request/retry/byte/file counts, artifact/root inventory and hashes,
   validator errors, storage audit and honest PASS/BLOCKED scope. Update `AGENTS.md`,
   `ssh-client/README.md`, the documentation index and this handoff.

Hard exclusions:

- no Dataset Viewer `/rows`, corpus rows, compressed row groups, full shards, corpus
  materialization, 151ah/151ak execution or sample calibration;
- no model/tokenizer access, scoring, inference, evaluation, GPU/Slurm, training, cleanup/deletion
  or Documents 152–154;
- no `ready_to_measure`, `ready_to_train`, corpus-quality PASS or training authorization. A 151an
  PASS closes only the bounded route/footer/byte/schedule-evidence feasibility component.

## Completed Current Task Override — Repair 151an retry semantics and worst-case artifact bounds

This is one bounded **local-only** code/test/append-only contract correction. It does not authorize
HU/SSH, network, source/footer access or 151an execution. Only after the user explicitly authorizes
this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff, complete 151ak and complete 151an;
   verify 151ak SHA-256 `9e35ba69fcd4885c339101e59f1d719681942571770a41023f20f6472782ea94`
   and current 151an SHA-256
   `e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac` before editing; inspect
   `transfer-vs-relearning` Git status and preserve all unrelated files;
2. reproduce `36 passed, 1 skipped` focused and `266 passed, 8 skipped` compatible, then add an
   adversarial request-chain fixture containing a genuine retryable failure (`429` or `503`, and
   where supported a no-response transport failure) followed by one successful terminal attempt;
3. separate failure-attempt validation from terminal-success validation. Only a successful HEAD,
   trailer or complete-footer attempt may be required to satisfy the frozen `200`/`206`, exact
   range, valid Thrift/footer parse and shard-ledger success-artifact bindings. Freeze an explicit
   retryable status/error vocabulary, nullable/no-response semantics, exact response-byte/hash
   rules and one unambiguous artifact rule for every failure that returned bytes. Do not permit a
   caller merely to relabel a valid successful response as `retryable_failure`;
4. reconcile worst-case attempts, retained response artifacts, regular files and inodes. The
   no-retry base is 97 evidence artifacts plus seven top-level files. If every response-bearing
   retry has its own evidence artifact, at most 24 retries fit the existing 128-file/inode ceiling
   (`97 + 24 + 7 = 128`). Either freeze that effective retry cap or freeze another internally
   consistent bounded representation; do not retain the contradictory 28-retry/128-file profile;
5. require a positive retry-chain control that passes with one real retryable failure followed by
   terminal success, a negative control that blocks a relabelled valid success, and exact boundary
   controls at the final retry/file/inode limits. Keep request totals, artifact-manifest bytes,
   feasibility projection and final audit mutually recomputable;
6. add a parser compatibility preflight using a Parquet file produced by an independent standard
   writer when that dependency is available. If it is unavailable locally, freeze this as an
   execution-time pre-source-access self-check and report the local skip honestly; a handcrafted
   compact-Thrift fixture alone is not independent writer compatibility evidence;
7. append-only correct and re-freeze 151an, preserving all prior hashes. Update the local contract
   SHA constant, focused/compatible tests, `AGENTS.md`, `ssh-client/README.md`, the documentation
   index and this living handoff. Keep 151ao/151ap reserved and uncreated.

Hard exclusions:

- no HU/SSH, network, public route/source/footer access, row/full-shard access, scratch-root
  creation or 151an execution;
- no 151ah/151ak execution, corpus materialization, scoring, inference, model/tokenizer access,
  evaluation, GPU/Slurm, training, cleanup/deletion or Documents 151ao/151ap/152–154;
- no `ready_to_measure`, `ready_to_train` or scientific PASS claim.

## Completed Current Task Result — Repair 151an retry semantics and worst-case artifact bounds

Under the explicitly authorized local-only override, corrected the retry state machine and
worst-case artifact accounting without HU/SSH, network, source/footer access or 151an execution.
The prior 151an SHA-256 `e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac` was
preserved; the current append-only correction is frozen at
`937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`.

Retryable HTTP failures are restricted to 429/503 with explicit error codes. No-response timeout
or connection failures use nullable response fields and no artifact. Every response-bearing retry
uses one unique `evidence/retry/` artifact with actual payload length and SHA-256 binding. A valid
200/206 response cannot be relabelled as retryable. The effective retry bound is 24, reconciling
97 base artifacts + 24 retry artifacts + 7 top-level files = 128 files/inodes.

Local verification completed with `40 passed, 2 skipped` focused and `270 passed, 9 skipped`
compatible using the same three collection exclusions plus the explicit independent-writer
compatibility skip. The independent PyArrow writer preflight is frozen as an execution-time
pre-source-access self-check because PyArrow was unavailable locally; the handcrafted fixture is
not claimed as independent-writer evidence. The contract remains unexecuted and
`blocked_by_measurement_design` remains primary with
`blocked_by_corpus_selection_or_materialization` contributing. `ready_to_measure` and
`ready_to_train` remain false; 151ao/151ap remain reserved and uncreated.

No HU/SSH, network, source/footer access, execution, corpus, scoring, inference, evaluation,
GPU/Slurm, training, cleanup/deletion or Documents 151ao/151ap/152–154 action occurred.

## Historical Single Next Authorization Request — completed 151aq diagnostic

The formerly requested bounded, HU-source-read-only operational diagnostic described in the
current task override. It may inspect and classify the 42 HU Git status entries, compute their path
overlap with the published `9f17552..c1a3127` change set, and run bounded read-only home-usage/
filesystem/path preflight diagnostics with exact exit/stdout/stderr/timeout accounting. It may
create local Document 151aq and update the listed local authority/handoff files. It may not fetch,
pull, merge, reset, checkout, stash, clean, restore, clone, create a worktree, write to HU, create
the 151an root, access public source/footer routes, run PyArrow/executor/151an, or perform any
corpus, model, scoring, GPU or training action. A later remediation and any new 151an execution
will require separate explicit authorization after this diagnostic identifies a safe path.

## Completed Current Task Override — Close the final 151an footer/parser/ledger byte-integrity gaps

This is one bounded **local-only** code/test/append-only contract correction. It does not authorize
HU/SSH, network, footer/source access or execution. Only after the user explicitly authorizes this
exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff, complete 151ak and complete corrected
   151an. Verify 151ak SHA-256
   `9e35ba69fcd4885c339101e59f1d719681942571770a41023f20f6472782ea94` and 151an SHA-256
   `572a14636dfc44f23cdff5ac536838ea671a488ddcd24968097bc4942bb0d4e4` before editing;
   inspect the `transfer-vs-relearning` Git status and preserve unrelated work;
2. reproduce the focused/compatible suites and these three adversarial controls before editing:
   (a) change one shard and row-group `row_count` without changing footer bytes, recompute the
   schedule/projection and observe acceptance; (b) set `attempt_ordinal=-999` plus a transferred
   byte count different from the bound payload length and observe acceptance; (c) replace the
   serialized artifact-manifest payload with unrelated bytes, update only its SHA and observe
   acceptance;
3. replace the suffix-only footer check with actual Parquet footer framing and metadata parsing.
   Parse the final eight bytes as little-endian metadata length plus `PAR1`, require the retained
   range to contain the complete declared metadata and use a frozen parser/tool identity to derive
   `row_count`, `row_group_count`, row-group row counts and byte/layout fields. Recompute every
   ledger field from parsed footer evidence; a caller-supplied row count must never be authoritative;
4. correct the fixed-tail protocol. Freeze a deterministic two-stage bounded procedure: retrieve
   the trailer needed to learn the exact metadata length, then retrieve exactly the complete
   footer metadata plus trailer when it is not already present. Freeze the maximum accepted footer
   size, exact Range headers, request roles and fail-closed behavior. Reconcile the resulting worst-
   case request, byte, artifact-file and inode counts with the existing ceilings; do not assume
   every footer fits in 65,536 bytes;
5. consolidate or explicitly justify duplicate HEAD requests. One immutable HEAD response may
   supply object metadata and route-header evidence only when both logical artifacts bind the same
   exact retained response bytes and all required header fields. Parse and reconcile request URL,
   final URL/redirect chain, HTTP status, content length/range, ETag/LFS OID, content type and
   encoding rather than accepting a hash-bound arbitrary JSON object;
6. repair request/retry semantics. A logical request ID may have a contiguous zero-based sequence
   of attempts; attempt IDs must be unique; `attempt_ordinal` and `retry_ordinal` must be integers
   with exact frozen meaning; at most one successful terminal attempt is allowed. Count actual
   retry attempts once rather than summing caller ordinals. Require method/status/outcome/range and
   shard/path foreign keys to agree with the route ledger;
7. for every response evidence artifact, require
   `response_transferred_bytes == len(actual_payload_bytes)` and
   `response_sha256 == SHA256(actual_payload_bytes)`. A response artifact reused across ledgers
   must have one unambiguous role or an explicitly frozen multi-role binding; conflicting reuse
   fails closed;
8. serialize the artifact-manifest rows canonically inside the validator and require the supplied
   manifest payload bytes to equal that serialization before checking its SHA. Bind the final audit
   to supplied canonical audit bytes, the corrected 151an contract SHA, exact manifest SHA,
   artifact paths/counts/bytes, request totals, scratch root and final-write order. Do not accept
   an arbitrary payload merely because the caller supplies its matching hash;
9. replace the fake `b"footer-only...PAR1"` positive fixture with a genuinely parseable minimal
   Parquet footer fixture. Prefer a deterministic checked-in byte fixture or a frozen parser-based
   generator; if an optional dependency is needed, its absence must block scientific execution,
   not silently skip the executable-contract proof. Add one-at-a-time negative tests for truncated
   trailer, false `PAR1`, inconsistent metadata length, fabricated row counts/layout, malformed
   headers, invalid retry chain, byte-count mismatch, unlinked manifest payload and unbound audit;
10. append-only correct and re-freeze 151an, preserving all prior hashes. It may retain
    `EXECUTION_READY_METADATA_FOOTER_ONLY` only after the strict positive control and every listed
    mutation behave correctly. Keep 151ao/151ap reserved and uncreated;
11. rerun focused and compatible suites with the same three documented collection exclusions, and
    update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff with
    exact commands, counts, hashes and remaining gates;
12. do not use HU/SSH, network/public metadata, source/footer/row access, corpus materialization,
    scoring, inference, model/tokenizer access, GPU/Slurm, training, cleanup/deletion or create
    151ao/151ap or Documents 152–154 during this correction.

## Completed Current Task Override — Make 151an metadata/footer feasibility execution-ready

This is one bounded **local-only** contract/code/test correction. It does not authorize HU/SSH,
network, source/footer retrieval, corpus access or execution. Only after the user explicitly
authorizes this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff, 151ak and 151an completely. Verify 151ak
   SHA-256 `9e35ba69fcd4885c339101e59f1d719681942571770a41023f20f6472782ea94` and
   151an SHA-256 `435e0c25cedd7fd8fcb70862c637040300c2d5b201bfb5fa25c2b20232e71096`
   before editing; inspect the `transfer-vs-relearning` Git status and preserve unrelated work;
2. record the official Dataset Viewer constraint from
   `https://huggingface.co/docs/dataset-viewer/rows`: `/rows` accepts only
   dataset/config/split/offset/length (length <= 100) and returns split-global row slices. It does
   not expose a frozen revision or selected-shard parameter. Mark the `/rows` branch unavailable
   for immutable 32-shard path-stratified provenance unless a separately verified official route
   proves both bindings; do not infer them from row content or current cache state;
3. split the current 151an scope into a **metadata/footer feasibility wave only**. That wave may
   later resolve the 32 immutable direct-file routes, LFS/object identities, sizes, footer bytes,
   exact row counts and row-group layout, then compute a pre-row retrieval schedule plus projected
   request/byte budget. It must not retrieve the 10,000 corpus rows, run LID/quality/PII/dedup,
   create a sample manifest or claim corpus calibration. A later sample wave requires its own
   frozen contract after feasibility is known;
4. freeze one exact scratch root for that future metadata/footer wave, mandatory HU
   storage/path/inode preflight, exact official route allowlist, HTTP methods/range rules, request,
   retry, single-response, total-byte, file-count and timeout ceilings. Existing roots and HU home
   remain read-only. No wildcard host/path or full-shard fallback is allowed;
5. correct object-integrity semantics. Keep the immutable LFS object OID/full-object identity and
   size as source metadata. Introduce a separate `object_metadata_evidence_sha256` for the compact
   canonical evidence payload. Never require the compact artifact's bytes to hash to the full
   Parquet object's SHA. Footer SHA must bind actual footer-range bytes; license SHA must bind exact
   license/attribution bytes. Full-object SHA verification requires the full object and remains
   unverified in a footer-only wave unless the immutable LFS OID supplies the authoritative object
   identity with its evidence explicitly classified;
6. remove the compact/text-free contradiction. Metadata, headers, LFS pointers and Parquet footer
   bytes may be retained as compact evidence. Corpus-row or compressed row-group response bytes are
   outside this wave; they must not be listed as required response artifacts. State explicitly
   that a later row-retrieval wave may require restricted scratch payload evidence and a separate
   retention/license decision;
7. use one route-kind vocabulary everywhere, for example `parquet_footer_range`; do not mix
   `rows`, `/rows` and URLs in the same field. Separate `route_kind` from `request_url`. Align the
   metadata validator and tests with the footer-only route. Do not make the final 151ak sample
   validator claim support for Parquet sampling until a later executable extraction design exists;
8. treat the structural `373` window count only as fixture evidence. The metadata/footer wave must
   recompute allocation, midpoint positions, minimum requests/ranges and projected bytes using the
   32 real footer row counts/row groups. It may end `BLOCKED` if the frozen sampling design cannot
   fit a precommitted later retrieval envelope; it must not change the estimand after inspecting
   source data;
9. append-only correct and re-freeze 151an, preserving its previous SHA. Set it
   `EXECUTION_READY` only for the bounded metadata/footer feasibility scope if every route, root,
   output and ceiling is exact; otherwise keep `PREPARATION_BLOCKED` and state the remaining item.
   Keep 151ao/151ap reserved and uncreated;
10. add local tests for the separated object-OID versus metadata-artifact hashes, exact route-kind
    vocabulary, absent revision/shard binding on `/rows`, footer-byte binding, forbidden row
    payloads, fixed scratch root/output set and fixture-only 373 classification. Rerun the focused
    and compatible suites with the same three documented collection exclusions;
11. update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff
    with exact hashes and the honest next gate. Do not alter historical result documents silently;
12. do not use HU/SSH, network/public metadata, source/footer/row access, corpus materialization,
    scoring, inference, model/tokenizer access, GPU/Slurm, training, cleanup/deletion or create
    151ao/151ap or Documents 152–154 during this local correction.

## Completed Current Task Result — Make 151an metadata/footer feasibility execution-ready

Under the explicit local-only override, the 151an preparation contradiction was corrected without
HU/SSH, network, public metadata, source/footer/row access or execution. The prior 151an SHA-256
`435e0c25cedd7fd8fcb70862c637040300c2d5b201bfb5fa25c2b20232e71096` was preserved as the
pre-correction record; the append-only corrected Document 151an is frozen at SHA-256
`572a14636dfc44f23cdff5ac536838ea671a488ddcd24968097bc4942bb0d4e4`.

The current contract is `FROZEN — UNEXECUTED — EXECUTION_READY_METADATA_FOOTER_ONLY`. It fixes
the new scratch root `/vol/tmp2/yesildau/luna_vngrs_metadata_footer_feasibility_v1`, the exact
seven top-level outputs, the 32-path immutable direct-file allowlist, one
`parquet_footer_range` vocabulary, GET/range and HEAD request rules, `/rows`/datasets-server
exclusion, separate LFS/full-object versus compact metadata hashes, actual footer/license byte
bindings, forbidden corpus-row/compressed-row-group/full-shard payloads, zero-row scope, the
pre-row schedule/projection and the `128` attempts / `28` retries / `4 MiB` single response /
`64 MiB` total / `7,200` second / `128` files / `128` inode ceilings. The structural `373`
window result remains fixture-only and does not alter the estimand. Documents 151ao/151ap remain
reserved and uncreated.

Local verification completed with `34 passed, 1 skipped in 11.15s` for
`PYTHONPATH=src python3 -m pytest -o addopts='' -q tests/test_vngrs_preparation.py`. The compatible
repository suite completed with `264 passed, 8 skipped in 16.97s` (`real 17.14s`) using the same
three documented collection exclusions. These are local implementation checks only. The primary
gate remains `blocked_by_measurement_design`, contributing
`blocked_by_corpus_selection_or_materialization`; `ready_to_measure` and `ready_to_train` remain
false. No source/footer evidence, corpus selection/materialization, 151ah/151ak/151an execution,
scoring, inference, evaluation, GPU/Slurm, training, cleanup or Documents 152–154 action occurred.

## Completed Current Task Result — Close the final 151an footer/parser/ledger byte-integrity gaps

Under the explicitly authorized local-only override, the local vngrs implementation was corrected
without HU/SSH, network, public metadata, source/footer/row access or execution. The previous
151an SHA-256 `572a14636dfc44f23cdff5ac536838ea671a488ddcd24968097bc4942bb0d4e4` was preserved;
the append-only strict-parser correction is frozen at
`e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac`.

The validator now parses a complete Parquet footer using the pure-Python
`vngrs_parquet_footer_compact_thrift_parser_v1`, derives row/group/layout values from the parsed
metadata, performs the `bytes=-8` trailer then exact-footer range protocol, reconciles shared HEAD
object/route roles, enforces contiguous logical-request retry chains, binds actual response bytes
and SHA-256 values, and requires canonical evidence-manifest and final-audit payload bytes. The
nominal 32-shard profile is 97 logical requests/artifacts and 104 files/inodes; all stated
128-attempt, 28-retry, 64 MiB, 4 MiB, 7,200-second, 128-file and 128-inode ceilings remain frozen.

Local verification is `36 passed, 1 skipped` focused and `266 passed, 8 skipped` compatible with
the same three collection exclusions. Negative controls fail closed for truncated/false/inconsistent
footers, fabricated row counts/layouts, malformed headers, invalid retry ordinals, response-byte
mismatch, unlinked manifest bytes and unbound audit bytes. The contract remains unexecuted;
`blocked_by_measurement_design` is primary with
`blocked_by_corpus_selection_or_materialization` contributing, and `ready_to_measure`/
`ready_to_train` remain false. Documents 151ao/151ap remain reserved and uncreated.

No HU/SSH, network, source/footer access, execution, corpus, scoring, inference, evaluation,
GPU/Slurm, training, cleanup/deletion or Documents 152–154 action occurred.

## Historical Single Next Authorization Request — superseded by completed retry correction

Authorize only the local-only “Repair 151an retry semantics and worst-case artifact bounds”
override above. Do not authorize 151an execution yet. The correction must preserve the current
151an hash as history, prove a genuine failure-then-success retry chain, reconcile the exact
worst-case file/inode arithmetic and keep HU/network/source/footer access and 151ao/151ap creation
outside scope.

## Completed Current Task Override — Close the remaining 151ak evidence-binding and sampling-schedule gaps

This is one bounded **local-only** code/test/append-only contract-correction task. It does not
authorize HU/SSH, public network, route/footer retrieval, corpus access or 151ak execution. Only
after the user explicitly authorizes this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff and complete current 151ah/151ak. Verify
   151ah SHA-256
   `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8` and 151ak SHA-256
   `1920f1c58d8ada250af50d1f088f5ad2fc3a15e8221f84d92f5458dab415154b` before editing;
2. inspect the `transfer-vs-relearning` Git status and preserve all unrelated user artifacts;
3. independently reproduce the current focused/compatible suites and the three known adversarial
   facts: the nominal positive graph passes, 90/100 successful requests exceed the declared shard
   row bounds, and format-valid child source hashes plus an updated parent hash pass the current
   source validator;
4. correct request/source validation so every successful request is joined to exactly one frozen
   source-shard row and route row. Require `0 <= offset`, `1 <= length <= 100`, and
   `offset + length <= exact shard row_count`. Reject overlapping or duplicate source windows and
   require the 10,000 records to be generated by the frozen, explicitly serialized sampling
   schedule rather than by arbitrary contiguous windows. If the exact midpoint design cannot be
   represented under the 100-request `/rows` envelope, fail closed and record the feasibility
   contradiction; do not silently redefine the sampling estimand;
5. distinguish the synthetic local fixture from real source evidence. Rename/classify it as a
   `STRUCTURAL_SYNTHETIC_CONTROL`; it must not demonstrate source provenance or route feasibility.
   Add a negative test proving that out-of-bounds windows block even when all manifests and parent
   hashes are internally consistent;
6. replace child-hash syntax checks with explicit evidence bindings suitable for a later bounded
   route/footer wave. Each shard object/footer/license hash and each route-evidence hash must name
   an exact compact evidence artifact or canonical payload whose bytes are rehashed. A newly
   computed parent hash alone must not turn invented child hashes into `verified` evidence. Keep
   real corpus row payloads and network access outside this local task;
7. close the near-dedup result gap. The final gate must recompute the affected count/rate from a
   canonical affected-record/pair evidence payload, or validate an exact hash-bound compact
   evidence artifact sufficient to reproduce those values. Add a mutation test in which only the
   near-dedup count/rate is changed and require `BLOCKED`. Apply the same evidence-class language
   consistently: values derived from the manifest may be `recomputed`; execution-derived values
   without raw text must be labeled `hash_bound_execution_evidence`, not independently
   recomputed;
8. append-only correct and re-freeze 151ak to describe these rules and preserve every prior hash.
   Do not claim route/footer feasibility, source access or scientific sample completion;
9. if `151an` is still unused, create it only as a frozen **unexecuted** minimal
   route/footer/byte-and-sampling-schedule evidence-resolution contract. It must freeze exact
   official routes, byte/request ceilings, required compact evidence payloads, fail-closed rules
   and the decision between `/rows` windows versus bounded Parquet footer/range evidence before
   any source access. Reserve result/gate documents but do not create them;
10. add focused positive and one-at-a-time negative tests for out-of-bounds windows, overlapping
    windows, schedule mismatch, unbound shard/footer/license/route hashes, unbound response hashes
    where applicable, and tampered near-dedup evidence. Rerun the focused suite and the compatible
    suite with only the same three documented collection exclusions;
11. update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff
    with exact hashes, commands, counts and the honest gate. Do not modify historical result
    documents silently;
12. do not use HU/SSH, network/public metadata, route resolution, corpus access, execution,
    scoring, inference, model/tokenizer artifacts, GPU/Slurm, training, cleanup/deletion or create
    execution result/gate documents in this task.

## Completed Current Task Override — Seal the 151ak final evidence graph before route resolution

This is a bounded local code/test/append-only contract correction. It does not authorize route
resolution or any source access. Only after the user explicitly authorizes this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff and complete corrected 151ah/151ak.
   Verify 151ah SHA-256
   `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8` and 151ak SHA-256
   `16f2978b10fc2b71490917ffe9ed549b574d2b364865675f0f1d900fb4320d68` before editing;
2. inspect the `transfer-vs-relearning` Git status and preserve all unrelated user artifacts;
3. make the final 151ak profile immutable rather than caller-selected: exactly 10,000 raw records,
   immutable vngrs revision `ee5c6201ee84457a18182bfc483a7d8a7f3655ba`, exact schema
   `text/corpus/original_id`, and the frozen 32 selected shard paths/payload identity. A caller-
   supplied `sample_target_records=1` must never make the final contract eligible for `PASS`;
4. add a strict source-evidence validator. Require exactly 32 unique selected-shard rows matching
   the frozen path set and selection payload, with positive exact bytes/footer row counts and all
   contract-required object/footer integrity fields. Require structured route mapping for every
   selected shard. Recompute a canonical evidence SHA-256 from the evidence payload after excluding
   only its own hash field; do not accept an arbitrary 64-character string as proof;
5. freeze request-attempt semantics. Use a unique attempt ID plus a logical request ID and exact
   zero-based attempt/retry ordinal (or an equivalently explicit schema). Validate contiguous retry
   chains, HTTP-status/outcome consistency and count retry attempts once; do not sum cumulative
   retry ordinals. Append-only update the request-ledger schema if required;
6. enforce relational integrity between the 10,000 record-manifest rows and successful `/rows`
   attempts: every record request ID must reference one successful row response; repo, revision and
   shard must agree; `(request, record_index_within_response)` and `sample_index` must be unique;
   sample indexes must be exactly `0..9999`; every successful row response may contribute at most
   100 records; orphan requests/records, fabricated extra records and failed-attempt references must
   block. Validate `offset/length` structurally with `1 <= length <= 100`;
7. do not trust caller-provided gate booleans as final metrics. Recompute all derivable final gates
   from the exact record manifest and structured metric payloads, or validate canonical serialized
   metric artifacts whose actual bytes/SHA/size match their output-manifest rows. Define the exact
   inputs for near-dedup, exact-dedup, overlap and split decisions;
8. bind the output chain to actual payload bytes. Recompute every seven-artifact byte count and
   SHA-256, recompute the canonical `output_artifact_manifest.jsonl` SHA-256, and validate the final
   audit against those computed values. A list containing plausible paths and `"a" * 64` hashes
   must not be final evidence;
9. add an explicit final positive-control test with the frozen 10,000-record cardinality and all
   evidence relationships valid. Then mutate one property at a time and require `BLOCKED`: target
   count 1/9,999, one missing/wrong shard, duplicate/missing sample index, orphan request ID,
   response length 101, failed HTTP status marked success, broken retry chain, fake source hash,
   fake artifact bytes/hash, unlinked metric artifact and altered final audit;
10. preserve preparation helpers for small fixtures as `DIAGNOSTIC_ONLY`; ensure only the strict
    frozen final profile may emit `PASS`/`CONDITIONAL`;
11. append-only correct and re-freeze 151ak, preserving every prior SHA. Keep it
    `FROZEN — PREPARATION_BLOCKED — UNEXECUTED`; keep 151al/151am uncreated;
12. rerun the focused and compatible suites with the same three documented collection exclusions,
    record exact commands/counts/times, and update `AGENTS.md`, `ssh-client/README.md`, the index and
    this living handoff.

Hard exclusions:

- no HU/SSH, network, public metadata/route pass, corpus row/shard access, materialization or new
  scratch root;
- no 151ah/151ak execution, model/tokenizer access, scoring, inference, evaluation, GPU/Slurm,
  training, cleanup/deletion, 151al/151am or Documents 151ai/151aj/152–154;
- no readiness or scientific-result claim.

Completion requires the one-record fabricated-evidence probe described in the current validation
summary to return `BLOCKED`, plus a genuine 10,000-row positive control whose evidence hashes and
foreign-key relationships are recomputed rather than trusted.

## Completed Current Task Override — Close final 151ak gate ownership, composite-manifest and evidence gaps

This is a final local code/test/append-only contract-consistency correction before any route pass.
Only after the user explicitly authorizes this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff, complete corrected 151ah and 151ak;
   verify 151ah SHA-256
   `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8` and 151ak SHA-256
   `eb520ece20b157ec342cd6511589907b561dd7cea5e4d68cb1cd84327c92bd8e` before editing;
2. inspect `transfer-vs-relearning` Git status and preserve all unrelated tracked/untracked files;
3. require valid LID evaluator ID and lowercase SHA-256 on **every raw scientific row**, not only
   rows that survive retention. Add an adversarial test with one missing-identity raw row and one
   valid retained row; the final scientific decision must be `BLOCKED`;
4. restore `corpus` and the explicit composite identity to the frozen record-manifest schema.
   Validate natural identity as `(immutable_revision, corpus, original_id)`, while retaining
   `source_repo` as repository provenance. Add a scientific-profile test proving that the same
   `original_id` in two different source corpora is valid and that a true same-corpus duplicate is
   rejected. Append-only correct the “31-field” wording and exact schema;
5. compute `raw_text_character_count` from decoded pre-normalization text. Keep normalized length
   separate if useful, but do not label normalized length as raw. Add whitespace/control/Unicode
   normalization tests that make the two lengths differ;
6. separate stage diagnostics from the final calibration decision. `prepare_records` alone must
   not emit a final contract `PASS`. Implement a contract-level evaluator/runner that can return
   final `PASS`/`CONDITIONAL` only after validating the exact record manifest, request ledger,
   source/footer/route evidence, request/retry/byte limits, overlap/dedup/split metrics,
   output-artifact manifest and final audit. Missing any one of these must be `BLOCKED`; a local
   preparation stage may use `DIAGNOSTIC_ONLY` or a clearly non-final stage status;
7. freeze and test request-ledger aggregate validation: at most 128 total attempts, 100 successful
   row requests, 28 retries, 64 MiB total response bytes and 4 MiB per response, with arithmetic
   reconciliation among attempt, retry and outcome rows. Do not treat a manually set boolean such
   as `source_integrity_verified=True` as sufficient final evidence;
8. require the final-audit path to equal the frozen `calibration_audit.json`, not merely avoid a
   collision. Test malformed paths, missing manifest SHA, reordered rows and self-reference;
9. reconcile the estimator/performance narrative with inspectable tests. Either add the exact
   deterministic cases that produced the currently reported `0.9048` and `0.0` values or correct
   the completion evidence to the actual committed cases (`0.8182`, `0.8108`, `0.2000`). Add an
   actual near-ceiling text fixture (at least 90,000 characters under the 100,000-character bound)
   and a multi-record candidate-cluster test; do not call the 21,000-character fixture near-ceiling;
10. clarify gate ownership append-only in 151ak: model-neutral sample calibration does not require
    tokenizer fertility under Section 5, while tokenizer-yield/dose adequacy remains a later
    151ah/materialization or model-specific planning blocker. Do not list fertility ambiguously as
    both mandatory and out of scope;
11. append-only correct and re-freeze 151ak, preserving every prior SHA. Keep status
    `FROZEN — PREPARATION_BLOCKED — UNEXECUTED`; do not create 151al/151am;
12. rerun the focused suite and the compatible suite excluding only the same three documented
    collection failures. Record exact commands, counts and elapsed times. Update `AGENTS.md`,
    `ssh-client/README.md`, the documentation index and this living handoff.

Hard exclusions:

- no HU/SSH, network, route-resolution pass, row/shard payload, corpus download, sample calibration,
  materialization or scratch-root creation;
- no model/tokenizer access, scoring, inference, evaluation, GPU/Slurm or training;
- no 151ah/151ak execution, cleanup/deletion, 151al/151am creation or Documents
  151ai/151aj/152–154;
- no claim of `ready_to_measure`, `quality_pass` or `ready_to_train`.

Completion requires the adversarial cases above to pass and a strict distinction between local
stage diagnostics and the final multi-artifact calibration gate. Route-resolution work is not the
next step until this local authority surface is coherent.

## Completed Current Task Override — Close 151ak implementation, estimator and route-feasibility blockers

This is a local code/test/contract-consistency task plus a bounded official-metadata-only route
analysis. It does not authorize sample retrieval or 151ak execution. Only after the user explicitly
authorizes this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff, Documents 145, 148, 150, corrected 151ah
   and complete corrected 151ak. Verify 151ah SHA-256
   `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8` and 151ak SHA-256
   `8fc6d3dbc89f9b71e7b9e1f6ca787fce81bbcde2cf715abfa2fec54eb0b07bd5` before editing;
2. inspect the `transfer-vs-relearning` Git status and preserve every unrelated tracked and
   untracked user file. Limit code changes to the vngrs package/tests and directly required local
   dependency metadata;
3. replace the current ad-hoc overall status expression with an explicit contract gate evaluator.
   For the frozen scientific profile, any missing/unresolved LID evaluator identity or status,
   invalid manifest/schema/cardinality, incomplete overlap audit, bound exceedance or unresolved
   required gate must return `BLOCKED`. Do not classify a partially retained sample as sufficient;
4. implement a frozen record-manifest serializer and strict schema/type validator matching every
   required field in 151ak, including request/source/revision/index/retrieval fields, flattened LID,
   quality, PII, dedup, contamination, overlap, retention and split fields. Require exactly one
   unique manifest row per raw record and exactly 10,000 rows in the scientific profile. Compact
   evidence must not contain raw or normalized corpus text; text may exist only in ephemeral
   processing objects. `raw_manifest_complete` must be the result of schema/cardinality/identity
   validation, not `len(output) == len(input)` alone;
5. replace the claimed `sha256_feature_once_splitmix_integer_permutations_v1` implementation with
   either a standard deterministic universal-hash MinHash family whose coefficient generation,
   prime, integer width, seed and version are frozen, or a pinned, inspectable library/version with
   equivalent frozen semantics. Do not retain a protocol name that the code does not implement;
6. add scientific estimator tests that compare exact 5-gram Jaccard with estimated MinHash Jaccard
   across deterministic pairs spanning low, boundary and high similarity, including known pairs
   above `0.80`. Report error/recall summaries without selecting tolerances after seeing outcomes.
   Test identical, exact-duplicate, near-duplicate and unrelated cases;
7. add realistic bounded performance/adversarial tests: long web-like records near the configured
   character ceiling, duplicate/near-duplicate clusters that create LSH candidates, candidate-
   explosion fail-closed behavior and memory/output bounds. The earlier 2,000 short random-like
   zero-candidate timing remains historical evidence but is not execution-readiness evidence.
   If the implementation retains all signatures, buckets or candidate pairs in memory, describe it
   truthfully and either implement the frozen streamed/bounded protocol or append-only correct the
   contract before execution;
8. implement largest-remainder allocation with exact integer arithmetic (`count * target`, integer
   division and modulus) and deterministic path tie-breaking. Add large-count and exact-tie tests;
9. strengthen output-chain validation: SHA-256 values must be exactly 64 hexadecimal characters;
   manifest paths and `artifact_order` must equal the frozen order with no gaps, duplicates or
   unknown rows; the final audit must validate this order and chain rather than only row count;
10. perform only a bounded official-public-metadata route-feasibility analysis. Retrieve no corpus
    rows and no complete shards. Prove how the 32 selected shard paths and exact footer row counts
    map to an official endpoint and whether midpoint-systematic positions can be retrieved within
    coherent total-success/retry/metadata request and byte limits. Because `/rows` returns
    contiguous windows of at most 100 rows, do not infer that 100 requests can retrieve 10,000
    dispersed positions. If exact mapping cannot be proved, leave `PREPARATION_BLOCKED` and state
    the precise missing evidence. Freeze limits whose combined accounting is arithmetically
    reachable; do not advertise 32 possible retries when only 28 total request slots remain after
    100 required successful row calls;
11. append-only correct and re-freeze 151ak only where the implementation or route analysis changes
    the frozen protocol. Preserve all prior hashes. Do not weaken fail-closed gates merely to make
    the contract executable. Keep 151al/151am reserved and uncreated;
12. rerun the focused vngrs suite and the compatible repository suite, record exact commands,
    exclusions, counts and elapsed time, and run the realistic estimator/performance probes above;
13. update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff.
    Report changed files, hashes, test/probe evidence and every residual blocker.

Hard exclusions:

- no HU/SSH, Dataset Viewer row payload, corpus-row/full-shard download, sample calibration,
  materialization or new scratch root;
- no model/tokenizer weights, scoring, inference, benchmark evaluation, GPU/Slurm or training;
- no 151ah/151ak execution, cleanup/deletion, 151al/151am result/gate creation or Documents
  151ai/151aj/152–154;
- no claim of `quality_pass`, `ready_to_measure` or `ready_to_train`.

Completion requires contract-compliant code/schema/gate tests, scientifically defensible MinHash
semantics and an honest route-feasibility verdict. A passing unit suite or metadata lookup alone is
insufficient.

## Completed Task Definition (Historical) — Repair the vngrs pipeline and 151ak before any sample execution

Independent review accepts the official metadata, exact systematic shard formula/payload and the
fact that the new code is a preparation scaffold. It does **not** authorize 151ak execution. The
reported 14/1 and 244/8 test outcomes reproduce, but several tests currently encode behavior that
contradicts the frozen scientific intent. This task is a local code-and-contract correction plus
bounded public route-metadata resolution, not sample calibration.

Only after the user explicitly authorizes this exact override:

1. read `AGENTS.md`, `ssh-client/README.md`, this handoff, Documents 145, 148, 150, corrected
   151ah and complete 151ak. Verify 151ah SHA-256
   `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8` and 151ak SHA-256
   `97a2d8a53cc8ff8390f71ddb833e9582d8b46fb9793deeb6673483a1384df012`. Correct the invalid
   63-character 151ah hash ending in `...a1e` wherever it appears in `AGENTS.md`,
   `ssh-client/README.md` and the documentation index; the real local hash ends in `...a1e8`;
2. inspect the `transfer-vs-relearning` status first and preserve every unrelated tracked and
   untracked user artifact. Change only the vngrs package/tests and directly affected dependency
   metadata when necessary;
3. correct source identity to use the composite natural key `(corpus, original_id)` under the
   immutable revision. Treat integer `original_id = 0` as valid. Detect duplicates on the
   composite key, include `corpus` in the record manifest and test same `original_id` across two
   distinct source corpora separately from a true composite-key duplicate;
4. make LID part of retention rather than a diagnostic-only annotation. Freeze canonical accepted
   Turkish labels and a per-record confidence rule in 151ak; unresolved evaluator/status,
   non-Turkish output or below-threshold confidence must not enter the retained pool. Preserve raw
   LID denominators and mixed-line diagnostics;
5. preserve one manifest row for **every raw sampled record**, including quality/PII/LID/
   contamination/exact-dedup exclusions. Add explicit retention status and rejection reason codes.
   Do not return only retained records as the scientific manifest surface;
6. compute and report benchmark/synthetic overlap on the raw/exact-canonical population and the
   retained population separately. A record excluded for synthetic contamination must not become
   invisible to the raw benchmark-overlap audit. Update the currently contradictory test;
7. replace the in-memory all-pairs near-dedup implementation with a deterministic scalable
   candidate-generation design consistent with the frozen 5-gram, 128-permutation, seed-42 and
   threshold-0.80 semantics. Freeze LSH/banding or another justified complete candidate protocol,
   stream compact results, bound memory/output, and define the near-dedup rate by affected records
   or another explicit denominator rather than ambiguous pair-count/record-count arithmetic;
8. correct quality metric implementation (for example code coverage must use matched character
   coverage, not marker count divided by document length) and classify any threshold without a
   literature/precommitted basis as exploratory calibration rather than a scientific PASS gate.
   Do not invent a comprehensive PII or adult-content claim from the current small regex set;
9. make empty, incomplete or non-10,000 scientific input fail closed. Unit-test helpers may use
   smaller explicit expected counts, but the frozen 151ak execution profile must require exactly
   10,000 complete raw manifest rows;
10. append-only correct and re-freeze 151ak. Replace equal 313/312 allocation with a frozen
    row-count-proportional largest-remainder allocation once exact footer counts are available, or
    explicitly freeze two different estimands: shard-balanced diagnostic and row-weighted corpus
    estimate. Report per-shard results and weights; do not present the equal-shard aggregate as a
    release-representative rate;
11. resolve only official public metadata needed to show that 10,000 systematic row retrieval is
    feasible within the frozen request/byte bounds. The pass is limited to 96 requests, 64 MiB
    total and 4 MiB per response, and may retrieve repo/API metadata, exact object/byte/license
    evidence and Parquet footer/range metadata only. No corpus-row or full-shard payload. If the
    route requires more than 96 attempts or 64 MiB, change the future contract bounds before any
    execution; do not hope the execution will fit;
12. add a named self-reference-free `output_artifact_manifest.jsonl`, freeze the final-audit
    ordering, and reserve distinct index-safe result/gate documents for a later 151ak execution
    (prefer 151al/151am if free). Do not use 151ai/151aj, which remain reserved for 151ah;
13. run the vngrs suite, a bounded performance test that exercises the near-dedup path at a
    meaningful scale, and the compatibility suite excluding only the three already documented
    environment/import collection failures. Record commands, counts and elapsed times;
14. update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this handoff. Report
    all changed files, corrected hashes, test/performance evidence and residual blockers.

Hard exclusions:

- no HU/SSH, corpus-row/full-shard download, sample calibration or materialization;
- no model/tokenizer weights, scoring, inference, benchmark evaluation, GPU/Slurm or training;
- no 151ah/151ak execution, cleanup/deletion, 151ai/151aj or Documents 152–154;
- no claim of `quality_pass`, `ready_to_measure` or `ready_to_train`.

Completion requires corrected code/tests and a corrected execution-ready-or-explicitly-blocked
151ak. A passing unit suite alone is insufficient if it preserves the current contract
contradictions.

## Completed Current Task Override — Validate the completed inventory and freeze the corpus materialization decision/contract

Under the explicit authorization, the local hashes of corrected 151ab, 151ae and 151af were
verified, and HU was accessed only through the documented SSH route for the six named inventory
files. The six-file inspection was read-only; no HU file, root or source path was changed. Exact
HU reconciliation is:

```text
source_allowlist_ledger.jsonl  = 60 rows, 51,879 bytes
model_tokenizer_inventory.jsonl = 5 rows, 4,142 bytes
evaluation_input_inventory.jsonl = 6 rows, 2,938 bytes
c1_reconciliation.jsonl         = 17 rows, 12,885 bytes
inventory_report.json            = 3,004 bytes
final_inventory_audit.json       = 2,702 bytes
```

C1 status counts are 12 `observed_existing_compact_evidence`, 2
`verified_existing_selected_manifest`, 1 `blocked`, 1 `existing_control_identity_stat_only` and
1 `existing_input_identity_stat_only`. Natural identity keys have no duplicates. The two
evaluation rows sharing a purpose label are distinct paths and are not duplicates. Model-state
coverage is three verified Qwen manifest rows plus two deliberate
`not_in_this_inventory_allowlist` rows for OLMo/Falcon; the latter are not inferred absent.
The selected adaptation corpus row is explicitly absent and blocked, while trwiki is a stat-only
cross-domain control.

Created local Document 151ag (SHA-256
`54c9e1f8c626fb0f67a2d1f5396277304c30714074683b386ac39da3a8f22497`) as the frozen decision:
vngrs is only the conditional primary materialization candidate, trwiki is control-only and
CulturaX is `excluded_access_blocked`. Created local Document 151ah (SHA-256
`a8c1d1d2082ec3ae5b31ace5dc0a9506ace90f82d0f7bd1a2c1a528069ef2269`) as the frozen,
unexecuted vngrs acquisition/materialization contract. 151ah is
`PREPARATION_BLOCKED` because exact release file/shard routes and execution-time license evidence
were not present in the authorized six files; no routes or shards were invented. Its proposed
new root was not created, and 151ai/151aj remain reserved and uncreated.

The primary gate remains `blocked_by_measurement_design`, with contributing
`blocked_by_corpus_selection_or_materialization`; `ready_to_measure=false` and
`ready_to_train=false`. No 151ah execution, network/download, corpus materialization, scoring,
inference, evaluation, GPU/Slurm, training, cleanup/deletion, prior-root write or Documents
152--154 action occurred.

## Completed Current Task Override — Execute the corrected 151ab bounded HU evidence/input inventory once

The explicitly authorized bounded inventory is complete. Corrected Document 151ab SHA-256
`3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c` was verified before
execution. The wave used HU/SSH, performed the mandatory preflight, wrote only the one new scratch
root and completed its post-run audit. The operational result is PASS; the scientific result is
BLOCKED because the selected adaptation corpus is not present in the closed allowlist.

The following frozen requirements governed the completed execution:

1. read `AGENTS.md`, `ssh-client/README.md`, this living handoff, Documents 127, 143, 145, 150,
   151, 151aa and the complete corrected 151ab;
2. verify 151ab SHA-256 exactly equals
   `3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c`; otherwise stop;
3. use only the documented `ssh-client` route and run the mandatory HU home/scratch capacity,
   inode and resolved-path preflight before reading or writing;
4. keep every allowlisted source and HU home immutable/read-only. Create and write only under
   `/vol/tmp2/yesildau/luna_measurement_design_input_preparation_v1`;
5. obey the closed source allowlist in 151ab exactly. Directory entries permit only bounded
   path/stat traversal and the explicitly referenced compact metadata; do not recursively read raw
   corpus text, caches, checkpoints or weight bytes;
6. enforce all frozen limits: 0 public HTTP requests, 0 downloads, 0 recursive corpus reads,
   0 large-weight rehash bytes, at most 256 source stat entries, 256 metadata files, 16 MiB source
   metadata, 64 new files, 16 MiB outputs, 900 seconds and one writable root;
7. create only the eight compact outputs named in 151ab. Write
   `final_inventory_audit.json` last; it must reconcile/hash the seven earlier named outputs and
   must not hash or include itself recursively;
8. inventory the two selected Qwen M1 seed chains, Qwen base/source identity linkage, existing
   WikiText-2/trwiki evidence, existing candidate-corpus evidence and supported C1 registry
   fields. Do not infer that OLMo/Falcon artifacts exist and do not call any model Turkish-unseen;
9. verify whether a selected adaptation-corpus artifact exists. Do not select or materialize one.
   If absent, report `blocked_by_corpus_selection_or_materialization` truthfully; do not fabricate
   a primary in-domain split or hash;
10. perform the required post-run storage audit and verify that prior roots and HU home were not
    changed;
11. create only Documents 151ae (execution result) and 151af (post-inventory decision gate), with
    exact paths, counts, hashes, limits, evidence/blocker classification and operational audit;
12. update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff.

Hard exclusions:

- no network/public HTTP or download;
- no model/tokenizer/corpus/benchmark materialization;
- no raw corpus recursive read and no large-weight rehash;
- no primary in-domain split construction;
- no scoring, inference, evaluation, GPU/Slurm or training;
- no cleanup/deletion, HU-home write or prior-root write;
- do not create 151ac/151ad or Documents 152–154;
- do not claim `ready_to_measure` or `ready_to_train`.

The completed honest outcome is operational `PASS_OPERATIONAL_INVENTORY` but scientific `BLOCKED`:
the compact inventory replaced uncertainty about existing evidence without fabricating a selected
corpus or primary split. The next decision therefore concerns corpus selection/materialization and
remaining measurement-design inputs, not training.

## Completed Current Task Override — Repair the 151ab input-preparation authorization contradiction

The explicitly authorized local documentation-only correction is complete. The existing 151ab
content and SHA-256 `3320516e674c12288d70396e31b33c059550c15365caabe9453e932e3858e2dc` were
preserved as the pre-operational-correction record; the append-only operational correction was
added and the final frozen SHA-256 is
`3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c`. No HU/SSH, network,
scratch-root creation, download, materialization, scoring, inference, evaluation, GPU/Slurm,
training, cleanup/deletion, result/gate document or Documents 152–154 action occurred. The
future request now uses HU/SSH only if separately authorized, a closed read-only source allowlist,
one new scratch root and bounded inventory outputs; it does not construct the unselected primary
in-domain split. The remaining gate is `blocked_by_measurement_design`, with corpus selection or
materialization as an explicit contributing blocker; `ready_to_measure` and `ready_to_train` are
false. The detailed requirements below are retained as the chronological task record and are not
an active authorization.

> Historical requirements retained for audit; not active after completion:

Only after the user explicitly authorizes this exact override, perform one **local documentation-
only append-only operational correction**:

1. read `AGENTS.md`, `ssh-client/README.md`, this living handoff, Documents 127, 143, 145, 150,
   151, 151aa and the complete corrected 151ab;
2. verify and preserve 151ab's current SHA-256
   `3320516e674c12288d70396e31b33c059550c15365caabe9453e932e3858e2dc` as the pre-operational-
   correction hash;
3. append a second effective correction to 151ab and re-freeze it with a new final SHA-256;
4. explicitly override the impossible phrase `local/source-read-only` plus `/vol/tmp2` writes.
   The future preparation wave, if separately authorized, may use the documented HU/SSH route,
   mandatory storage/path/inode preflight, read-only access to an exact frozen source allowlist,
   and writes only to the new scratch root
   `/vol/tmp2/yesildau/luna_measurement_design_input_preparation_v1`. HU home and every prior
   evidence/artifact root remain read-only;
5. freeze the future source allowlist from existing documented paths, at minimum covering only:
   - Qwen M1 seed-42 step-75 and seed-43 step-50 selected model-only manifests/artifact roots in
     Document 127, plus the approved home durability copy only as read-only fallback;
   - the Qwen base/source-model manifest already referenced by the frozen M1 evaluation chain;
   - frozen WikiText-2 input and its existing evidence;
   - frozen `trwiki-20260601` control split/manifest evidence;
   - the existing vngrs/provenance, benchmark registry and coverage-repair roots already named in
     Documents 151h/151r/151u/151y, all read-only.
   Resolve exact paths from those authorities; do not invent a path and do not add broad parent
   directories or globs as unrestricted sources;
6. freeze bounded inventory limits: metadata/manifests/config/tokenizer identity files only,
   path/stat inventory for large weights, no full weight re-hash unless an existing selected-model
   manifest specifically requires and authorizes it, no recursive corpus read, and explicit
   maximum file-count/metadata-byte limits in the future wave;
7. remove the claim that the next wave can prepare/freeze the primary in-domain Turkish split.
   Because the main adaptation corpus is not yet selected/materialized, the wave may only:
   - verify whether a selected-corpus artifact exists;
   - inventory existing candidate-corpus/sample/provenance evidence;
   - record `blocked_by_corpus_selection_or_materialization` if it does not exist;
   - verify existing WikiText/trwiki identities and model/tokenizer manifests;
   - reconcile C1 fields supported by those existing immutable sources.
   Actual in-domain split selection/materialization requires a later separate corpus decision and
   execution contract;
8. define the future output as compact manifests, ledgers and an inventory report only. It may not
   contain copied weights, raw corpus documents, benchmark scoring data or raw sensitive text;
9. reserve index-safe future result/gate documents without using 151ac/151ad if those remain
   reserved by the superseded baseline path; do not create any result/gate document now;
10. replace the Single Next Authorization Request in 151ab and this handoff with one bounded HU
    evidence/input **inventory** execution matching the corrected allowlist, preflight, read-only
    source roots and one writable scratch root. It must still forbid network/public HTTP,
    downloads, corpus/split materialization, scoring, inference, evaluation, GPU/Slurm, training,
    cleanup/deletion and Documents 152–154;
11. align `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff with
    the new final 151ab hash and corrected next request.

Hard exclusions in this correction turn:

- no HU/SSH or network access;
- no new scratch root or remote write;
- no model/tokenizer/corpus/benchmark access;
- no materialization, scoring, inference, evaluation, GPU/Slurm or training;
- no cleanup/deletion;
- do not create 151ac/151ad, any newly reserved result/gate documents, or Documents 152–154;
- do not claim registry PASS, `ready_to_measure` or `ready_to_train`.

This task is complete only when 151ab's future request is internally executable: HU/SSH and the
single new scratch write root agree, all source roots are read-only and explicitly bounded, and
the unselected in-domain corpus/split remains a blocker rather than a fabricated deliverable.

## Completed Current Task Override — Correct and re-freeze Document 151ab before any measurement execution

The explicitly authorized local documentation-only correction is complete. Document 151ab’s
original body and pre-correction SHA were preserved; the effective correction was appended and the
document was re-frozen with final SHA-256
`3320516e674c12288d70396e31b33c059550c15365caabe9453e932e3858e2dc`. No HU/SSH, network,
download, materialization, scoring, inference, evaluation, GPU/Slurm, training, cleanup/deletion,
151ac/151ad or Documents 152–154 action occurred. The global gate remains
`blocked_by_measurement_design` and `ready_to_train=false`.

The correction resolves the BPB/BPC naming conflict, separates the primary in-domain Turkish
held-out split from the `trwiki-20260601` cross-domain control, preserves the known WikiText-2
token-stream hash as distinct from file-byte evidence, freezes the exact model state matrix and
both thesis estimands, expands the review-field ledger, corrects candidate-role wording and
replaces the former baseline-scoring request with a bounded evidence/input-preparation request.

The detailed historical task requirements below are retained as the append-only record of what was
corrected; they do not authorize a new execution.

Only after the user explicitly authorizes this exact override, the worker must:

1. read `AGENTS.md`, `ssh-client/README.md`, this living handoff, Documents 144, 145, 148, 150,
   151, 151aa and 151ab completely;
2. verify the current local SHA-256 values of 151aa and 151ab before editing;
3. preserve the existing 151ab body and SHA as the pre-correction record, add an append-only
   effective correction, and re-freeze 151ab with a new final SHA-256;
4. correct the primary normalized metric name to **UTF-8 bits per byte (`BPB`)**. Do not call the
   byte-denominator formula BPC; BPC is reserved for a character denominator;
5. replace the single `trwiki` Turkish held-out design with two explicit roles:
   - a primary **in-domain, document-disjoint held-out split drawn from the finally selected
     Turkish adaptation corpus**;
   - `trwiki-20260601` as a frozen **cross-domain/control** split.
   Since the main adaptation corpus is not yet selected/materialized, keep execution preparation
   blocked rather than inventing the primary split hash;
6. preserve the known WikiText-2 token-stream hash
   `be2effefc9f0655b0fc5bc3052ecfd18b51bdfa48bffa1ab2d4f0c217b81c78f`, but distinguish it from
   the still-required file-byte SHA-256, manifest/path identity and exact scoring chain;
7. make the baseline state matrix exact:
   - OLMo and Falcon are **M0-only candidates** until separately authorized M1 acquisition exists;
   - Qwen baseline contains M0 plus both frozen M1 seed chains (seed 42 step 75 and seed 43 step
     50), subject to exact artifact manifests/hashes;
   - do not silently exclude OLMo/Falcon merely because their weights are not already cached;
     inventory/acquisition must be a separately authorized preparation step;
8. freeze both thesis estimands, not only relearning:
   - transfer: `TR→EN(M2-A) - TR→EN(M1)` within seed;
   - factual re-exposure/relearning contribution: `TR→EN(M2-B) - TR→EN(M2-A)` within
     sibling seed;
   preserve `EN→EN` retention, `TR→TR` secondary and `EN→TR` exploratory roles;
9. expand the pre-execution review-field ledger beyond the current five placeholders. It must
   include exact item/ordered-ID hashes, exact prompt and choice templates, TurBLiMP pair scoring,
   TurkishMMLU/EXAMS selection rules, base-model compatibility, BOS/EOS/masking, context/window/
   stride/document-boundary rules, bootstrap resample count and CI method, split manifests and
   all required artifact/tokenizer identities;
10. correct threshold semantics:
    - use paired document-level NLL/BPB deltas for uncertainty; report PPL as the exponentiated
      within-model secondary summary rather than bootstrapping unstable raw document PPL means;
    - use **non-inferiority/no-harm** terminology for a one-sided TurBLiMP margin; do not call it
      equivalence unless a genuine two-sided equivalence procedure is frozen;
    - define separate directional English guardrails for LM loss/BPB/PPL deterioration and
      `EN→EN` accuracy deterioration; a single ambiguous `delta_EN_retention_margin` is not
      sufficient;
    - define how repeated looks across the 50M/250M/1B dose ladder are handled (fixed gate or
      multiplicity/alpha-spending rule) before any outcome is observed;
11. retain OLMo as an a-priori **provenance-first candidate**, Falcon as secondary English
    comparator and Qwen as multilingual positive control. Do not describe OLMo as proven
    English-dominant or any model as Turkish-unseen. Make clear that C2 `not_reported` exposure
    fields remain uncertainty/caveats and are not perpetual blockers if C1 identity/licence/
    runtime provenance and empirical Turkish headroom are established;
12. change 151ab's next request: it must **not** request baseline scoring yet. The next bounded
    stage should inventory exact existing model/tokenizer artifacts and evaluation inputs,
    reconcile C1 registry fields, and prepare/freeze the in-domain Turkish and English retention
    split manifests. Any missing model download/materialization or later scoring requires its own
    explicit contract and authorization;
13. update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this living handoff to
    the corrected 151ab authority, without rewriting 151aa or earlier result documents.

Hard exclusions for this task:

- no HU/SSH access or HU write;
- no network/public HTTP;
- no model/tokenizer or corpus download;
- no split/corpus materialization;
- no scoring, inference, evaluation, GPU/Slurm or training;
- no cleanup/deletion or previous-root mutation;
- do not create Documents 151ac/151ad or 152--154;
- do not claim `ready_to_train`, `ready_to_measure` or registry PASS.

The task completes only when corrected 151ab is internally consistent, its pre-correction and
final hashes are recorded, all authority summaries are aligned, and this living handoff contains
one bounded next authorization request for evidence/input preparation rather than measurement.

## Completed Current Task Override — Inventory the final evidence gaps and prepare the measurement-design authority

The explicitly authorized task is complete. No new current task is active. The HU portion was a
bounded read-only inspection of exactly the six listed repair-root files; no HU file or root was
written, and no network/public HTTP, download, scoring, inference, corpus materialization,
GPU/Slurm, training, cleanup/deletion or Documents 152–154 action occurred.

### Verified correction and gap inventory

The six files were verified against 151y/151z. The manifest has exactly 7 rows and lists the seven
prior artifacts; 151aa records the append-only correction to 151y’s “previous eight” narrative.
The matrix has exactly 150 rows, with `96 verified + 54 non-verified`; the non-verified split is
`30 not_reported`, `18 not_retrieved_in_this_wave` and `6 blocked`. 151aa enumerates every one of
the 54 rows and classifies them as:

1. provenance-required (`33` rows);
2. admissible-not-reported (`12` rows); and
3. future-measurement-produced (`9` rows).

The three benchmark and three model registry entities retain `BLOCKED` status, consistently with
the field matrix. The gap audit preserves 151x/151y/151z and their hashes unchanged.

### Measurement-design authority prepared

151ab is frozen but unexecuted. It distinguishes metadata uncertainty from measurement-produced
evidence and freezes the model roles (OLMo a-priori candidate, Falcon secondary comparator, Qwen
multilingual positive control), BPC/PPL/fertility, TurBLiMP/TurkishMMLU/EXAMS roles, held-out
Turkish evaluation, contamination policy, baseline/dose ladder, precommitted estimands,
review-required thresholds and seeds/stop rules. It does not authorize model or corpus downloads,
training, scoring, inference or readiness to train. The global gate remains
`blocked_by_measurement_design`; `ready_to_train=false`.

The exact six-file paths are retained in 151aa, and the current next authorization request is
defined below. Do not execute 151ab in this task.

## Completed Current Task Override — Execute corrected Document 151x once

The user-authorized execution of corrected frozen Document 151x completed exactly once. The
contract SHA-256 was verified as
`9c66cb95ee264d8411750d8e79aff258eaaeb4d1789ffc77bc8e162ffc93555b`. Mandatory HU preflight,
read-only reconciliation of the two immutable input roots, nine new repair-root outputs, the
150-row field matrix, 3×27 benchmark registry, 3×23 model registry, named output manifest and
last-written final audit all completed within scope.

The result is `BLOCKED`, not `PASS` or `CONDITIONAL`: required benchmark evidence and source-model
provenance remained incomplete. Primary blockers are `blocked_by_benchmark_registry` and
`blocked_by_source_model_provenance`; the global gate remains `blocked_by_measurement_design`.
Documents 151y and 151z are now created as the execution result and post-repair decision gate.
The repair root contains 9 files / 203,993 bytes with inventory SHA-256
`7bce6b0d70c8069595d9c8ca96801b2eca1faf5a31973b8741e176926ef26e82`. No prohibited operation
occurred, and both prior HU roots remain unchanged/read-only.

## Completed Current Task Override — Correct and re-freeze the 151x output-manifest protocol

The authorized local documentation-only correction is complete. Document 151x retains its
pre-correction SHA-256 `a19ed3b7e15540fa2810d5f483b2015cc5badd2bd41949d8678f945d3a6fb32e` in the append-only overlay and is now re-frozen with final
SHA-256 `9c66cb95ee264d8411750d8e79aff258eaaeb4d1789ffc77bc8e162ffc93555b`. The correction names `output_artifact_manifest.jsonl`, excludes that
manifest and the later `post_repair_storage_audit.json` from its own hash chain, freezes the
one-way seven-artifact → manifest → final-audit sequence, requires exactly 150 required
field-level coverage rows (`81 + 69`), excludes CETVEL/TurkBench from that required matrix and
freezes mandatory HU storage/path/inode preflight.

151x remains **FROZEN — UNEXECUTED**. No HU/SSH, network, repair-root creation, 151y/151z
creation, scoring, inference, weights/tokenizers, corpus, GPU/Slurm, training, cleanup or
Documents 152--154 action occurred.

## Completed Current Task Override — Validate the 151u/151v coverage-matrix PASS and prepare repair if needed

The authorized bounded validation is complete. HU inspection was strictly read-only over the
eight listed registry/reconciliation/report files and parent-root inventories. No HU file or root
was written. The first-wave root inventory was 91 files / 13,063,617 bytes and the retry root
inventory was 16 files / 38,315,850 bytes. Documents 151t, 151u and 151v were preserved
unchanged.

Document 151w records the validation. It found six retry entity-summary rows rather than frozen
per-entity-field rows; all 132 first-wave coverage rows lack `evidence_sha256s`,
`source_revisions` and `last_checked_utc`; first-wave benchmark registries omit four required
fields; retry benchmark registries omit 26 of 27 required fields; and retry model registries omit
all 23 required model fields. The three raw model-card rows are retained metadata artifacts, not
complete provenance rows. The scoped 151u/151v coverage PASS is therefore
`PROVISIONAL / UNSUPPORTED BY THE FROZEN COVERAGE RULE`.

Document 151w SHA-256:
`2b19bfbea496bb76efc4e06d24d815d2b83b06090e6ebdee6526773c5fb96de3`.

Because the discrepancy was confirmed, Document 151x was prepared and frozen as an unexecuted
minimal repair contract. It uses only immutable existing evidence, proposes the new repair root
`/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_coverage_repair_v1`, and forbids
network/public HTTP, scoring, inference, weights/tokenizers, corpus work, GPU/Slurm, training,
cleanup and Documents 152--154. 151x SHA-256:
`a19ed3b7e15540fa2810d5f483b2015cc5badd2bd41949d8678f945d3a6fb32e`.

The narrow current coverage gate is `BLOCKED` by `blocked_by_coverage_schema`, with contributing
`blocked_by_benchmark_registry`; the global gate remains `blocked_by_measurement_design` and
`ready_to_train=false`. Documents 151y/151z are reserved but uncreated.

## Previous Completed Task Override — Correct and re-freeze the 151t Hugging Face artifact routes

> **This is the only current task.** Do not execute 151t yet. Append one short correction to
> Document 151t, preserve its current hash as the pre-correction hash, re-freeze it and align the
> authority files. This is a local documentation-only correction.

### Material route issue

The three frozen Hugging Face model-card URLs in 151t use the browser presentation route
`/blob/<revision>/README.md`. That route can return the Hub HTML page rather than the immutable raw
README artifact whose bytes and SHA-256 must enter the scientific file/hash ledger. Hugging Face's
official download mechanism uses a revision-pinned file-download/resolve route. Executing the
current URLs risks hashing HTML and needlessly blocking all three source-model rows.

### Required append-only correction

Append a correction that preserves pre-correction 151t SHA-256
`eef968538b2022250803504ba1f206860c053663bb9ce74f761c3ae25c4c11cc` and overrides only the three
model-card URLs as follows:

```text
https://huggingface.co/allenai/OLMo-2-0425-1B/resolve/a1847dff35000b4271fa70afc5db10fd29fedbdf/README.md
https://huggingface.co/tiiuae/falcon-rw-1b/resolve/e4b9872bb803165eb22f0a867d4e6a64d34fce19/README.md
https://huggingface.co/Qwen/Qwen2.5-1.5B/resolve/8faed761d45a263340a0528343f099c05c9a4323/README.md
```

The correction must require the later execution to retain and hash the raw README bytes, record
the final response URL/redirect chain and content type, and fail closed if the response is an HTML
presentation page, login/error page, partial body or revision/path mismatch. It must not add any
model weights, tokenizer files, repository snapshots, fallback URLs or new scientific sources.
All other 151t sources, request order, bounds, new retry root, reconciliation rules, prohibitions
and reserved 151u/151v outputs remain unchanged.

After the append-only correction, recompute the final 151t SHA-256 and update AGENTS.md,
`ssh-client/README.md`, the documentation index and this living handoff. Keep 151t unexecuted.

### Authorization boundary for this task

Allowed: local inspection, append-only 151t route correction, SHA-256 and documentation alignment.

Forbidden: HU/SSH, network/downloads, retry execution, 151u/151v creation, scoring, inference,
weights/tokenizers, corpus work, GPU/Slurm, training, cleanup/deletion or Documents 152–154.

### Completion of this override

The append-only 151t route correction was added locally and re-frozen. The pre-correction SHA-256
was preserved, the effective model-card URLs are the three immutable `/resolve/` routes specified
in the addendum, and the final SHA-256 is recorded in the authority files. 151t remains unexecuted;
151q/151r/151s, the first execution root and the retry root were not modified or created. Documents
151u/151v were not created. No HU/SSH, network, retry, scoring, inference, model/tokenizer,
corpus, GPU/Slurm, training, cleanup or deletion occurred.

## Completed Current Task Override — Execute corrected 151t

> This task was explicitly authorized once and is now complete. Do not execute 151t again without
> a new explicit authorization and a separately reviewed scope.

Document 151t SHA-256 was verified before execution as
`63951ba5543c2c803e8466d0c43e0aace9637ca1239164dc1d9f5e49ea75f46b`. The mandatory HU
storage/path/inode preflight completed with home `du` at `14G`; the first execution root was
reconciled read-only at 91 files, 13,063,617 bytes and inventory SHA-256
`4e06a47ddf58a81a3d6a86e4dc0dee75c0d123ff6b38dbda6d76e7be51daf3ad`.

The single bounded retry wrote only under
`/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_retry_v1`. It used 7 HTTP
requests, 0 retries and 38,233,119 response bytes. The EXAMS artifact was retained exactly at
38,208,781 bytes with SHA-256
`96960451dd4c41f208ee5ab3bdbce8ea06ecd3506faa830e3ce61d316222c2f3`; all three corrected raw
model-card rows completed with request-level redirect/content-type evidence. The old root was
unchanged after the wave. Documents 151u and 151v are created as the execution result and gate.

The result is **PASS for the narrow registry-completion scope**. The global gate remains
`blocked_by_measurement_design`, training remains `BLOCKED`, and `ready_to_train` remains false.
No benchmark scoring, inference, model/tokenizer weights or snapshots, corpus materialization,
GPU/Slurm, training, cleanup/deletion, prior-root write or Documents 151k/151l/152--154 action
occurred.

## Previous Completed Task Override — Prepare a minimal 151q registry-completion retry contract

> **This is the only current task.** Prepare and freeze a local documentation-only retry contract;
> do not execute it. Preserve Documents 151q, 151r and 151s and the completed first execution root
> as immutable chronological evidence.

### Validation finding and retry rationale

The first execution behaved correctly under its frozen contract. The EXAMS
`data/exams/cross-lingual/with_paragraphs/test_with_para.jsonl.tar.gz` artifact is reported by the
official `mhardalov/exams` dataset metadata as 38,208,781 bytes (about 36.44 MiB), which is
4,654,349 bytes (about 4.44 MiB) above the frozen 32 MiB single-file limit. Therefore the
fail-closed outcome in Documents 151r/151s is valid, but it is a narrow frozen-bound problem—not
evidence that the official EXAMS route is unavailable and not a scientific model result.

### Required local preparation

Create the next unused numbered local contract (prefer Document 151t if the index confirms it is
free) for one minimal registry-completion retry. The contract must:

1. preserve 151q/151r/151s and their hashes without editing them;
2. keep the original execution root
   `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_v1` read-only;
3. freeze a new, explicit retry scratch root such as
   `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_retry_v1`;
4. retrieve only the missing EXAMS `test_with_para` artifact and the source-model public metadata
   requests that were skipped after the fail-closed stop; do not redownload already verified
   TurBLiMP, TurkishMMLU or retained EXAMS artifacts unless an integrity check fails;
5. raise the single retained-file/response allowance only enough for the verified 38,208,781-byte
   EXAMS artifact with explicit safety headroom (48 MiB is sufficient), while freezing bounded
   request, retry, total-response, retained-byte, file-count, wall-clock and storage limits;
6. require a composite read-only reconciliation against the first root, with new request/file/hash
   ledgers, complete benchmark/source-model registry rows, coverage matrix and explicit provenance
   links to reused first-wave artifacts;
7. fail closed on revision, path, byte/hash, duplicate-ID, manifest, coverage or bound mismatch;
8. reserve separate execution-result and post-retry gate documents (prefer 151u/151v if free);
9. keep benchmark scoring, inference, weights/tokenizers, corpus materialization, GPU/Slurm,
   training, cleanup/deletion, HU-home writes, prior-root writes and Documents 152–154 forbidden;
10. state that even a successful registry retry cannot close `blocked_by_measurement_design`, set
    `ready_to_train = true`, or authorize capability measurement/training.

During this preparation task, use local files only. Do not access HU/SSH or the network, do not
create the retry root, and do not run the retry. After freezing the contract, compute its SHA-256
and align AGENTS.md, `ssh-client/README.md`, the documentation index and this living handoff.

### Authorization boundary for this task

Allowed: local read-only validation, creation of the unexecuted retry contract, hashing and
documentation alignment.

Forbidden: HU/SSH, network/downloads, retry execution, result/gate document creation, scoring,
inference, model/tokenizer artifacts, corpus work, GPU/Slurm, training, cleanup/deletion or
Documents 152–154.

### Completion of this override

Document 151t was created locally, frozen without execution, and hashed. Documents 151q/151r/151s
and the first execution root were not changed; the new retry root was not created. Documents 151u
and 151v remain reserved and uncreated. No HU/SSH, network, retry, result/gate document creation,
scoring, inference, model/tokenizer, corpus, GPU/Slurm, training, cleanup or deletion occurred.

## Previous Completed Task Override — Fix the 151q execution-location authorization contradiction

> **Historical record only.** Do not execute 151q from this superseded local-correction task. The
> third operational
> clarification addendum, re-freeze the contract and align the authority files. No network or HU
> access is required for this documentation-only correction.

> Completion status: the third append-only operational clarification was added and the contract
> was re-frozen. This task used local files only; 151q, 151r/151s and all experimental operations
> remain unexecuted.

### Material contradiction

The contract requires all execution outputs under:

```text
/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_v1
```

but the final overlay says a later execution may not access HU, and the living handoff's current
authorization text also excludes HU/SSH. Those rules cannot both be satisfied. The phrase
“benchmark scoring beyond the frozen registry contract” is also ambiguous because 151q permits no
benchmark scoring at all.

### Required third addendum

Append an operational clarification to Document 151q that:

1. preserves the original, first-addendum and second-addendum hashes, including current SHA-256
   `c217f4d8395a8e3b657f96fd46f3e6443a11fcde6bbfbd6f7a8414933ccf89ee`;
2. states that the earlier no-HU statements describe the two preparation/correction passes only;
3. explicitly permits the later authorized 151q execution to use the documented `ssh-client`
   route to HU, perform the mandatory storage/path/inode preflight, create and write only the
   frozen new `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_completion_v1` root,
   retrieve only the frozen public benchmark/model metadata and allowlisted benchmark item files,
   and perform the mandatory post-run storage audit;
4. continues to prohibit writes to HU home, writes to prior evidence roots, Slurm/GPU, model or
   tokenizer weights/snapshots, inference, benchmark scoring, corpus materialization, training,
   cleanup/deletion and Documents 151k/151l/152–154;
5. states unambiguously that benchmark scoring is entirely forbidden—there is no “scoring within
   the registry contract” exception;
6. keeps 151r/151s reserved for the later execution result/gate and keeps 151q unexecuted;
7. recomputes a post-third-addendum SHA-256 and updates AGENTS.md, ssh-client/README.md, the
   documentation index and this handoff.

After this local correction, set 151q to `UNEXECUTED — EXECUTION_READY`. The next authorization
must explicitly include bounded HU/SSH access and creation/writes only under the frozen scratch
root, as well as public HTTP retrieval inside 151q's existing limits. It must not authorize Slurm,
GPU, scoring, inference or training.

### Authorization boundary for this task

Allowed: local read-only inspection, append-only third 151q addendum, SHA-256 and documentation
alignment.

Forbidden: HU/SSH, network/downloads, 151q execution, 151r/151s creation, benchmark/model/corpus
operations, scoring, inference, GPU/Slurm, training, cleanup/deletion or Documents
151k/151l/152–154.

## Previous Completed Task Override — Execute corrected Document 151q

> **This was the only current task.** One bounded execution of corrected 151q was explicitly
> authorized, including HU/SSH preflight, public HTTP within frozen limits, writes only under the
> new scratch root, post-run storage audit and creation of Documents 151r/151s. Scoring, inference,
> model/tokenizer weights, corpus materialization, GPU/Slurm, training, cleanup/deletion, HU home,
> prior roots and Documents 151k/151l/152--154 were excluded.

> Completion status: the wave ran once and fail-closed on the frozen 32 MiB single-response bound
> for the EXAMS `with_paragraphs/test_with_para.jsonl.tar.gz` route. Document 151r records the
> execution; Document 151s records the blocked gate. The effective operational gate is
> `blocked_by_operational_access`; the global gate remains `blocked_by_measurement_design` and
> `ready_to_train` remains false.

## Previous Completed Task Override — Finalize the 151q immutable benchmark overlay

> **Historical record only.** The immutable overlay was completed successfully; only the separate
> execution-location authorization contradiction above remains to be corrected.

> Completion status: the bounded public-metadata verification and second append-only correction
> were completed. The corrected contract is execution-ready but remains unexecuted; 151r/151s were
> not created.

### Independent review findings that must be resolved

The first 151q addendum incorrectly treated public GitHub revision/tree metadata as unresolved.
Direct read-only checks against the official repositories resolved all three mandatory benchmark
`main` tips and complete, non-truncated recursive trees:

```text
TurBLiMP     https://github.com/ezgibasar/TurBLiMP
             297de13fb7a0ce524fe32e8b175c6b5255d66960
TurkishMMLU  https://github.com/ArdaYueksel/TurkishMMLU
             0686a674064a151567ae05757e1f2414ca9d83d5
EXAMS        https://github.com/mhardalov/exams-qa
             f859e665de6c370f6214ca5f36a34ace36ada6cb
```

The first addendum also contains a material nonexistent EXAMS path:

```text
data/exams/cross-lingual/test_tr.jsonl.tar.gz   # INVALID / absent
```

The official immutable tree contains the shared test artifact instead:

```text
data/exams/cross-lingual/test.jsonl.tar.gz
```

Turkish test membership must later be selected by the repository's documented language/item rule,
not by inventing a `_tr` test filename. The with-paragraphs test artifact is likewise shared:
`data/exams/cross-lingual/with_paragraphs/test_with_para.jsonl.tar.gz`.

Finally, benchmark item content hashes are required outputs of the later 151q execution. They are
not preparation blockers once exact repository revisions and path allowlists are frozen.

### Exact overlay decisions to freeze

1. **TurBLiMP primary item set:** freeze the 16 CSV files under `data/base/` at commit
   `297de13...`; exclude `data/experimental/` and `data/human_judgments/` from the primary
   linguistic diagnostic. Freeze `evaluation.py` at the same commit as the repository evaluator.
   Record its Git blob SHA `c386def30cfdcbab4cd4366ef5805ab6ce4ae26a`.
2. **TurkishMMLU item set:** freeze the nine `dev/*.json` and nine `test/*.json` subject files at
   commit `0686a674...`; exclude `turkishmmlu_sub.json` from the primary full registry.
3. **TurkishMMLU evaluator:** the maintainer item repository currently has no evaluator file even
   though the paper describes public evaluation code. Freeze the author-contributed
   EleutherAI/lm-evaluation-harness implementation as the evaluator route and label this evidence
   relationship explicitly rather than calling it the maintainer-repository evaluator:

```text
repository = https://github.com/EleutherAI/lm-evaluation-harness
revision   = f4d4b3de3ee6741a7151a9fe74945ee515262f4c
path       = lm_eval/tasks/turkishmmlu/config/
subtree    = 35814d4b510da85f1bbd9f3d7293ab42bac200c8
provenance = task added/maintained by TurkishMMLU author ArdaYueksel
```

   Freeze the standard non-CoT `config/` subtree; keep `config_cot/` out of the primary evaluator
   identity. Preserve the caveat that this is an upstream harness implementation, not a file in
   the benchmark item repository.
4. **EXAMS:** freeze commit `f859e665...`, the correct Turkish train/dev plus shared-test paths,
   and `scripts/evaluation/evaluate_exams.py` with Git blob SHA
   `ef242a76cd076d7144f7ed36eadc3d9da6ca7a69`. Remove the nonexistent `_tr` test path from the
   effective overlay.
5. Preserve the already resolved OLMo/Falcon/Qwen immutable revisions and the precommitted
   CETVEL/TurkBench exclusion.
6. Reclassify exact item byte/SHA and ordered item-ID manifests as later 151q execution
   deliverables. They may block the execution result if retrieval/integrity fails, but their
   not-yet-computed state must not keep the corrected contract preparation-blocked.

### Bounded verification and authorization boundary

Allowed after explicit authorization: at most 16 public read-only GitHub/Hugging Face metadata
requests, at most 8 MiB total responses, `git ls-remote`, repository commit/tree/contents API
metadata, append-only second 151q addendum, local SHA-256, and alignment of AGENTS.md,
ssh-client/README.md, the documentation index and this handoff.

Forbidden: HU/SSH, benchmark item-content download, 151q execution, creation of 151r/151s,
benchmark scoring, inference, model/tokenizer artifacts or weights, corpus work, GPU/Slurm,
training, cleanup/deletion, Documents 151k/151l or 152–154.

### Completion rule

Preserve both earlier hashes:

```text
original body     b55499242100263e0d9adbe946679b6175268012d1c3e897298413a2af1ef60c
first addendum    0acf5251bea811e07b6442681ec02c7bc4fa2ea584a55e8b48cbcb704d4209e3
```

Record a new post-second-addendum SHA-256. If independent verification matches the identities
above and no new route conflict appears, set 151q to `UNEXECUTED — EXECUTION_READY` and make the
single next authorization a bounded execution of corrected 151q with creation of 151r/151s.

## Previous Completed Task Override — Repair and re-freeze Document 151q before execution

> **Historical record only.** The first route-resolution addendum was completed but left avoidable
> preparation blockers and one incorrect EXAMS path; the current task is the finalization above.

### Why repair is required

151q has good schemas, bounds and fail-closed rules, but its “frozen official routes” are generic
descriptions such as “maintainer's official repository.” The canonical URLs, repository/dataset
IDs, immutable commits/revisions and evaluator paths are deferred to execution preflight. That
leaves source selection to the executing agent and can repeat the paper-landing-page-only failure
that 151q is intended to prevent.

The contract also requires exact item counts/manifests/hashes while prohibiting a “full benchmark
dataset.” For small benchmarks, the official item files may themselves be the full benchmark; the
present wording can make the required evidence impossible to obtain even when it fits every byte
and file bound.

### Objective

Preserve the original unexecuted 151q and its SHA-256, resolve the exact official route/revision
identities needed for deterministic execution, append a visible correction addendum, and compute
a new post-addendum contract SHA-256. The corrected contract must leave no response-dependent
source or release selection to the later execution.

### Required route-resolution scope

Resolve and freeze, using primary sources only:

1. TurBLiMP: official maintainer repository/dataset identity, immutable dataset revision, exact
   item paths/splits and official evaluator/scoring-code path/revision.
2. TurkishMMLU: official `ArdaYueksel/TurkishMMLU` GitHub route and official dataset route,
   immutable revisions, exact item paths/splits and evaluator path/revision.
3. Turkish EXAMS: official `mhardalov/exams-qa` route, immutable revision, exact Turkish subset
   item paths/splits and evaluator path/revision.
4. OLMo, Falcon and Qwen: exact official Hugging Face repository IDs and immutable repository
   revisions plus the exact official model-card/paper/corpus-documentation routes that the later
   registry execution may read.
5. CETVEL/TurkBench: either freeze an exact relevant task and official route now or precommit a
   reasoned exclusion. Do not leave task selection response-dependent.

### Required contract corrections

- Replace every generic source placeholder with an allowlisted canonical URL/repository ID and an
  immutable revision/commit or an explicit precommitted exclusion.
- Freeze the exact file/path allowlist and evaluator-code paths where discoverable.
- State that bounded official benchmark item files may be retrieved in full when each file and
  the combined set remain inside the existing 32-MiB/128-MiB limits; this is metadata-registry
  materialization for hashing, not benchmark scoring or corpus download.
- Continue to prohibit archives requiring broad extraction, corpora, model/tokenizer snapshots,
  model weights and unrelated repository checkout.
- Ensure the later 151q execution only retrieves/verifies the already selected revisions; it must
  not choose among releases, repositories or tasks.
- Record unresolved official revision/evaluator identity as a contract-preparation blocker rather
  than freezing a knowingly incomplete execution contract.
- Preserve the 96-request execution bound. The route-resolution repair itself is separately
  bounded below and its requests must not be confused with the later 151q execution ledger.

### Repair-pass bounds and authorization boundary

Allowed only after explicit authorization:

```text
max_public_http_requests = 32
max_total_retries = 8
max_total_response_bytes = 16777216
max_single_response_bytes = 4194304
max_wall_clock_seconds = 1200
```

Allowed: public read-only HTTP/API metadata for the named sources, small repository tree/commit/
release metadata, local documentation edits, append-only 151q correction, SHA-256, and alignment
of AGENTS.md, ssh-client/README.md, the documentation index and this handoff.

Forbidden: HU/SSH, 151q registry execution, creation of 151r/151s, benchmark item-content
collection in this repair pass, benchmark scoring, inference, model/tokenizer snapshot or weight
access, corpus work, GPU/Slurm, training, cleanup/deletion, Documents 151k/151l or 152–154.

### Completion

Keep the original 151q SHA-256
`b55499242100263e0d9adbe946679b6175268012d1c3e897298413a2af1ef60c` in the addendum and index as
the pre-correction hash. Record the final post-addendum hash separately. Only if every mandatory
route/revision decision is deterministic may the next authorization request be execution of the
corrected 151q with creation of 151r/151s. Otherwise stop with the exact unresolved preparation
blocker; do not authorize execution.

## Previous Completed Task Override — Prepare the bounded benchmark/model metadata registry contract

> **Historical record only.** The initial unexecuted 151q was prepared, but independent review
> found that it requires the execution-readiness correction above before it can be run.

### Objective

Create one small, execution-ready, metadata-only contract for completing the benchmark and
source-model registries that Document 151p correctly classifies as incomplete collection or
extraction. The contract must prevent another paper-landing-page-only pass and must keep registry
completion separate from benchmark scoring, model inference, overlap-policy design and Turkish
capability measurement.

### Required output

Create:

```text
documentation/151q_BENCHMARK_AND_SOURCE_MODEL_METADATA_REGISTRY_COMPLETION_CONTRACT_TR.md
```

Document 151q must remain `UNEXECUTED` in this task. Reserve, but do not create, one append-only
execution-result document and one post-execution gate document using the next available suffixes.
Do not reuse Documents 151k/151l, which remain reserved under the superseded 151j path.

### Contract requirements

1. Freeze an explicit primary-source route for each candidate benchmark: official dataset or
   repository, immutable revision/commit, exact task/subset/split, item manifest and hash, license,
   evaluator/scoring implementation and version. Paper landing pages may support interpretation
   but cannot by themselves satisfy the registry.
2. Cover TurBLiMP, TurkishMMLU and Turkish EXAMS. Include CETVEL or a TurkBench task only when its
   exact task relevance and official item/evaluator source can be demonstrated; otherwise record
   a reasoned exclusion.
3. Freeze exact source-model IDs and roles for OLMo, Falcon and Qwen. Require official model-card,
   repository, paper and corpus-documentation extraction for base-versus-instruction stage,
   revision, tokenizer revision, license, documented language/corpus mixture and Turkish evidence.
   Missing documentation must never be converted into a zero-Turkish-exposure claim.
4. Define a field-level status vocabulary such as `verified`, `not_reported`, `not_applicable`,
   `access_blocked`, `conflicting_primary_sources` and `excluded_with_reason`. Do not use a generic
   `NR` where these cases can be distinguished.
5. Require request and file manifests, retrieval timestamps, source URL/revision, HTTP status,
   byte counts and SHA-256 for every retained response/item file. Require a coverage matrix that
   reports every mandatory field, not only successful rows.
6. Set bounded request, byte, wall-clock, file-count and scratch limits. Permit only small public
   metadata and benchmark item/evaluator files needed to establish revisions and hashes. Prohibit
   model weights, tokenizer/model snapshots, full corpora and unrelated broad crawling.
7. Freeze fail-closed logic: a paper abstract alone is incomplete; an unresolved official
   repository must produce a field-level blocker; conflicting sources must be preserved and not
   silently reconciled.
8. Make clear that successful execution can close only the two metadata-registry components. It
   cannot close future alias/template/overlap definitions, Turkish capability measurement, the
   global `blocked_by_measurement_design` gate or the training gate.

### Local validation note to preserve

Document 151p's 65,717 total and all set hashes are verified. Its statement that 20,000 unique
training strings are attributed to four acquisition files reflects post-dedup provenance: the
builder traverses six declared train/validation sources, while the nested 10-subject rows are
duplicates already attributed to the lexicographically earlier 100-subject files. This is a
documentation clarification only and does not change any count or gate.

### Authorization boundary

Allowed: local read-only inspection, creation of unexecuted Document 151q, local SHA-256, and
authority/index/handoff alignment.

Forbidden: HU/SSH, web/network/API access, downloads, contract execution, benchmark scoring,
model/tokenizer inference or weight access, corpus scanning, GPU/Slurm, training, cleanup/deletion,
Documents 151k/151l or 152–154.

After preparation, record the final 151q SHA-256 and update `AGENTS.md`, `ssh-client/README.md`,
the documentation index and this handoff. The only next authorization request must be one bounded
execution of the frozen 151q contract; it must not authorize any later measurement or training.

## Previous Completed Task Override — Correct the Phase-1 scientific blocker classification

> **Historical record only.** Document 151p has been completed and must not be recreated. The
> current task is the unexecuted metadata-contract preparation above.

### Objective

Independently reproduce the local evidence summarized above, preserve Documents 151n/151o
unchanged, and create an append-only correction report that distinguishes genuine unresolved
measurement evidence from evidence that the Phase-1 worker failed to extract.

### Mandatory validation

1. Read `AGENTS.md`, this handoff, Documents 151m/151n/151o, the Relation V2 release README and
   manifest, the contamination implementation, the provenance-refresh Slurm assertions, and the
   relevant dataset tests.
2. Verify all cited SHA-256 values locally.
3. Recompute the two exact EN/TR surface unions from:
   - `syntheticFacts/data/canonical_subject_profiles_5000.csv` (legacy/source profile);
   - `syntheticFacts/output/relation_v2/data/canonical_subject_profiles_5000.csv` (derived
     Relation V2 profile).
4. Re-run `build_contamination_inventory` read-only against
   `syntheticFacts/output/relation_v2` and verify the exact 65,717 breakdown. Do not regenerate or
   alter the dataset.
5. Inspect the Phase-1 request ledger and registries. Explicitly distinguish “not retrieved in
   this bounded run” from “not publicly available” or “provenance cannot be resolved.”

### Required output

Create one new chronological report:

```text
documentation/151p_PHASE1_INDEPENDENT_LOCAL_VALIDATION_AND_BLOCKER_CORRECTION_TR.md
```

The report must:

- preserve 151n/151o and explain why their initial 95,000-fact/10,718-surface output is invalid;
- close `blocked_by_synthetic_inventory_provenance` using the frozen source/derived manifest
  chain;
- close the exact `65,717` inventory-reproduction component of
  `blocked_by_contamination_definition`, while keeping any genuinely undefined future overlap
  tiers or alias rules separate;
- reclassify the benchmark registry as incomplete collection/extraction from the executed wave,
  not evidence of public-source absence;
- reclassify source-model provenance similarly wherever the downloaded card/API evidence was not
  fully parsed;
- state that the global gate remains `blocked_by_measurement_design` and that no training is
  authorized;
- define the smallest remaining evidence follow-up, but not execute it and not create its
  execution contract in the same turn.

After creating 151p, update `AGENTS.md`, `ssh-client/README.md`, the documentation index and this
living handoff. Compute and record the final 151p SHA-256.

### Authorization boundary

Allowed: local file reads, deterministic local calculations, append-only creation of 151p, and
authority/index/handoff alignment.

Forbidden: HU/SSH, web/network/API access, downloads, model inference, benchmark scoring, corpus
scanning, GPU/Slurm, training, cleanup/deletion, rewriting 151n/151o, or Documents 151k/151l and
152–154.

## Previous Completed Task Override — Phase-1 Evidence-Resolution Contract (Do Not Execute)

> **Historical record only.** Document 151m was already executed once and must not be rerun. The
> current task is the correction override above; no instruction in this completed section grants
> present authority for HU access, network work, training, inference, scoring, full-corpus work or
> Documents 151k/151l/152–154.

The numbered work items below are the completed local-preparation record. Any sentence in that
historical record saying that 151m must remain unexecuted was superseded by the later explicit
151m authorization and is not current authority; the verified current state is recorded above
and in Documents 151n/151o.

### Objective

Correct the over-broad Document 151j state without hiding it, and prepare one small, genuinely
executable Phase-1 evidence-resolution contract. This turn remains local and documentation-only.

### Why this correction is required

Document 151j is not execution-ready because:

1. it requires revisions, hashes, inventories, thresholds, and measurement definitions to be
   frozen before execution even though its proposed execution is supposed to resolve them;
2. it combines metadata research, inventory reconstruction, contamination analysis, model
   provenance, BPC, benchmarks, and future M2-A/M2-B evaluation in one wave;
3. the future-design M2-A/M2-B models do not yet exist;
4. exact BPC corpus/split/evaluator/windowing and thresholds are not frozen;
5. its requested authorization excludes the result/gate documents needed to close the work.

Therefore 151j must be preserved as an unexecuted requirements draft, not executed as a frozen
measurement contract.

### Mandatory reading

Read completely:

- `AGENTS.md`
- `ssh-client/README.md`
- `documentation/00_DOCUMENTATION_INDEX.md`
- `documentation/144_SUPERVISOR_FEEDBACK_AND_SCIENTIFIC_REALIGNMENT_TR.md`
- `documentation/145_LITERATURE_FIRST_M1_MODEL_AND_TURKISH_ADAPTATION_ROUTE_TR.md`
- `documentation/148_CROSS_LINGUAL_LANGUAGE_ADAPTATION_LITERATURE_MATRIX_TR.md`
- `documentation/150_TURKISH_CAPABILITY_AND_ADAPTATION_MANIPULATION_CHECK_PLAN_TR.md`
- `documentation/151f_BOUNDED_AUDIT_INDEPENDENT_VALIDATION_AND_CORRECTION_REPORT_TR.md`
- `documentation/151h_BOUNDED_AUDIT_REPAIR_WAVE_EXECUTION_RESULT_TR.md`
- `documentation/151i_BOUNDED_AUDIT_POST_REPAIR_DECISION_GATE_TR.md`
- `documentation/151j_MEASUREMENT_DESIGN_CORRECTION_CONTRACT_TR.md`
- `documentation/151p_PHASE1_INDEPENDENT_LOCAL_VALIDATION_AND_BLOCKER_CORRECTION_TR.md`
- this living handoff in full.

### Authorization boundary for this task

Allowed:

- local read-only inspection;
- append-only documentation correction;
- creation of one new proposed Phase-1 contract;
- local SHA-256 computation;
- authority/index alignment.

Not allowed:

- HU/SSH;
- web/network/API access;
- dataset or model downloads;
- benchmark execution;
- model inference, BPC, PPL, TurBLiMP, MMLU, EXAMS, or factual evaluation;
- synthetic inventory materialization;
- contamination scanning;
- GPU/Slurm;
- training/fine-tuning;
- cleanup/deletion/migration;
- Documents 151k/151l or 152–154.

### Work item 1 — Preserve and reclassify Document 151j

Do not silently rewrite or delete Document 151j. Append a visible correction note stating:

```text
status = SUPERSEDED_UNEXECUTED_REQUIREMENTS_DRAFT
execution_authorized = false
scientific_results_produced = false
```

The note must explain the five execution-readiness problems listed above. State that its schemas
and conceptual distinctions remain useful requirements, but its single-wave execution request is
withdrawn. Documents 151k/151l remain uncreated and are not authorized from 151j.

Do not alter the original 151j body or hide the original SHA. Compute and report the post-addendum
SHA separately from the original historical SHA.

### Work item 2 — Create the bounded Phase-1 contract

Create:

```text
documentation/151m_PHASE1_MEASUREMENT_EVIDENCE_RESOLUTION_CONTRACT_TR.md
```

In the original local-preparation turn, Document 151m had to remain **UNEXECUTED**. That
historical state was superseded by the later explicit execution authorization; the contract still
covers only evidence that can be
resolved without model inference, GPU work, training, or future M2-A/M2-B models.

#### Phase-1 scope

1. **Benchmark registry metadata**
   - Resolve exact revisions, splits, item manifests/hashes, licenses, evaluator revisions, and
     base-model compatibility for TurBLiMP, TurkishMMLU, Turkish EXAMS, and only demonstrably
     relevant CETVEL/TurkBench tasks.
   - This phase may later acquire only bounded public metadata and small benchmark item files;
     it must not run benchmark scoring.
   - Candidates may be included, excluded, conditional, or unresolved.

2. **713/829 exact-set reconciliation**
   - Use only the frozen Relation V2 release, existing local/HU source files, and deterministic
     set extraction.
   - Produce exact membership files, counts, hashes, intersections, and `713-only`/`829-only`
     differences where reproducible.
   - Never alter the fact population to recover a historical number.

3. **Pattern/alias/template inventory provenance**
   - Determine whether the declared 65,717 pattern inventory and required alias/template sets are
     reproducible from frozen code/data.
   - Produce deterministic manifests/hashes only in a later authorized execution.
   - Fuzzy matching rules may be specified, but no fuzzy candidate is automatically
     contamination.

4. **Contamination measurement freeze**
   - Freeze Tier 0 exact sentence, Tier 1 subject+canonical object, Tier 2 subject+alias, Tier 3
     template/full-fact, and Tier 4 adjudicated fuzzy-candidate definitions.
   - Object-only and subject-only remain diagnostics.
   - A later Phase-1 execution may run the frozen tiers only on already bounded authorized
     samples; no full-corpus scan or materialization.

5. **Source-model provenance metadata**
   - Resolve exact base/instruction stage, revision, license, corpus/language claims, and Turkish
     evidence for OLMo, Falcon, and Qwen using primary-source metadata.
   - No model weights, inference, or capability scoring.
   - Never equate missing Turkish documentation with zero Turkish exposure.

#### Explicitly outside Phase 1

- BPC/bits-per-byte execution;
- Turkish or English PPL;
- TurBLiMP/MMLU/EXAMS scoring;
- model/tokenizer inference requiring weight access;
- M0/M1 capability comparison;
- M2-A/M2-B construction, training, or measurement;
- checkpoint selection;
- final model/corpus selection;
- training readiness.

Phase 1 may only produce evidence needed to prepare a later baseline-measurement contract.

### Required 151m execution bounds

Document 151m must freeze, before any future authorization:

- exact allowed sources and immutable revisions already known;
- exact new scratch root, without creating it;
- total network request, download-byte, wall-clock, file-count, and storage limits;
- allowed public metadata/item-file downloads and explicit prohibited downloads;
- deterministic seeds/orderings;
- per-work-package manifests and SHA requirements;
- raw-text/PII protection;
- fail-closed rules;
- preflight and post-run audit;
- no overwrite of existing audit/repair roots;
- no model weights, GPU, Slurm, training, or full-corpus operations.

Use a proposed future root such as:

```text
/vol/tmp2/yesildau/luna_phase1_measurement_evidence_resolution_v1
```

Do not create it in this local task.

### Phase-1 result and gate records

Reserve but do not create:

```text
Document 151n — Phase-1 measurement evidence-resolution execution result
Document 151o — Phase-1 post-execution decision gate
```

The later execution authorization for 151m must explicitly authorize creation of 151n/151o so
the work can be documented atomically. It must not authorize 151k/151l or 152–154.

### Phase-1 decision vocabulary

At minimum freeze:

```text
ready_to_freeze_baseline_measurement_contract
blocked_by_benchmark_registry
blocked_by_synthetic_inventory_provenance
blocked_by_contamination_definition
blocked_by_source_model_provenance
blocked_by_operational_access
```

`ready_to_freeze_baseline_measurement_contract` means only that a later BPC/benchmark/capability
contract may be drafted. It never means ready to train.

### Documentation updates

After creating 151m:

- update `documentation/00_DOCUMENTATION_INDEX.md`;
- update `AGENTS.md` current authority;
- update `ssh-client/README.md` current-state section;
- preserve 151d–151i unchanged;
- modify 151j only through the append-only correction note;
- historically identify 151m as the unexecuted Phase-1 contract; this was superseded after the
  explicit execution authorization and is now recorded by 151n/151o;
- keep the measurement-design and global training gates blocked.

### Completion and freeze

After local documentation edits:

1. compute and report the final post-addendum SHA-256 of 151j;
2. compute and report the final SHA-256 of 151m;
3. record both in the documentation index with their exact status;
4. in the original local-preparation turn, do not execute 151m;
5. in the original local-preparation turn, do not create 151n/151o;
6. update this living handoff according to the Usage and Update Rules;
7. report every changed file and every remaining blocker.

The only allowed next authorization request in the original preparation turn was the request
below; it was fulfilled by the later explicit user authorization and is no longer current:

> User explicitly authorizes one bounded, source-read-only, non-destructive execution of the
> frozen Document 151m Phase-1 measurement evidence-resolution contract, including creation of
> Documents 151n and 151o, but excluding model inference, GPU/Slurm, training, full-corpus
> operations, cleanup, and Documents 151k/151l or 152–154.

## Previous Completed Task Definition (Historical; Do Not Execute)

### Task title

Prepare the local, documentation-only measurement-design correction contract.

### Authorization boundary

This task is local and documentation-only. It does **not** authorize:

- HU/SSH access;
- external dataset API calls;
- web/download work not already evidenced locally;
- Slurm or GPU work;
- model or dataset downloads;
- benchmark execution;
- corpus or synthetic-inventory materialization;
- training or evaluation;
- cleanup, deletion, migration, or remote artifact mutation;
- Documents 152–154.

The task may create the proposed Document 151j and update local authority documentation as defined
below. Document 151j must remain unexecuted.

## Mandatory Reading

Read completely before editing:

- `AGENTS.md`
- `ssh-client/README.md`
- `documentation/00_DOCUMENTATION_INDEX.md`
- `documentation/100_MASTER_PROJECT_STATUS_AND_EXECUTION_PLAN.md`
- `documentation/138_QWEN_M2_M3_COMPLETED_MILESTONE_AND_SCIENTIFIC_INTERPRETATION_EN.md`
- `documentation/139_POST_M2_M3_INDEPENDENT_NEXT_ACTION_PLAN_EN.md`
- `documentation/144_SUPERVISOR_FEEDBACK_AND_SCIENTIFIC_REALIGNMENT_TR.md`
- `documentation/145_LITERATURE_FIRST_M1_MODEL_AND_TURKISH_ADAPTATION_ROUTE_TR.md`
- `documentation/146_LUNA_WORKER_2_DETAILED_RESEARCH_AND_AUDIT_HANDOFF_TR.md`
- `documentation/147_PRETRAINING_MODEL_TURKISH_PROVENANCE_AUDIT_TR.md`
- `documentation/148_CROSS_LINGUAL_LANGUAGE_ADAPTATION_LITERATURE_MATRIX_TR.md`
- `documentation/149_TURKISH_CORPUS_PROVENANCE_QUALITY_AND_CONTAMINATION_AUDIT_TR.md`
- `documentation/150_TURKISH_CAPABILITY_AND_ADAPTATION_MANIPULATION_CHECK_PLAN_TR.md`
- `documentation/151_PRETRAINING_MODEL_CORPUS_AND_MEASUREMENT_DECISION_GATE_TR.md`
- `documentation/151a_BOUNDED_MODEL_AND_CORPUS_PROVENANCE_SAMPLE_AUDIT_CONTRACT_TR.md`
- `documentation/151f_BOUNDED_AUDIT_INDEPENDENT_VALIDATION_AND_CORRECTION_REPORT_TR.md`
- `documentation/151g_BOUNDED_AUDIT_REPAIR_CONTRACT_TR.md`
- `documentation/151h_BOUNDED_AUDIT_REPAIR_WAVE_EXECUTION_RESULT_TR.md`
- `documentation/151i_BOUNDED_AUDIT_POST_REPAIR_DECISION_GATE_TR.md`

Preserve the current authority:

```text
vngrs operational/sample-manifest gate = PASS / closed
CulturaX = excluded_access_blocked
measurement-design gate = blocked_by_measurement_design
global training gate = BLOCKED
Documents 152–154 = unauthorized and uncreated
```

## Work Item 1 — Append-Only Clarification to Document 151h

Append a clearly labelled clarification. Do not modify the original numerical results.

Record why the initial manifest was superseded:

- initial `source_compressed_bytes` was `null` instead of explicit `NR`;
- the API page identifier was initially represented as if it were a genuine source shard;
- the rows API does not expose a genuine source shard;
- the corrected authoritative manifest uses `source_compressed_bytes = "NR"`, `shard = "NR"`,
  and an explicit reason;
- the initial manifest remains preserved and non-authoritative.

Explicitly record the bounded near-dedup scope breakdown:

```text
vngrs internal MinHash-estimated J>=0.80 pairs: 156
trwiki internal MinHash-estimated J>=0.80 pairs: 3,201
vngrs–trwiki cross-source MinHash-estimated J>=0.80 pairs: 0

vngrs internal exact normalized-text duplicate pairs: 14
trwiki internal exact normalized-text duplicate pairs: 563
vngrs–trwiki cross-source exact normalized-text duplicate pairs: 0
```

State that cross-source zero is a bounded-sample result, not proof of zero overlap in the complete
corpora.

Clarify that no full corpus was downloaded, but one explicitly authorized bounded 10,000-record
vngrs sample was acquired.

Do not change the 151h scientific verdict.

## Work Item 2 — Create Document 151j

Create:

```text
documentation/151j_MEASUREMENT_DESIGN_CORRECTION_CONTRACT_TR.md
```

Document 151j is an immutable proposed execution contract and must remain **UNEXECUTED** in this
task. It must freeze exact definitions, inputs, outputs, thresholds, evidence schemas, failure
rules, decision logic, and authorization boundaries for:

1. benchmark registry;
2. `713/829` surface-set reconciliation;
3. pattern/alias/fuzzy inventory;
4. corpus contamination measurement;
5. source-model Turkish provenance;
6. Turkish capability and language-adaptation manipulation checks.

Positive, negative, and inconclusive results must be equally reportable. Do not optimize the
contract for a positive outcome.

## WP-A — Benchmark Registry

Candidate benchmarks:

- TurBLiMP;
- TurkishMMLU;
- Turkish EXAMS;
- CETVEL and/or a demonstrably relevant TurkBench task.

Do not assume every candidate is suitable. For each benchmark require:

- canonical benchmark name, task, and subset;
- primary/secondary role and scientific purpose;
- immutable repository/dataset revision;
- exact split and item count;
- item-set path and SHA-256;
- license/access evidence;
- scoring/evaluator source revision and code SHA-256;
- normalization/scoring rule;
- chance baseline where applicable;
- floor/ceiling diagnostic;
- benchmark–corpus overlap procedure;
- retrieval timestamp;
- known limitations.

Freeze intended roles:

- TurBLiMP: preferred independent linguistic diagnostic, subject to exact provenance and evaluator
  verification;
- TurkishMMLU/EXAMS: broad knowledge/reasoning diagnostics, not pure language-adaptation checks;
- CETVEL/TurkBench: include only if exact task, revision, scoring, and relevance are resolved.

Never invent revisions, item counts, or hashes. An item set cannot be called clean before the exact
registry and overlap audit exist.

## WP-B — `713/829` Surface Reconciliation

Freeze counting units:

```text
subject_count = 5,000
semantic_fact_id = subject_id | relation
semantic_fact_count = 25,000
bilingual_resolved_row = semantic_fact_id | language
bilingual_resolved_row_count = 50,000
relations = profession, birthplace, residence, university, employer
```

Keep semantic facts, English/Turkish canonical surfaces, union surfaces, aliases, templates,
patterns, and training sentences separate.

The future procedure must:

1. use the frozen Relation V2 release commit and manifest;
2. identify the exact source and definition of the declared 713 count;
3. reconstruct the 713 membership only when supported by frozen code/data;
4. produce a deterministic sorted membership file and SHA-256;
5. independently reproduce the profile-derived 829 union and SHA-256;
6. report intersection, `713-only`, `829-only`, and normalization differences;
7. distinguish language expansion, normalization, alias inclusion, and genuine inventory changes;
8. never alter the semantic fact population to force reconciliation.

If 713 cannot be reproduced, use:

```text
blocked_by_synthetic_inventory_provenance
```

Do not silently replace 713 with 829.

## WP-C — Pattern, Alias, and Fuzzy Inventory

Define immutable future files for:

- subjects;
- semantic facts;
- canonical EN/TR surfaces;
- aliases by language;
- exact training sentences;
- template/pattern inventory;
- normalized matching surfaces;
- fuzzy candidates.

Records must include, as applicable: fact/subject ID, relation, language, category, original and
normalized surfaces, source file, row/template ID, release commit, deterministic record ID, and
membership-file SHA.

Freeze separate normalization for exact sentences, subject/object matches, aliases, and fuzzy
candidates. Fuzzy matching may generate candidates only; it cannot automatically decide
contamination.

The declared 65,717 patterns must be reproduced from frozen evidence or explicitly classified as
unreproducible. Do not tune generation to recover the historical number.

## WP-D — Contamination Definitions

Freeze at least:

```text
Tier 0: exact normalized declared training-sentence match
Tier 1: exact subject + canonical object in one document
Tier 2: exact subject + alias/object surface in one document
Tier 3: template/full-fact-pattern match
Tier 4: fuzzy candidate requiring deterministic adjudication
Object-only: diagnostic only
Subject-only: diagnostic only
```

For each tier define normalization, unit, boundary, language and alias handling, false-positive
risk, output schema, aggregate report, adjudication, and pass/conditional/blocked rule.

Do not treat common object strings as factual contamination. Do not claim zero contamination when
required inventories are absent. Keep benchmark overlap separate from synthetic-fact
contamination.

Privacy rules:

- no raw PII in documentation;
- stakeholder reports contain aggregates only;
- raw candidate snippets remain on bounded scratch;
- documentation uses IDs/hashes whenever possible.

## WP-E — Source-Model Turkish Provenance

Model roles remain:

- OLMo: a priori preferred English-dominant candidate;
- Falcon: secondary comparator;
- Qwen: multilingual/Turkish positive control.

Do not call OLMo or Falcon `Turkish unseen` without primary-source evidence.

Require exact model ID/revision, base versus instruction status, training stage, tokenizer,
model-card language claims, corpus documentation, known language mixture, explicit or missing
Turkish evidence, license, runtime compatibility, and experimental role.

Use conservative labels such as:

```text
documented_multilingual_with_Turkish
documented_English_dominant_Turkish_fraction_unreported
Turkish_exposure_not_resolvable
not_suitable_training_stage
```

Absence of documented Turkish data is not proof of zero Turkish exposure.

## WP-F — Turkish Capability Measurement

Keep these dimensions separate:

1. Turkish language modelling;
2. Turkish linguistic competence;
3. broad Turkish knowledge/reasoning;
4. tokenizer efficiency;
5. English retention;
6. synthetic factual retrieval.

Do not combine them into one Turkish-capability score.

Define applicable measurements at:

```text
source pretrained model
post-M1 sibling checkpoint
M2-A endpoint
M2-B endpoint
```

M2-A/M2-B must start from the same sibling M1 checkpoint and match adaptation seed, token/update
budget, document order, optimizer, and checkpoint schedule. Only controlled factual replacement
may differ.

### Primary manipulation check

Use UTF-8 bits-per-byte/BPC on a frozen held-out general Turkish corpus:

```text
total_negative_log_likelihood_bits / total_UTF8_bytes
```

Freeze BOS/EOS, truncation, sliding windows, byte denominator, document boundaries, aggregation,
and confidence intervals.

### Secondary language modelling

Within-model Turkish PPL may compare stages only within the same tokenizer chain. Raw PPL must not
rank models across tokenizers.

### Independent linguistic diagnostic

TurBLiMP is preferred only after revision, item set, scoring, and overlap evidence are frozen.

### Broad diagnostics

TurkishMMLU/EXAMS and any accepted CETVEL/TurkBench task are secondary. No improvement on broad
knowledge tasks does not automatically mean language adaptation failed.

### Tokenizer diagnostics

Record tokens/word, tokens/character, UTF-8 bytes/token, unknown/special/byte-fallback behaviour,
and diacritic probes. Fertility is not language capability.

### English retention

Freeze a compatible English held-out evaluation and a pre-outcome retention rule. Do not select
the threshold after observing M2-A/M2-B.

### Factual channels

Keep separate:

```text
EN prompt -> EN answer
TR prompt -> EN answer
TR prompt -> TR answer
EN prompt -> TR answer  [secondary lexicalization/control only]
```

`TR->EN` measures cross-lingual factual access. `TR->TR` combines access and Turkish answer
lexicalization. `EN->TR` is not the primary expected behaviour. Do not merge these into one
bilingual metric. Freeze EN/TR answer aliases before evaluation.

## Thresholds, Floor/Ceiling, and Statistics

Do not invent arbitrary thresholds. Label each threshold as literature-derived, inherited from a
frozen gate, requiring bounded pre-outcome calibration, or descriptive-only.

Calibration must not inspect future M2-A/M2-B outcomes.

Freeze:

- unit of analysis;
- pairing keys;
- seed handling;
- confidence intervals;
- multiple-comparison treatment;
- missing/failed item policy;
- macro/micro aggregation;
- primary/secondary endpoints.

Future sibling comparisons must pair the same M1 seed, adaptation seed, evaluation item, and fixed
endpoint/update. Never select separate post-treatment checkpoints using Turkish factual outcomes.

## Decision Vocabulary

Define at least:

```text
ready_to_execute_bounded_measurement_audit
blocked_by_benchmark_registry
blocked_by_synthetic_inventory_provenance
blocked_by_contamination_definition
blocked_by_capability_measurement_design
blocked_by_source_model_provenance
blocked_by_operational_access
```

Successful correction never means `ready_to_train`; it can only make a later bounded measurement
audit separately authorizable.

## Future Outputs and Storage

Reserve but do not create:

```text
Document 151k — measurement-design correction execution result
Document 151l — post-measurement-design decision gate
```

Define but do not create:

```text
/vol/tmp2/yesildau/luna_measurement_design_correction_v1
```

Future folders may include `contracts/`, `registries/`, `inventories/`, `manifests/`, `overlap/`,
`capability/`, `reports/`, `logs/`, and `tmp/`. Existing audit/repair roots and frozen artifacts
remain immutable.

## Documentation Authority Updates

After creating 151j:

- update `documentation/00_DOCUMENTATION_INDEX.md`;
- in the historical preparation turn, update `AGENTS.md` because 151j becomes the current
  unexecuted measurement-design contract;
- update the current-state section of `ssh-client/README.md`;
- preserve 151d–151i as chronological evidence;
- modify 151h only through the requested append-only clarification;
- modify Document 100 only if needed to eliminate a direct current-status contradiction.

Authority must remain:

```text
vngrs operational gate = closed
CulturaX comparative selection = unavailable
measurement-design gate = blocked pending execution of 151j
training gate = blocked
Documents 152–154 = unauthorized
```

## Freeze and Completion Protocol

After local edits:

1. compute final SHA-256 of Document 151j;
2. record it in the documentation index;
3. report it in the final response;
4. do not execute 151j;
5. do not access HU;
6. do not create 151k/151l;
7. do not run benchmark, contamination, capability, evaluation, or training work;
8. update this living handoff file according to **Usage and Update Rules**.

In that historical local task, the only allowed next authorization request was:

> User explicitly authorizes one bounded, source-read-only, non-destructive execution of the
> frozen Document 151j measurement-design correction contract.

That later authorization still does not authorize training, Slurm/GPU work, full model/corpus
downloads, cleanup, or Documents 152–154.

## Last Worker Completion Summary

The latest local-only correction is complete. Document 151ax retains pre-clarification SHA-256
`15bdc5a7ae0e0356254c5d5ffd5ad47b091f459a52689ce4c0cb1ecc9699ed22` and is frozen-unexecuted at
current final SHA-256 `b32550966e29f3398239e7be778cb20e3344e427bbec6f664fdda062c0e9eaff`.
Follow-up commit `92460a00ec136dd885b4940184bee9d954da9106` contains only the vngrs
executor/test correction after preserved predecessor `6ff9ceb13bbf2b9a4de19ba1db7788f11d239570`
and has not been pushed. Exact-byte `du -x -B1 -s` is now the 120-second primary home-usage check;
`du -xsh` is a non-blocking diagnostic after exact-byte PASS below 30 GiB. Bounded >500 MiB
home-file manifests, explicit audit status and pre/post reconciliation are implemented. Top-level
PASS now requires source-stage PASS and post-run-audit PASS; source evidence is preserved when
the audit fails. Focused verification is `82 passed, 2 skipped`; compatible verification is
`321 passed, 9 skipped`. No
HU/SSH, network, source/footer, 151an/151at execution or experimental operation occurred. The
operational gate remains `blocked_by_operational_access` and the global gate remains
`blocked_by_measurement_design`.

The latest explicitly authorized 151an wave is `BLOCKED` before source access. The reviewed local
follow-up commit `de4a14e3370326173bdf04ce33356aae7826ddda` was published and HU was fast-forwarded
with the 42-entry dirty-state digest unchanged. The mandatory 30-second home-usage `du` timed out,
so PyArrow and the executor were not run; the post-run audit recorded root absence but its `du` and
large-home-file checks also timed out. Document 151au is frozen at SHA-256
`a83d832efa7478e86fa6bfa555cfe70d900f5284bf1bc862a8e5ca696d43fd8e`; 151av is frozen at
`d25789ddece62628a9cd6913eb1cbb94306816413a5b0b7768fec85a2709944a`. Documents 151an–151at
remain unchanged; no vngrs route/source byte, corpus, scoring, inference, evaluation, training or
readiness claim resulted. Operational gate remains `blocked_by_operational_access`; global gate
remains `blocked_by_measurement_design`.

A subsequent documentation-only alignment pass created Document 151aw and reconciled the restored
literature map, historical Document 60, active Document 145 and the 151at operational boundary.
It corrected over-strong OLMo/Pythia language-provenance wording and kept all model/corpus names at
their evidence-appropriate roles. The later authorized wave then published the commit and stopped
on the bounded storage preflight; no source/footer bytes or experiment artifacts were accessed.

The prior explicitly authorized preservation-checked fast-forward and one-time 151an wave
completed with a fail-closed `BLOCKED` result. The HU checkout moved once from
`9f1755219ba003d4aaf962558b3c0512fc74f99a` to
`c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`; the 42-entry status blob remained byte-for-byte
unchanged at 6,989 bytes with SHA-256
`71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`, and overlap remained zero.
Preflight and the independent PyArrow 24.0.0 writer/parser self-check passed. The only remote
executor invocation made one source attempt (`head_metadata_route-00000`) and failed closed on
HTTP 302 from the frozen direct route; no redirect was followed, retries/response bytes/artifacts
were zero, the root remained absent and executor exit code was 3. Documents 151ar/151as are the
current result/gate with SHA-256
`e531443254133a3ade95fcdf004420cc8726d28f337c7171c730937de3019967` and
`03c603265836320b173489a6659f91916c97db7ec78ebdd7b8faf0c1122a0ceb`. The narrow gate is
`blocked_by_operational_access` due route integrity; global gate remains
`blocked_by_measurement_design`; no retry, corpus, scoring, evaluation, GPU/Slurm or training
occurred.

The following older diagnostic summary is retained as historical context and is not current
execution authority:

The explicitly authorized HU-source-read-only diagnostic is complete and fail-closed with respect
to any new execution. Frozen Document 151an remains unchanged at SHA-256
`937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`; Documents 151ao/151ap also
remain unchanged at `5b8cb4be094e78f9e37a927348efab4491e5f00355496f2996d1170494dfae46` and
`aef81d9b72dd856b802310daaa4c64c7e37089ef22e2527cb6c88bbba8923468`. The exact reviewed commits
`cf6307c2055e4d1c947e9807b1af4cb8b03db70b`,
`22e1c305726c6214dd5325391d5e9cb943ebd119` and
`c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23` are published by ordinary non-force fast-forward;
pre-existing untracked artifact directories were not included. HU remains on
`9f1755219ba003d4aaf962558b3c0512fc74f99a` with 42 dirty status entries: 39 `.D` tracked
worktree deletions and three untracked top-level entries. Exact overlap with the 13-path
published change set is zero, so no pull or other HU mutation was attempted and no source/footer
request was made.

The mandatory 30-second `du -xsh` check returned exit 0, 32 stdout bytes, 0 stderr bytes and
`14G`; the 30-second `du -x -B1 -s` check returned exit 0, 40 stdout bytes, 0 stderr bytes and
`14687617024` bytes. Capacity/inode/path checks were read-only and the frozen root remained
absent. Document 151aq is the resulting diagnostic at SHA-256
`5a48d297ef5475550df41fd7e2baace4278acf54bbfb32bbfe455909dde7dbea`. The current `du` component
is therefore closed, but the dirty HU checkout keeps 151an `blocked_by_operational_access`.

Local verification before publication and after the documentation-only result pass passed with
`56 passed, 2 skipped` for the focused vngrs suite in 11.65 seconds and `286 passed, 9 skipped`
for the compatible repository suite in 17.43 seconds after excluding the three documented
collection failures. The execution result is
Document 151ao SHA-256
`5b8cb4be094e78f9e37a927348efab4491e5f00355496f2996d1170494dfae46`; the post-wave gate is
Document 151ap SHA-256
`aef81d9b72dd856b802310daaa4c64c7e37089ef22e2527cb6c88bbba8923468`. Executor invocations,
HTTP attempts, retries, response bytes, artifacts and output files are zero. The result is
`blocked_by_operational_access`; the global gate remains `blocked_by_measurement_design` with
`blocked_by_corpus_selection_or_materialization` contributing. `ready_to_measure` and
`ready_to_train` remain false. No corpus, scoring, inference, evaluation, GPU/Slurm, training,
cleanup/deletion or Documents 152–154 action occurred.

The older completion narratives below are retained as historical context; their superseded test
counts and unresolved implementation claims are not current authority.

The earlier review narrative below is preserved as historical context. Its statements that the
three authority files still contained the invalid 151ah hash are superseded by the completed
documentation correction and the current hashes recorded above and in the completion entry below.

```text
Independent review reproduced the worker's test claims: the vngrs suite completed with 14 passed
and 1 skipped; the compatibility suite excluding only the three documented collection failures
completed with 244 passed and 8 skipped. Full collection still fails only on two `scripts` import
paths and the locally absent `yaml` package. The systematic ordinal list and canonical selection
payload SHA-256 `dbb9714347970634386ff62366384fcae12cf5f12f1df1df676cb9af3ae25686`
were independently recomputed exactly.

The new code/151ak package is a useful scaffold but needs revision before execution. The pipeline
wrongly treats `original_id` as globally unique rather than `(corpus, original_id)`, rejects valid
integer zero IDs, retains non-Turkish records, returns only retained records instead of a complete
raw manifest, and scans benchmark overlap only after other exclusions. The all-pairs in-memory
near-dedup path is not safely bounded for 10,000 long web documents. The contract's equal 313/312
per-shard sample is not a release-weighted estimand, the 96-request/64-MiB access plan is not yet
proved feasible, threshold provenance and output/result-document protocols are incomplete. 151ak
must be append-only corrected and re-frozen before execution. This review changed only the living
handoff; no numbered document or code was modified.

A local timing probe of the current all-pairs near-dedup implementation on 1,000 short synthetic
records took 5.58 seconds. Its candidate comparison count grows quadratically; 10,000 records
imply roughly 100 times the pair comparisons before accounting for much longer web documents or
large overlap output, so this is performance evidence for correction rather than an execution
guarantee.

The worker report and three authority files also record an invalid 63-character 151ah SHA ending
in `...a1e`. Independent `shasum -a 256` gives the real current hash
`18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`. This handoff now uses the
real hash in its current authority/task fields; `AGENTS.md`, `ssh-client/README.md` and the
documentation index still require the same documentation-only correction.

Independent post-completion validation verified corrected 151ah SHA-256
`9151da7112b6d1ab9bbb3b483b202dec23449624beeddb53c23682569a0f598b` and confirmed the official
metadata against the immutable Hugging Face tree/card. The 284-shard layout, 50,336,214 rows,
text/corpus/original_id schema, CC BY-NC-SA 4.0 label and release-level byte totals are supported.
The structural correction correctly revokes the full-release target, old split arithmetic,
request/retry contradiction and tautological LID order.

151ah remains PREPARATION_BLOCKED. The proposed first-32 ordinal domain may be source/domain biased
if shard order is structured, so exact selected shards must be source-aware or systematically
spread unless exchangeability is evidenced. More importantly, sample fertility and missing LID,
PII, near-dedup and overlap implementations cannot be resolved by another metadata-only pass.
The living task now requires exact shard-selection metadata, local pipeline implementation/unit
tests and a frozen bounded sample-calibration contract. No HU/network or numbered scientific
document changed during this independent validation; only the living handoff changed.

Completed the authorized six-file C1 reconciliation and local corpus-decision/contract task.
Document 151ag SHA-256 is
`54c9e1f8c626fb0f67a2d1f5396277304c30714074683b386ac39da3a8f22497`; its exact counts and
conditional corpus roles are accepted. Document 151ah SHA-256 is
`a8c1d1d2082ec3ae5b31ace5dc0a9506ace90f82d0f7bd1a2c1a528069ef2269`; it remains frozen,
unexecuted and PREPARATION_BLOCKED.

Independent contract validation found that route/license metadata resolution alone is
insufficient. 151ah currently targets all 50,336,214 source documents despite a maximum planned
1B-token dose; fixes a 1,006,724-document held-out set and 49,329,490-document training set that
sum to the raw release even though earlier filters may exclude documents; permits 4,096 retries
under a 2,048 total-request ledger bound; and places an aggregate Turkish-LID check after a
per-document Turkish-LID retention rule, making the aggregate gate tautological. Exact tool/model/
evaluator identities for filtering, PII, deduplication and overlap are also not yet frozen.
151ah must therefore receive an append-only metadata-informed structural correction before any
execution request. No HU/network or numbered-document mutation occurred during this independent
validation; only this living handoff was updated.

Completed the explicitly authorized single bounded execution of corrected Document 151ab.
Verified 151ab SHA-256: `3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c`.
Mandatory HU storage/path/inode preflight passed; only the closed read-only allowlist was used and
only `/vol/tmp2/yesildau/luna_measurement_design_input_preparation_v1` was writable. Exactly eight
compact outputs were created, with `final_inventory_audit.json` written last and self-reference
excluded. The post-run audit passed and reported unchanged source roots.

Document 151ae records the execution result with SHA-256
`b6a90ce5573de1c29828186dbc278c7c92c87dc1e435ef44965d0eff6f8e1601`; Document 151af records the
post-execution gate with SHA-256
`1e96a4b8d29edc50a8f151a34990c93edf3b5115dfb76416500261f8f8d817d1`. Operational inventory is
PASS. Scientific status is BLOCKED: primary `blocked_by_measurement_design`, contributing
`blocked_by_corpus_selection_or_materialization`, `ready_to_measure=false` and
`ready_to_train=false`. No selected adaptation corpus or primary in-domain split was fabricated.
No network/download, materialization, raw recursive corpus read, large-weight rehash, scoring,
inference, evaluation, GPU/Slurm, training, cleanup/deletion, HU-home/prior-root write,
151ac/151ad or Documents 152--154 action occurred. A later guard-only re-entry saw the existing
root and failed closed before any write; it did not constitute a second inventory.

Independent validation verified the document hashes and recomputed the output totals: the seven
pre-final-audit outputs sum to 78,118 bytes and all eight outputs sum to 80,820 bytes. The
operational PASS/scientific BLOCKED distinction is supported. The compact result, however, does
not enumerate exact C1 status counts, and corpus selection and materialization must be separated
from the generic measurement blocker. The living task is therefore a bounded six-file read-only
validation plus local corpus decision/materialization-contract preparation, not another 151ab
execution.

Completed the authorized bounded official-public-metadata and local read-only implementation
audit for 151ah. The immutable release metadata verified `ee5c6201ee84457a18182bfc483a7d8a7f3655ba`,
50,336,214 train rows, the `text/corpus/original_id` schema, CC BY-NC-SA 4.0 metadata and a
284-file Parquet tree. The release card's approximately 25.33B VBART-token statement was kept as
release-level planning metadata only, not sample-based fertility evidence. The local audit found
no vngrs Parquet materializer, frozen Turkish-LID evaluator, PII recognizer, near-dedup
implementation or benchmark-overlap evaluator; the generic normalization, filtering, exact-dedup,
contamination and split helpers are insufficient to close those blockers.

Appended the effective correction to 151ah, preserved its pre-correction SHA-256
`a8c1d1d2082ec3ae5b31ace5dc0a9506ace90f82d0f7bd1a2c1a528069ef2269`, and froze current SHA-256
`9151da7112b6d1ab9bbb3b483b202dec23449624beeddb53c23682569a0f598b`. The corrected contract
overrides the full-release target, fixes request/retry accounting, separates raw/retained/heldout/
training populations, and bounds the candidate domain to 32 whole shards without freezing an
executable allowlist. The result is `PREPARATION_BLOCKED`, with primary
`blocked_by_measurement_design` and contributing `blocked_by_corpus_selection_or_materialization`;
`ready_to_measure=false` and `ready_to_train=false`. Updated AGENTS.md, `ssh-client/README.md`,
the documentation index and this handoff. 151ai/151aj remain reserved and uncreated.
```

## Completed Prior Task Override — Prepare the vngrs pipeline and freeze bounded sample calibration

Under the explicit authorization, the official public metadata was checked again only at the
immutable vngrs revision; no HU/SSH, full shard/row download, materialization or actual sample
calibration occurred. The official tree/card supports the 284-shard layout, 50,336,214 train rows,
`text/corpus/original_id`, CC BY-NC-SA 4.0 and release-level planning metadata. Exact per-shard
object IDs, exact bytes, footer facts, execution-time license-byte hashes and sample fertility
remain unresolved and were not fabricated.

The local `transfer-vs-relearning` repository now contains the inspectable package
`src/transfer_vs_relearning/corpora/vngrs/` with deterministic metadata/selection, Parquet
metadata and streaming, stable identity, normalization, fail-closed LID adapter,
quality/spam/code/adult and PII diagnostics, exact deduplication, feature-cap-free MinHash
near-deduplication, synthetic contamination/benchmark overlap and document-disjoint split
helpers. Unit tests are in `tests/test_vngrs_preparation.py`; no raw PII or benchmark answers are
embedded.

The exact systematic midpoint selection is frozen as `vngrs_systematic_midpoint_32_of_284_v1`:
ordinals `00004,00013,00022,00031,00039,00048,00057,00066,00075,00084,00093,00102,00110,00119,00128,00137,00146,00155,00164,00173,00181,00190,00199,00208,00217,00226,00235,00244,00252,00261,00270,00279`, with selection payload SHA-256
`dbb9714347970634386ff62366384fcae12cf5f12f1df1df676cb9af3ae25686`. This replaces the
provisional first-32 candidate rule in 151ah; it is an exact path set, not proof of source balance
or exact per-shard metadata.

Document 151ah preserves its prior SHA-256 `9151da7112b6d1ab9bbb3b483b202dec23449624beeddb53c23682569a0f598b`
and is re-frozen at `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`.
Document 151ak is frozen and unexecuted at SHA-256
`97a2d8a53cc8ff8390f71ddb833e9582d8b46fb9793deeb6673483a1384df012`; it targets exactly 10,000
records with seed 42 and bounded fail-closed calibration gates. No proposed scratch root was
created.

Test result for that prior task was `14 passed, 1 skipped` across 15 tests. No 151ah execution,
sample calibration, network/SSH, materialization, scoring, inference, weights/tokenizers,
GPU/Slurm, training, cleanup/deletion, 151ai/151aj or Documents 152–154 action occurred. The
primary gate remained `blocked_by_measurement_design`, with contributing
`blocked_by_corpus_selection_or_materialization`; `ready_to_measure=false` and
`ready_to_train=false`.

## Completed Current Task Override — Repair the vngrs pipeline and 151ak before any sample execution

The explicitly authorized task was completed locally without HU/SSH, corpus-row or full-shard
download, actual sample calibration, materialization, model-weight access, scoring, GPU/Slurm or
training. Bounded official public metadata was limited to the immutable Hugging Face release
tree/card and Dataset Viewer route documentation; no corpus payload was retrieved.

The local `transfer-vs-relearning` package now enforces composite `(corpus, original_id)` identity
with integer zero valid; retains only verified accepted Turkish LID labels at confidence `>= 0.80`
while preserving all raw rows; records exact serialized record payload bytes separately from HTTP
response bytes; reports raw, exact-canonical and retained overlap surfaces; uses deterministic
32-band/4-row LSH with unbounded character 5-grams and no undeclared feature cap; reports matched-
character quality coverage and exploratory/non-comprehensive heuristic scope; and allocates rows by
row-count-weighted largest remainder with per-shard weights. The output protocol is
self-reference-free: seven artifacts feed `output_artifact_manifest.jsonl`, and the final audit is
written last. Helper tests can use smaller inputs, while a scientific profile still requires exact
10,000 raw manifest rows.

Official route resolution established that `/rows` permits at most 100 rows per request, so the
former 96-request ceiling could not retrieve 10,000 rows. The corrected future bound is 128 total
requests and 32 retries, with 64 MiB total and 4 MiB single-response ceilings. Shard-stratified
offset mapping, exact per-shard footer/byte evidence and sample fertility remain unresolved; 151ak
therefore remains `FROZEN — PREPARATION_BLOCKED — UNEXECUTED`, not execution-authorized.

Document 151ah current SHA-256 is
`18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`; Document 151ak preserves
pre-correction SHA-256 `97a2d8a53cc8ff8390f71ddb833e9582d8b46fb9793deeb6673483a1384df012` and is
re-frozen at current SHA-256 `8fc6d3dbc89f9b71e7b9e1f6ca787fce81bbcde2cf715abfa2fec54eb0b07bd5`.
151al/151am remain reserved for a later separately authorized result/gate pair; 151ai/151aj remain
reserved for 151ah. The primary gate remains `blocked_by_measurement_design`, contributing
`blocked_by_corpus_selection_or_materialization`, and `ready_to_measure`/`ready_to_train` remain
false.

## Completed Current Task Result — Close 151ak implementation, estimator and route-feasibility blockers

The explicitly authorized local-only task is complete. No HU/SSH access, network corpus retrieval,
corpus-row or full-shard download, 151ah/151ak execution, materialization, scoring, inference,
evaluation, model/tokenizer access, GPU/Slurm, training, cleanup/deletion or 151al/151am/151ai/151aj/
152–154 action occurred.

Changed local implementation/test files are under `transfer-vs-relearning/src/transfer_vs_relearning/
corpora/vngrs/` and `tests/test_vngrs_preparation.py`. The package now has explicit scientific
fail-closed gate evaluation; the exact 31-field text-free record manifest; separate request-level
versus record-level byte accounting; the universal mod-prime MinHash family with no feature cap;
SQLite-spilled LSH candidate/output bounds; exact integer largest-remainder allocation; and strict
gap-free lowercase-SHA output-chain validation. The realistic probes covered long web-like
near-clusters, duplicate/candidate explosion fail-closed behavior, estimator error, the historical
2,000-record zero-candidate timing case and large-count/tie allocation.

Verification results were `25 passed, 1 skipped` in the focused suite (the PyArrow-only local
fixture was unavailable) and `255 passed, 8 skipped` in the compatible repository suite in 8.96
seconds after excluding the three documented pre-existing collection failures. The long web-like
cluster probe completed in 0.041 seconds with 3 candidates and 2 verified near-duplicate pairs;
the adversarial candidate explosion failed closed at its bound. The historical 2,000-record probe
completed in 3.423 seconds with zero candidates and zero verified pairs. Estimator absolute errors
for exact Jaccards 0.8182, 0.9048 and 0.0 were 0.0412, 0.0093 and 0.0 respectively.

Document 151ak was corrected append-only, preserving pre-correction SHA-256
`8fc6d3dbc89f9b71e7b9e1f6ca787fce81bbcde2cf715abfa2fec54eb0b07bd5`, and re-frozen at
`eb520ece20b157ec342cd6511589907b561dd7cea5e4d68cb1cd84327c92bd8e`. AGENTS.md,
`ssh-client/README.md`, the documentation index and this handoff now point to that hash and the
coherent route envelope (`128` total attempts, `100` successful-row-request maximum, `28` retries,
`64 MiB` total, `4 MiB` single response). The 32-shard/midpoint route mapping, exact footer/byte
evidence and sample fertility remain blockers; 151ak is still `PREPARATION_BLOCKED` and
unexecuted, with the global gate `blocked_by_measurement_design` and contributing
`blocked_by_corpus_selection_or_materialization`.

## Historical Single Next Authorization Request — superseded by later vngrs authorities

```text
The user may explicitly authorize only the current local-only override: append-only correct and
re-freeze 151an as a metadata/footer feasibility contract; separate immutable object identity from
compact evidence hashes; remove the unsupported `/rows` provenance branch; freeze an exact future
scratch root/routes/bounds/output set; align the local validator/tests; and update the authority
files. This authorization does not permit HU/SSH, network/public metadata, source/footer/row
access, 151an execution, 151ah/151ak execution, corpus materialization, scoring, inference,
model/tokenizer access, GPU/Slurm, training, cleanup/deletion, 151ao/151ap or Documents 152–154.
The global gate remains `blocked_by_measurement_design`.
```

## Task History

- **2026-08-08 — Final 151an byte-integrity correction requested:** Independent validation
  verified the corrected 151an hash and reproduced `34 passed, 1 skipped` plus
  `264 passed, 8 skipped`. Adversarial probes then showed that the validator accepted a fabricated
  shard/row-group count with unchanged footer bytes, accepted `attempt_ordinal=-999` and a response
  byte count inconsistent with the bound payload, and accepted unrelated artifact-manifest bytes
  after only the caller SHA was updated. The current task was reset to `READY_FOR_USER_HANDOFF`
  for one local-only real-footer-parser, request/retry, response-byte, canonical-manifest and
  final-audit correction. Only this living handoff changed; no HU, network, source/footer data,
  numbered document, execution, evaluation or training action occurred.
- **2026-08-08 — 151an execution-readiness correction requested:** Independent validation
  verified 151ak/151an hashes and reproduced `30 passed, 1 skipped` plus `260 passed, 8 skipped`.
  The 151ak evidence-binding fixes were accepted. Review of the official Dataset Viewer contract
  showed that `/rows` has no immutable-revision or selected-shard parameter, so it cannot establish
  the frozen path-stratified provenance assumed by 151an. Local inspection also found the
  `parquet_footer_range` versus `/rows` validator mismatch, `rows`/`/rows` vocabulary conflict,
  unfrozen scratch root, full-object-SHA versus compact-evidence-SHA conflation and compact/text-free
  response contradiction. The current task was reset to `READY_FOR_USER_HANDOFF` for one
  local-only append-only 151an metadata/footer-feasibility correction. Only this living handoff
  changed locally; no HU, source data, execution, corpus, evaluation or training action occurred.
- **2026-08-08 — Completed:** Under the explicitly authorized local-only override, preserved
  Document 151ak's pre-correction SHA-256 `1920f1c58d8ada250af50d1f088f5ad2fc3a15e8221f84d92f5458dab415154b`,
  appended the evidence-binding and deterministic sampling-schedule correction, and re-froze it at
  SHA-256 `9e35ba69fcd4885c339101e59f1d719681942571770a41023f20f6472782ea94`. Added explicit
  child-artifact and request-response byte bindings, exact source-window and schedule validation,
  synthetic-control classification and hash-bound near-dedup reconciliation. Focused tests were
  `30 passed, 1 skipped in 11.57s`; the compatible suite was `260 passed, 8 skipped in 17.52s`
  with the same three collection exclusions. Created frozen unexecuted 151an at SHA-256
  `435e0c25cedd7fd8fcb70862c637040300c2d5b201bfb5fa25c2b20232e71096`, reserving 151ao/151ap.
  Updated AGENTS.md, `ssh-client/README.md`, the documentation index and this handoff. No HU/SSH,
  network, corpus access, route resolution, execution, result/gate document, evaluation, training,
  cleanup or Documents 152–154 action occurred. Primary gate remains
  `blocked_by_measurement_design`; `ready_to_measure` and `ready_to_train` remain false.
- **2026-08-08 — Remaining evidence-binding/sampling correction requested:** Independent
  validation verified 151ah/151ak hashes and reproduced `28 passed, 1 skipped` plus
  `258 passed, 8 skipped`. The nominal positive graph still returned `PASS` although 90/100
  successful requests exceeded their declared shard row bounds. A source graph with invented but
  format-valid child hashes passed after only its parent hash was recomputed, and tampered
  near-dedup count/rate remained `payloads_canonical_and_linked=true`. The current task was reset
  to `READY_FOR_USER_HANDOFF` for one local-only validator/test/append-only 151ak correction and an
  unexecuted route/footer/sampling-resolution contract. Only this living handoff changed; no HU,
  network, numbered document, corpus, evaluation or training action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only authorization for “Seal the 151ak final
  evidence graph before route resolution,” preserved 151ak’s pre-correction SHA-256
  `16f2978b10fc2b71490917ffe9ed549b574d2b364865675f0f1d900fb4320d68` and re-froze the
  append-only contract at `1920f1c58d8ada250af50d1f088f5ad2fc3a15e8221f84d92f5458dab415154b`.
  The implementation now enforces the immutable 10,000-row/32-shard profile, strict source and
  route evidence, explicit request-attempt retry chains, record/request foreign keys, recomputed
  metric inputs and actual artifact/final-audit payload bytes. A complete positive control passed;
  all required one-at-a-time mutations returned BLOCKED. Focused tests were `29 collected, 28
  passed, 1 skipped`; the compatible suite was `258 passed, 8 skipped`. Updated AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. No HU/SSH, network, public
  metadata, route resolution, corpus access, execution, evaluation or training occurred.
- **2026-08-08 — Final evidence-graph hardening requested:** Independent validation reproduced the
  final hash and both suite populations and accepted the earlier local fixes. A one-record positive-
  looking adversarial payload nevertheless made `evaluate_final_contract` return `PASS` with one
  shard, one request and arbitrary format-valid hashes. The task was reset to
  `READY_FOR_USER_HANDOFF` to freeze 10,000-row/32-shard identity, request-record foreign keys,
  retry semantics, recomputed evidence/artifact hashes and positive-plus-mutation final-gate tests.
  Only this handoff changed; no HU/network, route, numbered-document, corpus, evaluation or
  training action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only authorization, closed the final 151ak
  gate-ownership, composite-manifest and evidence gaps without HU/SSH, network, route resolution,
  corpus access, 151ak execution, evaluation or training. Preserved 151ak's pre-correction
  SHA-256 `eb520ece20b157ec342cd6511589907b561dd7cea5e4d68cb1cd84327c92bd8e` and froze final
  SHA-256 `16f2978b10fc2b71490917ffe9ed549b574d2b364865675f0f1d900fb4320d68`. Added raw-row LID
  identity enforcement, the 34-field composite manifest and raw/normalized length distinction,
  request-ledger aggregate validation, a non-final preparation stage, final contract runner and
  exact final-audit path validation. Focused suite: 28 collected / 27 passed / 1 skipped in 3.58s;
  compatible suite: 257 passed / 8 skipped in 8.90s with exactly the three documented collection
  exclusions. Updated AGENTS.md, `ssh-client/README.md`, the documentation index and this handoff;
  151ah remained at SHA-256 `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`.
- **2026-08-08 — Final 151ak local gate correction requested:** Independent validation reproduced
  the new hashes and both test suites and accepted the MinHash/SQLite/integer/output-validator
  improvements. Adversarial probes nevertheless found a missing-evaluator-identity scientific
  `PASS`, loss of `corpus` from manifest identity, normalized length mislabeled as raw length, and
  a preparation-stage `PASS` without request-ledger or final-audit evidence. It also found that the
  estimator/performance numbers in the completion narrative do not match the committed cases and
  that the 21,000-character fixture is not near the 100,000-character ceiling. The task was reset
  to `READY_FOR_USER_HANDOFF` for a final local code/test/append-only authority correction before
  any route pass. Only this handoff changed; no HU/network, numbered-document, corpus, evaluation
  or training action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only authorization, closed the vngrs
  implementation, estimator and route-feasibility corrections without HU/SSH, network corpus
  retrieval or 151ak execution. The focused suite passed `25 passed, 1 skipped`; the compatible
  suite passed `255 passed, 8 skipped` in 8.96 seconds after the three documented pre-existing
  collection failures were excluded. Long web-like cluster, candidate-explosion, estimator,
  integer-allocation and output-chain probes were run. Appended the effective correction to 151ak,
  preserving pre-correction
  SHA-256 `8fc6d3dbc89f9b71e7b9e1f6ca787fce81bbcde2cf715abfa2fec54eb0b07bd5` and freezing
  `eb520ece20b157ec342cd6511589907b561dd7cea5e4d68cb1cd84327c92bd8e`. Updated AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. The route mapping, exact
  footer/byte evidence and sample-fertility blockers remain unresolved; no training or later
  numbered document is authorized.
- **2026-08-08 — 151ak post-completion correction requested:** Independent validation verified
  both document hashes and reproduced `18 passed, 1 skipped` plus `248 passed, 8 skipped`, while
  accepting the completed identity/LID/overlap/output-protocol improvements. It found a fail-open
  overall LID gate, incomplete and text-bearing record-manifest rows, a tautological manifest-
  completeness flag, unsupported MinHash-family semantics, non-representative zero-candidate
  performance evidence, floating-point apportionment, incomplete SHA/order-chain validation and an
  unresolved contiguous-route versus dispersed-position/request-budget problem. The task was reset
  to `READY_FOR_USER_HANDOFF` for a local code/test/contract-consistency and official-metadata-only
  route correction. Only this living handoff changed; no HU/network, numbered-document, corpus,
  evaluation or training action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only authorization, corrected the vngrs
  preparation package and tests, verified bounded official release/route metadata, append-only
  corrected and re-froze 151ak, and fixed the invalid 63-character 151ah hash in AGENTS.md,
  `ssh-client/README.md` and the documentation index. The focused suite passed `18 passed, 1
  skipped` (19 tests; PyArrow fixture unavailable); the compatible repository suite passed `248
  passed, 8 skipped` in 8.55 seconds after excluding the three documented collection failures.
  A direct 2,000-record near-dedup probe completed in 3.059225 seconds with zero LSH
  candidates/evaluated pairs and no near-duplicate pairs; the focused command took 3.25 seconds.
  Current 151ak SHA-256 is
  `8fc6d3dbc89f9b71e7b9e1f6ca787fce81bbcde2cf715abfa2fec54eb0b07bd5`; 151ah SHA-256 is
  `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`. No HU/SSH, corpus
  retrieval, sample calibration, materialization, scoring, inference, model access, GPU/Slurm,
  training, cleanup or forbidden document creation occurred. The current gate remains
  `blocked_by_measurement_design` with contributing `blocked_by_corpus_selection_or_materialization`.
- **2026-08-08 — vngrs scaffold and 151ak independently reviewed:** Recomputed the exact
  systematic selection and payload hash; reproduced 14/1 and 244/8 tests. Found material
  pre-execution defects in composite source identity, zero-ID handling, LID retention, raw manifest
  completeness, raw overlap coverage, all-pairs near-dedup scalability, sample weighting,
  access-route feasibility, threshold provenance and result/audit protocol. Replaced the proposed
  151ak execution with a bounded local code/contract correction task. Also found that the reported
  151ah SHA omitted its final `8`; the actual local SHA is
  `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`. No HU/network, code or
  numbered scientific document changed in this independent review; only this handoff changed.
- **2026-08-08 — Completed:** Under the explicit local preparation authorization, verified official
  vngrs release metadata without HU/SSH or network-side corpus access, implemented and locally
  tested the reusable vngrs preparation package, and froze the exact systematic midpoint 32-shard
  path set. Appended the selection correction to 151ah, preserving prior SHA-256
  `9151da7112b6d1ab9bbb3b483b202dec23449624beeddb53c23682569a0f598b` and freezing current SHA-256
  `18bf6d59b0552b044bec70f2f41852912c493ec098917dbd1ed87f5078eda1e8`. Created frozen-unexecuted
  151ak with SHA-256 `97a2d8a53cc8ff8390f71ddb833e9582d8b46fb9793deeb6673483a1384df012`.
  The focused tests passed `14 passed, 1 skipped` across 15 tests; the compatible repository suite
  passed `244 passed, 8 skipped` after excluding three pre-existing collection failures. No sample calibration, 151ah execution,
  materialization, scoring, inference, weights/tokenizers, GPU/Slurm, training, cleanup or
  Documents 151ai/151aj/152--154 action occurred. The gate remains
  `blocked_by_measurement_design` with contributing `blocked_by_corpus_selection_or_materialization`.
- **2026-08-08 — Corrected 151ah independently validated:** Verified the corrected hash and
  official release metadata. Accepted the bounded structural repair, but identified potential
  ordinal/source bias in the first-32 shard domain and the impossibility of closing missing
  evaluator implementations or sample fertility through metadata alone. Reset the next task to
  exact source-aware/systematic shard selection, local pipeline implementation/unit tests and a
  frozen bounded sample-calibration contract. No HU/network or numbered document changed.
- **2026-08-08 — 151ah independent contract validation:** Accepted 151ag and verified both new
  document hashes. Classified 151ah as requiring structural correction before execution because
  it over-scopes acquisition to the full 50,336,214-document release, fixes post-filter split
  counts to the raw total, has inconsistent request/retry bounds, makes the aggregate LID gate
  tautological and does not freeze all required tool/evaluator identities. Reset the living task
  to a bounded official-metadata pass plus append-only 151ah repair. No HU/network or numbered
  scientific document was changed.
- **2026-08-08 — Completed:** Under the explicit authorization, completed the six-file HU
  read-only C1 reconciliation and created 151ag/151ah. The exact counts were 60 source-allowlist,
  5 model/tokenizer, 6 evaluation-input and 17 C1 rows; C1 statuses were 12 observed compact,
  2 verified selected manifests, 1 blocked, 1 existing control identity and 1 existing input
  identity. Document 151ag SHA-256 is
  `54c9e1f8c626fb0f67a2d1f5396277304c30714074683b386ac39da3a8f22497`; Document 151ah SHA-256 is
  `a8c1d1d2082ec3ae5b31ace5dc0a9506ace90f82d0f7bd1a2c1a528069ef2269`. 151ah is frozen but
  `PREPARATION_BLOCKED` because exact release file/shard routes and execution-time license
  evidence were not in the authorized six files. No HU writes, network/download, materialization,
  scoring, inference, evaluation, GPU/Slurm, training, cleanup/deletion or 151ai/151aj/152--154
  action occurred. The next request is metadata-only route/license/shard resolution, not 151ah
  execution.
- **2026-08-08 — Post-inventory corpus-decision preparation requested:** Independent validation
  accepted the 151ae/151af operational and scientific verdicts, recomputed both output byte totals
  and identified the need for exact C1 counts plus a separate conditional corpus decision. The
  task was reset to `READY_FOR_USER_HANDOFF` for six-file HU read-only validation and local
  151ag/151ah decision/contract preparation. Only this living handoff changed; no HU/network or
  numbered-document action occurred.
- **2026-08-08 — Completed:** Under the explicit authorization, verified corrected 151ab SHA-256
  `3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c` and completed exactly one
  bounded HU evidence/input inventory. Mandatory storage/path/inode preflight passed; only the
  closed read-only allowlist was used and only
  `/vol/tmp2/yesildau/luna_measurement_design_input_preparation_v1` received writes. Exactly eight
  outputs were created, final audit last/self-reference-free, and the post-run storage audit
  passed with prior roots unchanged. Created 151ae (SHA-256
  `b6a90ce5573de1c29828186dbc278c7c92c87dc1e435ef44965d0eff6f8e1601`) and 151af (SHA-256
  `1e96a4b8d29edc50a8f151a34990c93edf3b5115dfb76416500261f8f8d817d1`). Operational inventory is
  PASS; scientific gate remains `blocked_by_measurement_design` with contributing
  `blocked_by_corpus_selection_or_materialization`; no selected corpus, primary split or training
  authorization resulted. No network, downloads, materialization, scoring, inference,
  evaluation, GPU/Slurm, training, cleanup/deletion, prior-root/HU-home writes or Documents
  151ac/151ad/152--154 action occurred.
- **2026-08-08 — Corrected 151ab inventory execution prepared:** Independent validation verified
  the final 151ab hash and accepted the bounded HU inventory contract as operationally coherent.
  The task was reset to `READY_FOR_USER_HANDOFF` for exactly one inventory execution with
  151ae/151af result/gate creation. Only this living handoff changed; no HU/network or numbered-
  document action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only authorization, preserved 151ab’s
  pre-operational-correction SHA-256
  `3320516e674c12288d70396e31b33c059550c15365caabe9453e932e3858e2dc`, appended the operational
  HU/SSH/source-allowlist/corpus-gate correction, and re-froze final SHA-256
  `3ff0823f8a4aa5f806ab451abfffa989e0d70f1b1ca6b240229306cfac99c06c`. Updated AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. No HU/SSH, network,
  scratch-root creation, download, materialization, scoring, inference, evaluation, GPU/Slurm,
  training, cleanup/deletion, result/gate document or Documents 152–154 action occurred. The
  next request is a separately authorized bounded HU/SSH inventory only; 151ae/151af are reserved
  and uncreated, and the corpus-selection/materialization blocker remains.

- **2026-08-08 — 151ab input-preparation authorization correction requested:** Independent
  validation accepted the substantive BPB/state/estimand/review-ledger correction but found that
  the proposed `/vol/tmp2` output conflicts with the HU/SSH prohibition and that an in-domain split
  cannot be frozen before corpus selection/materialization. The task was reset to
  `READY_FOR_USER_HANDOFF` for one local append-only operational correction. Only this living
  handoff changed; no HU/network, preparation execution or numbered-document mutation occurred.
- **2026-08-08 — 151ab execution-readiness correction requested:** Independent validation accepted
  151aa's six-file audit, 7-row manifest correction and exact 150-row reconciliation, but found
  151ab incomplete for baseline execution. The current task was reset to
  `READY_FOR_USER_HANDOFF` for a local append-only BPB/held-out/state-matrix/estimand/scoring/
  threshold correction and re-freeze. Only this living handoff changed; no HU/network,
  measurement, scoring, inference, materialization, training or numbered-document mutation
  occurred.
- **2026-08-08 — Completed:** Under the explicit local-only authorization, preserved 151ab’s
  pre-correction SHA-256 `500b24f6945272cbf7ddb0f26e95449434857bcac89ed5fb5d593e3fd189b4dd`,
  appended the execution-readiness correction and re-froze final SHA-256
  `3320516e674c12288d70396e31b33c059550c15365caabe9453e932e3858e2dc`. Updated AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. The next request is now
  evidence/input preparation rather than baseline scoring. No HU/SSH, network, download,
  materialization, scoring, inference, evaluation, GPU/Slurm, training, cleanup/deletion,
  151ac/151ad or Documents 152–154 action occurred.
- **2026-08-07 — Created:** Living handoff created after the completed Document 151g repair wave.
  Current assignment is the unexecuted local preparation of Document 151j.
- **2026-08-07 — Completed:** Local documentation-only measurement-design correction contract
  preparation. Appended the required 151h clarification, created unexecuted 151j, updated the
  index/AGENTS/SSH authority summaries, and recorded 151j SHA-256
  `15d43a2bd2c87802c0d1f9c20d20ac59c370af5135751ff02709b938be5e1259`. No remote or execution
  activity occurred.
- **2026-08-07 — Completed:** Local Document 151p correction pass. Read the required local
  sources/tests, verified cited SHA-256 values, recomputed the 829/713 surface unions and exact
  set hashes, reproduced the 65,717 inventory, created 151p, and aligned AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. Documents 151n/151o were
  preserved unchanged. No HU or network access occurred.
- **2026-08-07 — Review correction prepared:** Independent review found 151j too broad and
  internally circular for execution. Current task reset to `READY_FOR_USER_HANDOFF`: preserve 151j
  as a superseded unexecuted requirements draft and prepare a smaller unexecuted Phase-1 evidence-
  resolution contract as Document 151m. No numbered document was changed during this handoff-only
  update.
- **2026-08-07 — Completed:** Executed only the Phase-1 documentation override. Appended the 151j
  supersession note, created unexecuted 151m, aligned the index/AGENTS/SSH authority summaries,
  computed both required hashes, and updated this handoff. No remote, network, measurement or
  execution activity occurred.
- **2026-08-07 — Completed:** Executed the explicitly authorized frozen 151m Phase-1 wave once,
  created 151n/151o, performed the mandatory storage/path/inode checks and preserved the existing
  roots. Operational bounds PASS; benchmark registry, synthetic 713/829 provenance,
  contamination-definition and source-model provenance gates remain blocked. No inference,
  scoring, training, cleanup, deletion, migration, 151j, 151k/151l or 152–154 action occurred.
- **2026-08-07 — Independent validation correction prepared:** Local frozen evidence disproved the
  claimed unresolved 713/829 provenance and exactly reproduced the 65,717 contamination inventory.
  The benchmark request ledger and model registry were found incomplete rather than exhaustive.
  Current task reset to `READY_FOR_USER_HANDOFF` for an append-only Document 151p correction. Only
  this living handoff was changed; no HU/network or experimental action occurred.
- **2026-08-07 — Document 151p independently validated:** Recomputed all decision-critical counts,
  set hashes and the 65,717 channel breakdown; verified 151p and upstream hashes and authority
  alignment. Found only the non-material six-traversed/four-attributed source-path clarification.
  Current task reset to `READY_FOR_USER_HANDOFF` for local preparation of unexecuted Document 151q.
  Only this living handoff was changed; no HU/network or experimental action occurred.
- **2026-08-07 — Completed:** Prepared unexecuted Document 151q for bounded benchmark/source-model
  metadata registry completion, computed SHA-256
  `b55499242100263e0d9adbe946679b6175268012d1c3e897298413a2af1ef60c`, reserved 151r/151s, and
  aligned AGENTS.md, `ssh-client/README.md`, the documentation index and this handoff. No HU,
  network or contract execution occurred.
- **2026-08-07 — 151q execution-readiness correction prepared:** Independent review accepted the
  schemas and bounds but found unfrozen generic source routes and an item-hash/download-policy
  contradiction. Current task reset to `READY_FOR_USER_HANDOFF` for bounded public route
  resolution and append-only re-freeze of 151q. Only this living handoff was changed; 151q remains
  unexecuted and 151r/151s remain uncreated.
- **2026-08-07 — Completed:** Authorized bounded public-metadata route-resolution and append-only
  151q correction pass. Preserved the original 151q SHA, appended the route-resolution addendum,
  resolved model revisions and canonical benchmark routes where discoverable, recorded the exact
  remaining preparation blockers, computed corrected 151q SHA-256
  `0acf5251bea811e07b6442681ec02c7bc4fa2ea584a55e8b48cbcb704d4209e3`, and aligned AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. No HU/SSH, benchmark item
  collection, model artifact access, scoring, inference, training, 151q execution or 151r/151s
  creation occurred.
- **2026-08-07 — Final 151q correction prepared:** Independent official Git metadata checks
  resolved all three benchmark repository SHAs and complete trees, identified the nonexistent
  EXAMS `_tr` test path, and located the author-contributed TurkishMMLU harness evaluator at an
  immutable revision. Current task reset to `READY_FOR_USER_HANDOFF` for a second append-only 151q
  addendum and final re-freeze. Only this handoff was changed; 151q remains unexecuted.
- **2026-08-07 — Completed:** Per explicit authorization, performed the bounded public-metadata
  second verification pass, appended the final immutable benchmark overlay to 151q, computed final
  SHA-256 `c217f4d8395a8e3b657f96fd46f3e6443a11fcde6bbfbd6f7a8414933ccf89ee`, and aligned AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. 151q remains unexecuted;
  151r/151s were not created; no HU/SSH, item-content download, scoring, inference, model artifact,
  corpus, GPU/Slurm, training, cleanup or deletion occurred.
- **2026-08-07 — Operational contradiction correction prepared:** Final review found that the
  frozen HU scratch output requirement conflicts with the blanket no-HU execution language and
  that the scoring clause is ambiguous. Current task reset to `READY_FOR_USER_HANDOFF` for a
  local-only third 151q addendum. Only this handoff was changed; 151q remains unexecuted.
- **2026-08-07 — Completed:** Per explicit authorization, appended the third operational
  clarification to 151q, computed final SHA-256
  `f1cdfe082a78fce612d7bc53766e88dae3182ffcf52a225f2aa81e24c2491561`, and aligned AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. The future execution-location
  authority is now explicit; benchmark scoring remains unconditionally forbidden. No HU/SSH,
  network, 151q execution, 151r/151s creation or experimental work occurred.
- **2026-08-07 — Completed:** Under one explicit Single Next Authorization Request, executed
  corrected 151q once with mandatory HU storage/path/inode preflight, fixed public routes, new-root
  only writes and post-run storage audit. The wave fail-closed on the EXAMS 32 MiB single-response
  bound. Created Documents 151r/151s, preserved the initial report and added an append-only audit
  correction for its stale operational-gate field. Effective operational gate is
  `blocked_by_operational_access`; global gate remains `blocked_by_measurement_design`; no
  training or ready-to-train status was authorized.
- **2026-08-07 — Minimal retry preparation requested:** Independent validation confirmed the
  missing EXAMS artifact is 38,208,781 bytes and genuinely exceeds the frozen 32 MiB limit. The
  first fail-closed record remains valid. Current task reset to `READY_FOR_USER_HANDOFF` for a
  local-only minimal retry contract that preserves and reuses the verified first-wave evidence.
  Only this living handoff was changed; no retry, HU/network access or numbered-document mutation
  occurred.
- **2026-08-07 — Completed:** Prepared and froze unexecuted Document 151t
  `MINIMAL_151Q_REGISTRY_COMPLETION_RETRY_CONTRACT_TR.md`, SHA-256
  `eef968538b2022250803504ba1f206860c053663bb9ce74f761c3ae25c4c11cc`. The contract raises only
  the single EXAMS response/file allowance to 48 MiB, freezes the minimal four-request retry plan,
  preserves the first execution root read-only, and reserves 151u/151v without creating them.
  AGENTS.md, `ssh-client/README.md`, the documentation index and this handoff were aligned. No
  HU/SSH, network, retry execution, result/gate document creation or numbered-document mutation
  occurred.
- **2026-08-07 — 151t route correction requested:** Independent review found that the three model
  card requests freeze Hugging Face browser `/blob/` routes instead of immutable raw `/resolve/`
  artifact routes. Current task reset to `READY_FOR_USER_HANDOFF` for one local append-only route
  correction and re-freeze. Only this handoff changed; no execution or numbered-document mutation
  occurred.
- **2026-08-07 — Completed:** Appended the 151t Hugging Face route correction, preserved the
  pre-correction SHA-256 `eef968538b2022250803504ba1f206860c053663bb9ce74f761c3ae25c4c11cc`, and
  froze final SHA-256 `63951ba5543c2c803e8466d0c43e0aace9637ca1239164dc1d9f5e49ea75f46b`. The three
  effective model-card URLs now use immutable `/resolve/` paths with raw-byte, redirect-chain,
  content-type and fail-closed integrity requirements. AGENTS.md, `ssh-client/README.md`, the
  documentation index and this handoff were aligned. No HU/SSH, network, retry, 151u/151v or
  experimental operation occurred.
- **2026-08-07 — Completed:** Under one explicit Single Next Authorization Request, verified
  Document 151t SHA-256 `63951ba5543c2c803e8466d0c43e0aace9637ca1239164dc1d9f5e49ea75f46b`, ran the
  mandatory HU preflight, reconciled the first root read-only, executed the bounded retry once and
  completed the post-run storage audit. The result was PASS: 7 HTTP requests, 0 retries, exact
  EXAMS SHA-256 `96960451dd4c41f208ee5ab3bdbce8ea06ecd3506faa830e3ce61d316222c2f3`, all three raw
  model-card rows, unchanged old root, and no prohibited operations. Created Documents 151u/151v
  and aligned AGENTS.md, `ssh-client/README.md`, the documentation index and this handoff. The
  global gate remains `blocked_by_measurement_design`; training and `ready_to_train` remain blocked.
- **2026-08-07 — Coverage PASS validation requested:** Independent local review found that 151t
  requires one coverage row per required entity-field while 151u/151v report and accept only six
  coverage rows. Current task reset to `READY_FOR_USER_HANDOFF` for a bounded read-only inspection
  of the existing first/retry registries and conditional preparation of an unexecuted repair
  contract. Only this handoff changed; no HU access or numbered-document mutation occurred.
- **2026-08-07 — Completed:** Under the explicit coverage-validation authorization, performed
  the bounded HU read-only inspection of the listed registry/reconciliation/report files and
  parent-root inventories. Confirmed the six-row retry coverage/schema violation and incomplete
  benchmark/model registries; preserved all successful artifact/hash/storage facts and left
  151t/151u/151v unchanged. Created 151w as the current validation authority and froze unexecuted
  151x as the minimal repair contract. Updated AGENTS.md, `ssh-client/README.md`, the documentation
  index and this handoff. No HU write, network, scoring, inference, weights/tokenizers, corpus,
  GPU/Slurm, training, cleanup or Documents 152--154 action occurred.
- **2026-08-08 — 151x protocol correction requested:** Independent review accepted 151w's
  correction but found 151x's unnamed self-including output-manifest rule impossible to satisfy.
  Current task reset to `READY_FOR_USER_HANDOFF` for a local append-only manifest-protocol
  correction, an exact 150-row required matrix freeze and re-hash. Only this handoff changed; no
  execution or numbered-document mutation occurred.
- **2026-08-08 — Completed:** Appended the effective 151x output-manifest protocol correction,
  preserved pre-correction SHA-256
  `a19ed3b7e15540fa2810d5f483b2015cc5badd2bd41949d8678f945d3a6fb32e`, and froze final SHA-256
  `9c66cb95ee264d8411750d8e79aff258eaaeb4d1789ffc77bc8e162ffc93555b`. Named
  `output_artifact_manifest.jsonl`, the self-reference-free final-audit chain, exact 150-row
  required coverage cardinality and mandatory HU preflight are now explicit. Updated AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. No HU/SSH, network, 151x
  execution, repair-root/151y/151z creation or experimental action occurred.
- **2026-08-08 — 151x execution handoff prepared:** Final review accepted corrected 151x and fixed
  the living handoff's contradictory exclusion of reserved Documents 151y/151z. Current task reset
  to `READY_FOR_USER_HANDOFF` for exactly one bounded 151x execution with result/gate creation.
  Only this handoff changed; no HU or numbered-document action occurred.
- **2026-08-08 — Completed:** Under the explicit Single Next Authorization Request, verified
  corrected 151x SHA-256 `9c66cb95ee264d8411750d8e79aff258eaaeb4d1789ffc77bc8e162ffc93555b`, ran
  exactly one bounded execution with mandatory HU preflight and immutable-input read-only access,
  and wrote only the nine ordered outputs under
  `/vol/tmp2/yesildau/luna_benchmark_model_metadata_registry_coverage_repair_v1`. The output root
  has 9 files / 203,993 bytes and inventory SHA-256
  `7bce6b0d70c8069595d9c8ca96801b2eca1faf5a31973b8741e176926ef26e82`; both prior roots remained
  unchanged. The 150-row schema and output/final-audit chain completed, but required benchmark
  evidence and source-model provenance remained incomplete, so the fail-closed result is
  `BLOCKED` with global `blocked_by_measurement_design`. Created 151y (SHA-256
  `1309af278901009c22d2ee5b2438fdec886abe27cdaa60c4555dcd3af42ae6ba`) and 151z (SHA-256
  `51e3cdda3db8a636f1308a42910c2dd76bfdca5ef0906a3a316dc639c4b984db`). No public network,
  scoring, inference, weights/tokenizers, corpus, GPU/Slurm, training, cleanup/deletion or
  Documents 152--154 action occurred.
- **2026-08-08 — Measurement-design handoff prepared:** Independent review accepted the 151x
  BLOCKED result as truthful evidence incompleteness, identified the seven-versus-eight manifest
  narrative discrepancy and reset the current task to `READY_FOR_USER_HANDOFF` for a six-file
  read-only gap inventory plus local measurement-design authority preparation. Only this handoff
  changed; no HU or numbered-document action occurred.
- **2026-08-08 — Completed:** Under the explicit current-task authorization, inspected exactly the
  six listed 151x repair-root files on HU read-only and made no HU changes. Verified the manifest’s
  actual 7 rows, corrected the 151y seven/eight narrative through 151aa, and classified all 54
  non-verified fields in the exact 150-row matrix. Created 151aa with SHA-256
  `0a063d7d7465eb8bffdfa47a55fa95adc8420cef0a641e9d967c19ef6cdb69ae` and froze unexecuted 151ab
  with SHA-256 `500b24f6945272cbf7ddb0f26e95449434857bcac89ed5fb5d593e3fd189b4dd`. Updated
  AGENTS.md, `ssh-client/README.md`, the documentation index and this handoff. The measurement
  design remains blocked pending review-required fields; no network, downloads, measurement,
  scoring, inference, corpus, GPU/Slurm, training, cleanup or Documents 152–154 action occurred.
- **2026-08-08 — Completed:** Under the explicit “Resolve vngrs release metadata and
  repair/re-freeze 151ah” authorization, performed only the bounded official-public-metadata review
  and local read-only implementation audit. Verified immutable revision
  `ee5c6201ee84457a18182bfc483a7d8a7f3655ba`, 50,336,214 train rows, schema
  `text/corpus/original_id`, CC BY-NC-SA 4.0 metadata and the 284-shard tree. Exact per-shard
  object/SHA/footer/license-byte evidence and sample-based tokenizer-yield evidence remained
  unresolved. Appended the structural correction to 151ah, preserved pre-correction SHA-256
  `a8c1d1d2082ec3ae5b31ace5dc0a9506ace90f82d0f7bd1a2c1a528069ef2269`, and froze current SHA-256
  `9151da7112b6d1ab9bbb3b483b202dec23449624beeddb53c23682569a0f598b`. The candidate domain is
  bounded to at most 32 whole shards, but no executable selected list is frozen; status remains
  `PREPARATION_BLOCKED`, with `blocked_by_measurement_design` primary and
  `blocked_by_corpus_selection_or_materialization` contributing. Updated all authority files;
  no HU/SSH, full-shard/corpus-row download, materialization, execution, scoring, inference,
  evaluation, GPU/Slurm, training, cleanup or 151ai/151aj/152–154 action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only “Make 151an metadata/footer feasibility
  execution-ready” authorization, corrected the local vngrs metadata/footer validator and tests,
  appended the 151an execution-profile correction, preserved pre-correction SHA-256
  `435e0c25cedd7fd8fcb70862c637040300c2d5b201bfb5fa25c2b20232e71096`, and froze current SHA-256
  `572a14636dfc44f23cdff5ac536838ea671a488ddcd24968097bc4942bb0d4e4`. Focused verification was
  `34 passed, 1 skipped in 11.15s`; compatible verification was `264 passed, 8 skipped in
  16.97s` (`real 17.14s`) with the same three collection exclusions. The corrected profile is
  `EXECUTION_READY_METADATA_FOOTER_ONLY` but remains unexecuted; it freezes the exact root,
  direct routes, one route-kind vocabulary, separate object/metadata hashes, footer/license byte
  bindings, zero-row scope, schedule projection and bounded outputs. Updated AGENTS.md,
  `ssh-client/README.md`, the documentation index and this handoff. No HU/SSH, network,
  source/footer/row access, execution, corpus, scoring, inference, evaluation, GPU/Slurm,
  training, cleanup or Documents 151ao/151ap/152–154 action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only “Close the final 151an footer/parser/
  ledger byte-integrity gaps” authorization, preserved the prior 151an SHA-256
  `572a14636dfc44f23cdff5ac536838ea671a488ddcd24968097bc4942bb0d4e4`, appended the strict
  complete-footer/parser, two-stage range, shared-HEAD, retry-chain, actual-response-byte,
  canonical-manifest and final-audit correction, and froze current SHA-256
  `e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac`. Focused verification was
  `36 passed, 1 skipped`; compatible verification was `266 passed, 8 skipped` with the same three
  collection exclusions. The corrected profile remains `FROZEN — UNEXECUTED —
  EXECUTION_READY_METADATA_FOOTER_ONLY`; primary gate remains `blocked_by_measurement_design` with
  `blocked_by_corpus_selection_or_materialization` contributing. No HU/SSH, network, source/footer
  access, execution, corpus, scoring, inference, evaluation, GPU/Slurm, training, cleanup or
  Documents 151ao/151ap/152–154 action occurred.
- **2026-08-08 — Independent post-correction validation:** Recomputed 151an SHA-256 as
  `e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac`, reproduced
  `36 passed, 1 skipped` focused and `266 passed, 8 skipped in 17.39s` compatible, and confirmed
  that the local skip is the unavailable PyArrow compatibility fixture. Read-only code/contract
  inspection found that retryable attempts are incorrectly forced through terminal-success
  status/footer parsing and shard-artifact rules, making genuine 429/503/transport failures
  unrepresentable. It also found the worst-case arithmetic conflict
  `97 base artifacts + 28 retry artifacts + 7 top-level outputs = 132 > 128`. No HU/network or
  source access occurred. Execution authorization was withdrawn from the living next step and the
  task was reset to `READY_FOR_USER_HANDOFF` for one local-only retry/bound correction.
- **2026-08-08 — Retry/bound correction independently accepted:** Recomputed current 151an
  SHA-256 as `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`, reproduced
  `40 passed, 2 skipped` focused and `270 passed, 9 skipped in 17.60s` compatible, and inspected
  the genuine 429/no-response retry, relabelled-success rejection and exact 24/25 boundary
  controls. The two skips are PyArrow-dependent and the independent-writer parser test remains a
  mandatory execution-time pre-source-access check. No reusable 151an HTTP executor currently
  exists in the repository, so the current task now explicitly requires an inspectable executor
  plus its tests before the one bounded wave. The handoff status was reset to
  `READY_FOR_USER_HANDOFF`, stale duplicate next-authorization headings were marked historical,
  and only Documents 151ao/151ap were authorized as future execution result/gate records. No
  HU/network/source access occurred during this validation.
- **2026-08-08 — Executor push withheld after independent review:** Verified local HEAD commit
  `cf6307c2055e4d1c947e9807b1af4cb8b03db70b`, reproduced `41 passed, 2 skipped` focused and
  `271 passed, 9 skipped in 17.52s` compatible, and confirmed that no push/HU execution occurred.
  Fake-response probes reproduced two executor defects: 24 retryable failures block the terminal
  success (`attempts=24`, `retries=24`), and a monkeypatched 10-byte cumulative bound accepted 12
  response bytes. Static inspection also confirmed that `post_run_storage_audit()` is defined but
  never invoked and that preflight subprocesses have no timeouts or 30-GB home-rule evaluation.
  The commit contains 13 files / 6,243 inserted lines—the full vngrs package/tests—not only the
  executor. The current task was reset to `READY_FOR_USER_HANDOFF` for one local-only follow-up
  repair; external push, HU/network and 151an execution remain unauthorized.
- **2026-08-08 — Completed:** Under the explicit local-only “Repair 151an retry semantics and
  worst-case artifact bounds” authorization, preserved prior 151an SHA-256
  `e23ae18d35791e91d05f094fe7c675871214df6a9fe9714a660ae703fe84a0ac`, appended explicit 429/503,
  no-response transport, response-artifact and retry-chain rules, reduced the effective retry
  ceiling to 24, and froze current SHA-256
  `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`. Local verification was
  `40 passed, 2 skipped` focused and `270 passed, 9 skipped` compatible with the same three
  exclusions plus the explicit independent-writer compatibility skip. No HU/SSH, network,
  source/footer access, 151an execution, corpus, scoring, inference, evaluation, GPU/Slurm,
  training, cleanup or Documents 151ao/151ap/152–154 action occurred.
- **2026-08-08 — Completed:** Under the explicit local-only “Repair and test the 151an executor
  before any push” authorization, preserved `cf6307c2055e4d1c947e9807b1af4cb8b03db70b` and created
  separate follow-up commit `22e1c305726c6214dd5325391d5e9cb943ebd119`. Focused verification passed
  `50 passed, 2 skipped`; the compatible repository suite passed `281 passed, 9 skipped` after
  excluding the three documented collection failures. Added fake-opener coverage for the exact
  24-retry-to-terminal-success boundary, 25th-retry rejection, cumulative 64 MiB overflow,
  response-artifact slot bounds, non-retryable 3xx context, preflight timeout/home-limit/root
  failures, writer-check ordering and PASS/source-BLOCKED post-run audit invocation. No push,
  HU/SSH, network, 151an execution, scratch-root creation, 151ao/151ap creation, corpus,
  scoring, inference, evaluation, GPU/Slurm, training, cleanup or Documents 152–154 action
  occurred. Current status remains `READY_FOR_USER_HANDOFF`; a separately authorized 151an
  execution is still required before any source/footer access.
- **2026-08-08 — Follow-up executor review requires one final local repair:** Verified actual
  follow-up commit `22e1c305726c6214dd5325391d5e9cb943ebd119` (correcting the reported hash that
  omitted the `1` after `...2539`), reproduced `50 passed, 2 skipped` focused and `281 passed,
  9 skipped in 16.96s` compatible, and confirmed the retry/artifact/audit improvements. A bounded
  fake-response probe nevertheless showed that with a monkeypatched 10-byte cumulative budget and
  one byte remaining, `_read_bounded()` consumed a 100-byte body before the later cumulative
  check blocked. A second probe showed unparseable successful `du` output yields
  `home_usage_bytes=null`, no errors and `complete=true`. Current task reset to
  `READY_FOR_USER_HANDOFF` for one local-only body-read/preflight-parse correction; no push,
  HU/network or execution is authorized.
- **2026-08-08 — Completed:** Under the explicit local-only “Close final executor read-budget and
  preflight-parse gaps” authorization, preserved commits
  `cf6307c2055e4d1c947e9807b1af4cb8b03db70b` and
  `22e1c305726c6214dd5325391d5e9cb943ebd119`, and created one narrow follow-up commit
  `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`. The executor now performs remaining-budget-aware
  normal and `HTTPError` reads, uses trusted declared lengths before body consumption, and fails
  storage preflight closed on empty/unparseable successful `du` output. Focused verification was
  `56 passed, 2 skipped`; compatible verification was `286 passed, 9 skipped` with the same three
  collection exclusions. The 151an SHA remains
  `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`. No push, HU/SSH, network,
  151an execution, 151ao/151ap creation or prohibited experimental action occurred. Current
  status is `COMPLETED`; a separately authorized bounded 151an execution remains the only next
  request.
- **2026-08-09 — Local executor correction independently accepted; next authority repaired:**
  Reverified the exact three-commit chain ending at
  `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`, reproduced `56 passed, 2 skipped` focused and
  `286 passed, 9 skipped` compatible, confirmed that local `corpus-update` is three commits ahead
  of recorded `origin/corpus-update` base `9f17552`, and recomputed unchanged 151an SHA-256
  `937ec22c4b0f32d6c15a43ef872e497a0c62266383d46d2e74fca9069f952b79`. No push, HU/network or
  151an execution occurred. The prior next request was operationally circular because it required
  an HU execution while explicitly excluding the Git publication needed to synchronize the new
  executor. The active task is now `READY_FOR_USER_HANDOFF` for one explicit, fail-closed wave:
  verify the remote base, non-force-push exactly the three reviewed commits, synchronize HU, run
  mandatory preflight/PyArrow self-check, execute 151an exactly once, then write 151ao/151ap and
  update the listed authorities. All corpus-row/full-shard, model, scoring, GPU and training work
  remains forbidden.
- **2026-08-09 — 151ao/151ap independently reviewed; retry withheld:** Recomputed 151an/151ao/
  151ap hashes, verified the local remote-tracking branch at published `c1a3127`, and reconciled
  the report's zero executor/request/byte/artifact counts with its pre-source stop. The documents
  correctly classify the wave as an operational BLOCKED result and do not claim route or corpus
  failure. Exact raw classification of the 42 HU status entries and exact `du` exit/stdout/stderr/
  timeout evidence are not embedded deeply enough to select a safe reconciliation method. The
  current task is therefore `READY_FOR_USER_HANDOFF` for one bounded HU-source-read-only diagnostic
  and local Document 151aq; all HU writes, Git reconciliation, source access and 151an retry remain
  unauthorized.
- **2026-08-09 — 151aq independently accepted; preservation-checked retry prepared:** Recomputed
  151aq SHA-256, verified its exact 39 `.D` plus three `?` classification and zero overlap with the
  13 incoming code/test paths, and confirmed that both 30-second `du` commands completed normally
  with exit 0 (`14G` and `14687617024` bytes). The phrase “timeout 30 s” is the configured command
  bound, not an observed timeout; the current storage-preflight parse component is closed. The
  39 deletions are confined to tracked artifact/run paths while the incoming range changes only
  vngrs source/tests. Current task reset to `READY_FOR_USER_HANDOFF` for one explicit wave that
  preserves the canonical 42-entry status list, performs a single verified fetch plus
  `merge --ff-only` to `c1a3127`, reruns preflight/PyArrow and, only if every gate passes, retries
  151an exactly once with new append-only result/gate Documents 151ar/151as. No HU mutation or
  151an execution occurred during this validation.
- **2026-08-09 — Completed authorized 151an publication/execution wave:** Verified live remote base
  `9f1755219ba003d4aaf962558b3c0512fc74f99a`, ordinary non-force-pushed exactly the three reviewed
  commits ending at `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`, and verified the remote after push.
  HU was read-only inspected but remained dirty at the old base with 42 status entries, so no pull
  or executor call was made. The mandatory byte-form `du` preflight returned no parseable home
  usage; capacity/inode/path checks and a no-write post-attempt audit were recorded, and the frozen
  root remained absent. Created 151ao (`5b8cb4be094e78f9e37a927348efab4491e5f00355496f2996d1170494dfae46`)
  and 151ap (`aef81d9b72dd856b802310daaa4c64c7e37089ef22e2527cb6c88bbba8923468`) as an honest
  pre-source-access `BLOCKED` result/gate. HTTP attempts, retries, response bytes, artifacts and
  executor invocations were zero. No corpus, scoring, inference, evaluation, GPU/Slurm, training,
  cleanup/deletion or Documents 152–154 action occurred. The next request requires owner-controlled
  HU reconciliation and a new explicit 151an authorization.
- **2026-08-09 — Completed:** Under the explicit HU-source-read-only diagnostic authorization,
  verified live/local publication at `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`, inspected the HU
  `corpus-update` checkout at old base `9f1755219ba003d4aaf962558b3c0512fc74f99a`, and classified
  exactly 42 entries: 39 tracked `.D` worktree deletions plus three untracked top-level entries.
  The exact overlap with all 13 published `9f17552..c1a3127` paths was zero. The mandated bounded
  `du -xsh` returned exit 0 / 32 stdout bytes / 0 stderr bytes / `14G`; the bounded GNU byte-form
  returned exit 0 / 40 stdout bytes / 0 stderr bytes / `14687617024` bytes. Read-only capacity,
  inode and resolved-path checks passed; the 151an root remained absent. Created 151aq with
  SHA-256 `5a48d297ef5475550df41fd7e2baace4278acf54bbfb32bbfe455909dde7dbea`, updated the local
  authority files and preserved 151an/151ao/151ap unchanged. The current `du` component is closed,
  but the dirty HU checkout keeps new 151an execution `blocked_by_operational_access`; no HU
  mutation, source/footer access, PyArrow, executor, retry, corpus, scoring, evaluation, GPU,
  Slurm, training, cleanup or Documents 152–154 action occurred.
- **2026-08-09 — Completed:** Under the explicit preservation-checked execution authorization,
  reverified the 42-entry HU status blob and zero overlap, fetched only `origin corpus-update`,
  and performed exactly one ordinary `merge --ff-only` from `9f1755219ba003d4aaf962558b3c0512fc74f99a`
  to `c1a3127a4e4c6d9afd3bb0fd06741ce4114ecf23`. The status blob stayed byte-for-byte unchanged
  (6,989 bytes, SHA-256
  `71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`). Mandatory preflight and
  independent PyArrow 24.0.0 writer/parser checks passed. Frozen 151an was invoked exactly once;
  its first direct route request returned non-retryable HTTP 302, so the result was `BLOCKED` with
  one attempt, zero retries, zero response bytes, zero artifacts and an absent root. Created 151ar
  (`e531443254133a3ade95fcdf004420cc8726d28f337c7171c730937de3019967`) and 151as
  (`03c603265836320b173489a6659f91916c97db7ec78ebdd7b8faf0c1122a0ceb`) and aligned the authority
  files. 151an/151ao/151ap/151aq remained unchanged; no redirect, retry, corpus, scoring,
  evaluation, GPU/Slurm, training, cleanup or Documents 152–154 action occurred.
- **2026-08-09 — 151ar/151as independently accepted; route correction prepared:** Recomputed
  151ar/151as hashes and confirmed that reconciliation, storage preflight and independent PyArrow
  checks passed before the single executor invocation failed closed on the observed first-hop 302.
  Official Hugging Face documentation states that resolved large-file locations may be hosted on
  a content-addressed CDN, and official `huggingface_hub` tests expect LFS locations under
  `xethub.hf.co` or `cdn.hf.co`. The observed result is therefore a frozen empty-redirect-chain
  contract mismatch, not source unavailability. Current task reset to `READY_FOR_USER_HANDOFF`
  for one local/public-metadata-only Document 151at contract and executor/test correction with
  zero-or-one controlled redirect, secret-safe Location hashing, separate hop/retry accounting
  and terminal content-identity validation. No HU/source access or execution occurred in this
  independent review.
- **2026-08-09 — Completed:** Under the explicit local/public-metadata-only authorization, rehashed
  and preserved Documents 151an–151as, consulted only official Hugging Face documentation/source
  metadata, created and froze unexecuted Document 151at, and implemented the bounded secret-safe
  zero-or-one-hop CDN redirect protocol. The local validators/manifests now bind redacted Location
  metadata, separate logical request/retry/HTTP-hop arithmetic and retain the original HTTP status
  on bounded HTTPError response-read failure. Focused verification was `66 passed, 2 skipped`; the
  compatible suite was `305 passed, 9 skipped` after excluding only the local PyYAML collection
  failure in `tests/test_m1_cross_family.py`. Created one narrow local follow-up commit
  `de4a14e3370326173bdf04ce33356aae7826ddda`; no push, HU/network, vngrs route, source/footer
  byte, 151at execution or 151au/151av creation occurred. The operational gate remains
  `blocked_by_operational_access` and the global gate remains `blocked_by_measurement_design`.
- **2026-08-09 — Documentation alignment completed:** Read the restored comprehensive literature
  map, historical Document 60, active Document 145, master Document 100 and frozen Document 151at.
  Created Document 151aw (SHA-256
  `d28cf560dc8ee3eba0ca435d81df2651f88d30ef6f412f1b83e8c4bd0b6255a8`) and aligned Document 60,
  Document 145, the literature map, Document 100, AGENTS, README, index and this handoff. Corrected
  over-strong OLMo/Pythia exposure wording, separated model/corpus evidence roles, and made the
  future 151at authorization explicitly include ordinary publication plus preservation-checked HU
  synchronization. Protected 151an–151at hashes remained unchanged. No push, HU/network,
  evaluation, materialization or training occurred.
- **2026-08-09 — Completed with BLOCKED result:** Under the explicit Single Next Authorization
  Request, published only `de4a14e3370326173bdf04ce33356aae7826ddda` by ordinary non-force push,
  verified HU's 42-entry dirty state before/after exactly one `merge --ff-only`, and preserved its
  status SHA-256 `71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`. The mandatory
  30-second home-usage `du` timed out with no parseable value; `df`, inode, path and root-absence
  checks passed. PyArrow was `SKIPPED_PRE_FLIGHT`, the executor and source routes were not invoked,
  and the post-run audit recorded zero root files/bytes while its home `du` and large-file checks
  also timed out. Append-only Documents 151au/151av are `BLOCKED` with hashes
  `a83d832efa7478e86fa6bfa555cfe70d900f5284bf1bc862a8e5ca696d43fd8e` and
  `d25789ddece62628a9cd6913eb1cbb94306816413a5b0b7768fec85a2709944a`. No automatic retry,
  source/footer access, corpus, scoring, evaluation, GPU/Slurm, training, cleanup or Documents
  152–154 action occurred.
- **2026-08-09 — Completed:** Under the explicit local-only “HU Storage Preflight Resilience
  Correction” authorization, created frozen-unexecuted Document 151ax at SHA-256
  `15bdc5a7ae0e0356254c5d5ffd5ad47b091f459a52689ce4c0cb1ecc9699ed22`. Made exact-byte
  `du -x -B1 -s` with a 120-second bound primary, retained `du -xsh` as a diagnostic that cannot
  alone block after exact-byte PASS below 30 GiB, and added mandatory bounded filesystem/path/
  root/write-policy checks plus a manifest-hashed >500 MiB home-file audit with pre/post
  reconciliation. Focused verification was `75 passed, 2 skipped`; compatible verification was
  `314 passed, 9 skipped` with the documented collection exclusion. Created local follow-up commit
  `6ff9ceb13bbf2b9a4de19ba1db7788f11d239570`; no push, HU/SSH, network, source/footer access,
  151an/151at execution, corpus, calibration, materialization, scoring, evaluation, GPU/Slurm,
  training, cleanup or later numbered-document action occurred. The next separate authorization
  is ordinary non-force push plus preservation-checked HU fast-forward, corrected preflight and
  exactly one bounded execution.
- **2026-08-09 — Completed:** Under the explicit local-only final fail-closed post-run audit
  binding correction, preserved 151ax pre-clarification SHA-256
  `15bdc5a7ae0e0356254c5d5ffd5ad47b091f459a52689ce4c0cb1ecc9699ed22` and re-froze its
  append-only final SHA-256 as `b32550966e29f3398239e7be778cb20e3344e427bbec6f664fdda062c0e9eaff`.
  Updated the executor so top-level PASS requires source-stage PASS and post-run-audit PASS;
  source evidence is retained on audit BLOCKED/INCOMPLETE, and audit statuses distinguish
  definitive violations from incomplete evidence. Focused verification was `82 passed, 2 skipped`;
  compatible verification was `321 passed, 9 skipped`. Created unpushed follow-up commit
  `92460a00ec136dd885b4940184bee9d954da9106` after preserving `6ff9ceb13bbf2b9a4de19ba1db7788f11d239570`.
  No HU/SSH, network, source/footer, 151an/151at execution, corpus, scoring, evaluation,
  GPU/Slurm, training, cleanup or later numbered-document action occurred.
- **2026-08-09 — BLOCKED:** Under the explicit authorization for one publication-checked
  151an/151at wave, the live remote guard returned
  `2ff1cacdffd55820fdf9a8f633c2bc20bffac807` for `origin/corpus-update`, while the authorization
  required base `de4a14e3370326173bdf04ce33356aae7826ddda`. The wave stopped before push, HU/SSH,
  fetch/merge, dirty-state inspection, preflight, PyArrow or executor invocation. Created
  Document 151ay at SHA-256
  `a98ba8b8ddcd95742e7956c76c3ffc7364ade716ed4d0a45c8a6ca8fe352b23b` and Document 151az at
  SHA-256 `c161a0eac7fe2c619511a30419d1ae6168c76ea83d25e4922e18cec2d968ede5`. Operational gate
  remains `blocked_by_operational_access`; global gate remains `blocked_by_measurement_design`.
- **2026-08-10 — BLOCKED:** Under the revised-base authorization, verified live remote base
  `2ff1cacdffd55820fdf9a8f633c2bc20bffac807`, merge-base equality, local ahead 2 and remote ahead
  0, then published only the reviewed chain ending at
  `92460a00ec136dd885b4940184bee9d954da9106` by ordinary non-force push. HU preservation
  checks passed; exactly one fetch and one `merge --ff-only` advanced HU from `2ff1cac...` to
  `92460a0...` while preserving 42 entries, 39 tracked `.D`, 3 untracked entries and status
  SHA-256 `71a2e3b1d03a5c73ab3fb16c02e59910b7ec35f553956b079edcf71f3a3c59e9`; incoming overlap
  was zero. Corrected 151ax preflight failed closed because exact-byte `du -x -B1 -s` timed out
  at 120 seconds and diagnostic `du -xsh` timed out at 30 seconds. PyArrow and the executor
  were not run; all request/hop/retry/response/file counts were zero. Mandatory post-run audit
  PASSed with post-run exact usage `14689083392`, unchanged large-file manifest and an absent
  0-file/0-byte root, but this did not reverse the preflight BLOCKED result. Created 151ba
  with current SHA-256 `d14c3b31b4d35517fbbcfaac2706b20f4108241c25f21b51c0e3157f0373ae26` and 151bb
  with current SHA-256 `2ffaf1eed56a8c895dbb48715b4bab07d9e3d368363ee3a81dc7bdff2c34c606`. A later
  read-only check observed local and remote at `210e47256a499d098da9879d7ade990527cdbe35`; it
  was not part of the authorized push and no further operation followed. Documents
  151ay/151az remained unchanged; no source/footer, corpus, scoring, evaluation, GPU/Slurm,
  training, cleanup or Documents 153–154 action occurred. The next authorization must reverify
  remote/HU state and obtain a successful preflight; no automatic retry is authorized.
- **2026-08-10 — BLOCKED:** Under the explicit HU-only prewarmed retry authorization, no
  push/fetch/merge or HU movement occurred. A bounded SSH read-only probe timed out after
  `30.004 s` with return code `null`, 226 stdout bytes containing only the spawn line and 0
  stderr bytes. The live expected HEAD/status/root/large-file manifest/exact-byte values were
  not obtained; historical values were not substituted. Internal preflight, PyArrow and
  executor invocation were zero. The mandatory post-run audit probe also timed out after
  `30.005 s` before returning a remote result. Created 151bc at SHA-256
  `376c5e380ba1fa22262626b66b531d19f9333e168a2ffe3c86017b1218726edc` and 151bd at SHA-256
  `c9544bbe410c2d4353ef6b1f1c4c72debd269bb3122d0c0de559b46680d61683`. Documents 151ba/151bb
  were preserved; no source/footer, corpus, scoring, evaluation, GPU/Slurm, training,
  cleanup or Documents 153–154 action occurred. The gate remains
  `blocked_by_operational_access` with global `blocked_by_measurement_design`.
