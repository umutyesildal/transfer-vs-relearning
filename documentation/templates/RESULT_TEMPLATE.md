# <Result name>

**Contract:** `<path>` | **Run identity:** `<id>`
**Status:** `completed | partial | failed | NOT_RUN`
**Date:** YYYY-MM-DD

## Outcome

State what occurred without changing the contract or hiding failures.

## Identity and integrity

Record code/data/model/config/environment identities, output root, manifest hash, and verification.

## Results

Link machine-readable normalized tables and report denominators, uncertainty, and missing rows.

## Gate

Evaluate only the precommitted gate. Separate scientific negative results from operational
`NOT_RUN` evidence.

## Artifacts and retention

List retained paths, hashes, sizes, and retention class. Record cleanup only if separately
authorized and verified.

## State transition

State the exact `PROJECT_STATE.yaml` fields this result justifies changing.
